> 来源: [https://deepwiki.com/deepseek-ai/DualPipe/4.2-communication-system](https://deepwiki.com/deepseek-ai/DualPipe/4.2-communication-system)
> DeepWiki deepseek-ai/DualPipe

# Communication System

  Relevant source files 
 - [dualpipe/comm.py](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py)
 
  The Communication System in DualPipe provides efficient point-to-point (P2P) tensor exchange between pipeline stages during distributed training. This page documents the communication utilities, patterns, and implementation details used by both DualPipe and DualPipeV to enable overlapped computation and communication. For information about the Weight Gradient Management system that works alongside this communication system, see [Weight Gradient Management](https://deepwiki.com/deepseek-ai/DualPipe/4.1-weight-gradient-management).

 
## Overview

 The DualPipe communication system enables efficient tensor exchange between pipeline stages by:

 
 - Pre-registering tensor shapes and data types to avoid metadata transfer during runtime.
 - Using non-blocking point-to-point communication primitives (`dist.P2POp`) for overlap.
 - Batching communication operations for efficient execution via `dist.batch_isend_irecv`.
 - Translating local group ranks to global ranks for compatibility with `torch.distributed`.
 
 
### Natural Language to Code Entity Mapping

 
```

```

 Sources: [dualpipe/comm.py7-38](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L7-L38)

 
## Global State Configuration

 Before performing any communication, the tensor shapes and data type must be registered. This pre-registration allows the system to create properly sized buffers without transferring metadata during execution.

 
| Function | Code Entity | Description |
|---|---|---|
| Set Shapes | set_p2p_tensor_shapes(shapes) | Sets the global TENSOR_SHAPES list. dualpipe/comm.py11-13 |
| Set Dtype | set_p2p_tensor_dtype(dtype) | Sets the global TENSOR_DTYPE variable. dualpipe/comm.py16-18 |

 The communication module uses these variables in `build_from_tensor_shapes` to allocate `torch.empty` tensors on the CUDA device with `requires_grad=True`.

 Sources: [dualpipe/comm.py7-23](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L7-L23)

 
## Buffer Allocation and Rank Translation

 The communication system abstracts the complexity of buffer management and rank mapping through two primary functions: `append_irecv` and `append_isend`.

 
### Rank Translation logic

 Both functions perform global rank translation using `dist.distributed_c10d.get_global_rank(group, rank)` before creating a `dist.P2POp`. This ensures that even when pipeline stages are defined within sub-groups (e.g., in complex 3D parallelism), the P2P operations correctly target the physical ranks.

 
### Communication Primitives

 
```

```

 
 - **`append_irecv(ops, src, group)`**:

 
 - Calls `build_from_tensor_shapes()` to allocate buffers. [dualpipe/comm.py26](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L26-L26)
 - Translates the `src` rank. [dualpipe/comm.py27](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L27-L27)
 - Appends a `dist.P2POp` with `dist.irecv` for each tensor in the shape list. [dualpipe/comm.py30](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L30-L30)
 - Returns the list of allocated tensors to the caller. [dualpipe/comm.py31](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L31-L31)
 - **`append_isend(ops, tensors, dst, group)`**:

 
 - Translates the `dst` rank. [dualpipe/comm.py35](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L35-L35)
 - Appends a `dist.P2POp` with `dist.isend` for each non-null tensor provided. [dualpipe/comm.py38](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L38-L38)
 
 Sources: [dualpipe/comm.py25-38](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/comm.py#L25-L38)

 
## Batching and Execution Flow

 DualPipe and DualPipeV do not execute communication operations immediately. Instead, they accumulate operations into a `comm_ops` list and execute them in a single batch.

 
### Pipeline Stage Integration

 The pipeline classes (`DualPipe` and `DualPipeV`) manage a `self.comm_ops` list and a `self.to_free` list for memory management.

 
```

```

 
### Safety Checks

 Before execution, both `DualPipe` and `DualPipeV` verify that global state is initialized:

 
```

```

 Sources: [dualpipe/dualpipe.py325-326](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/dualpipe.py#L325-L326) [dualpipe/dualpipev.py311-312](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/dualpipev.py#L311-L312)

 
### View Tensor Assertion

 To prevent memory corruption or unexpected behavior during `_free_tensors`, the system asserts that communicated tensors are not views:

 
```

```

 Sources: [dualpipe/dualpipe.py227-231](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/dualpipe.py#L227-L231) [dualpipe/dualpipev.py228-231](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/dualpipev.py#L228-L231)

 
## Memory Management

 The communication system relies on the pipeline stage to track and release memory.

 
 - **Send Buffers**: Tensors passed to `append_isend` are typically added to `self.to_free` in the calling stage logic.
 - **Receive Buffers**: Tensors created by `append_irecv` are managed by the computation chunks and eventually cleared after their backward pass or once they are no longer needed.
 - **Cleanup**: The `_free_tensors()` method in the pipeline classes sets the tensor data to a small 1-element tensor to trigger memory release while maintaining the object reference if necessary.
 
 Sources: [dualpipe/dualpipe.py279-284](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/dualpipe.py#L279-L284) [dualpipe/dualpipev.py279-286](https://github.com/deepseek-ai/DualPipe/blob/030ce432/dualpipe/dualpipev.py#L279-L286)

# 迭代质量门：费曼闭环的工程化实现

> 一句话灵魂：**费曼学习法不是"讲通俗"，而是"测量并修复故事与真实之间的 KL 散度"。把它工程化，就是给知识迭代器装上自动刹车。**

承接 `故事即世界迭代器-元理论.md` 断言 3（迭代 = 有方向的修正）和断言 5（统一生成函数），本文把 L2 质量门做成可执行规范。

---

## 1. 为什么需要质量门

没有质量门的迭代 = **漂移**。每一版都在改，但不知道改好了还是改坏了。费曼 F1-F4 提供的就是**四道独立判据**，任一不过即打回。

类比软件：CI 不过不准合并。费曼 = 知识版本的 CI。

---

## 2. F1-F4 的工程化规格

| 门 | 名字 | 输入 | 输出 | 通过判据 | 失败处理 |
|---|---|---|---|---|---|
| **F1** | 外行复述 | 章节文本 | 复述文本 | 核心断言保留率 ≥ 80% | 重写直觉层 |
| **F2** | 卡壳自曝 | 章节文本 | ≥1 个 gap | gap 数 ≥ 1 | **强制重读，禁止发布** |
| **F3** | 术语翻译 | 术语清单 | 翻译表 | 覆盖率 100% | 补直觉比喻 |
| **F4** | 回炉记录 | v1 + gap | v2 + diff | diff 非空 | 标记"未迭代" |

**F2 是硬门**：项目内反复验证，"挖不出 gap"几乎 100% 是偷懒，不是真懂。

---

## 3. 与熵的硬连接

- F1 测量 **可还原熵**：$H(\text{concept} \| \text{retelling})$，越低越好。
- F2 测量 **局部 KL**：$KL(p_{\text{data}} \| p_{\text{story}})$ 的局部峰值，就是卡壳点。
- F3 测量 **术语依赖熵**：能否用更低熵的词替换术语。
- F4 记录 **熵减 diff**：$\Delta S = S(v_1) - S(v_2) > 0$ 才算有效迭代。

---

## 4. 自动化：与项目内 `feynman-coach.py` 对齐

`费曼学习法/` 下已有 `feynman-coach.py` AI 陪练脚本。本规范建议其判定逻辑：

```python
def feynman_gate(chapter_md):
    # F1
    retelling = simulate_outsider(chapter_md)
    if core_claim_retention(retelling) < 0.8:
        return FAIL("F1: 直觉层不达意")

    # F2
    gaps = extract_gaps(chapter_md, author_self_report=True)
    if len(gaps) == 0:
        return FAIL("F2: 未自曝 gap，疑似偷懒")

    # F3
    jargon = extract_jargon(chapter_md)
    translations = translate_jargon(jargon)
    if len(translations) < len(jargon):
        return FAIL(f"F3: {len(jargon)-len(translations)} 个术语未翻译")

    # F4
    if not has_iteration_diff(chapter_md):
        return FAIL("F4: 缺少回炉记录")

    return PASS()
```

---

## 5. 质量门与发布节奏

| 阶段 | 必过门 | 产出 |
|---|---|---|
| 草稿 | — | v0 |
| 内审 | F2 | v0.5 |
| 评审 | F1+F2+F3 | v1（可发布） |
| 迭代 | F4 | v2+ |

---

## 6. RL 视角重写：费曼闭环 = 一个 RL 训练循环

> 承接 [`强化学习视角-元迭代器.md`](./强化学习视角-元迭代器.md)。本节把 F1-F4 **重写为一个 RL 训练循环**，揭示它为什么是"唯一不能省略的组件"。

### 6.1 F1-F4 的 RL 翻译

| 费曼门 | RL 概念 | 说明 |
|--------|---------|------|
| **F1 外行复述** | **reward signal**（即时奖励） | 可还原熵 $H(\text{concept}\|\text{retelling})$ ↓ = reward ↑ |
| **F2 卡壳自曝** | **exploration**（主动找 high-uncertainty 状态） | 卡壳点 = 局部 $V(s)$ 低的点，必须被探测 |
| **F3 术语翻译** | **reward shaping**（降奖励的歧义性） | 术语=高熵编码，翻译=改写成低熵奖励 |
| **F4 回炉记录** | **policy update + credit assignment** | diff = 梯度方向 + 这次改动归因于哪个 gap |

### 6.2 费曼闭环 = policy iteration

把 `feynman_gate` 重写成 RL 的 policy iteration：

```python
def feynman_as_policy_iteration(chapter_state, max_iter=10):
    """
    费曼闭环 = 知识版本的 policy iteration
    """
    for i in range(max_iter):
        # ① policy evaluation: 评估当前章节的 value
        r_f1 = reward_outsider_retention(chapter_state)   # F1
        gaps = explore_for_gaps(chapter_state)             # F2
        
        # ② 如果 value 够高（F1 过 + F2 挖不出新 gap），收敛
        if r_f1 >= 0.8 and len(gaps) == 0:
            return CONVERGED(chapter_state)
        
        # ③ reward shaping: 术语翻译降低歧义
        chapter_state = translate_jargon(chapter_state)    # F3
        
        # ④ policy improvement: 朝 gap 方向更新（梯度下降的语义版）
        advantage = estimate_advantage_from_gaps(gaps)
        chapter_state = policy_update(chapter_state, gaps, advantage)  # F4
        
        # 记录 credit assignment: 这次改动是因为哪个 gap
        log_credit_assignment(iter=i, gaps=gaps, diff=diff)
    
    return chapter_state
```

### 6.3 为什么"F2 是硬门"——RL 的解释

F2（卡壳自曝）在 RL 语言里是 **exploration**。没有 exploration 的 RL = **纯利用（pure exploitation）**，必然困在局部最优。

> **"挖不出 gap 几乎 100% 是偷懒"= "没有 exploration 的策略必然过拟合当前 value estimate"**。F2 强制探索，是防止知识坍缩成"自以为懂"的局部最优。这就是为什么 F2 是**唯一不可省略**的门——它是 RL 闭环里 exploration 的唯一来源。

### 6.4 反模式 = negative reward 的显化

工程化手册里的"反模式 10 条"= **显式列出的 reward 负端**。每条反模式都在说"这样做 reward 会 ↓"。把它写出来 = 把 negative reward 做成可查表，让 policy update 时能避开。

### 6.5 一句话收束（RL 版）

> **费曼闭环不是"教学法"，是一个完整的 RL 训练循环：F1 给 reward，F2 做 exploration，F3 做 reward shaping，F4 做 policy update + credit assignment。四件套缺任何一个，闭环就断——这就是为什么它"唯一不能省略"。**

---

## 📌 收束

> 费曼闭环是这套生成器里**唯一不能省略的组件**。没有它，"简单可迭代"退化成"简单可漂移"，世界级无从谈起。它是知识版本的 CI/CD。
>
> **RL 视角补强**：它更本质地是一个 RL 训练循环——reward（F1）+ exploration（F2）+ reward shaping（F3）+ policy update（F4）。详见 [`强化学习视角-元迭代器.md`](./强化学习视角-元迭代器.md)。

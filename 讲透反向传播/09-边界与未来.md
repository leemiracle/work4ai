# 09 · 反传的边界与未来

> 反传统治深度学习，但它有局限：要求完全可微、需要完整计算图、和生物大脑迥异。本章讲这些边界怎么绕过，以及有没有替代反传的范式。

---

## 一、反传的局限

1. **要求完全可微**：不可导点用次梯度（ReLU 的 0），不可微操作（采样）要绕过。
2. **需要完整计算图**：要等整个前向跑完才能反传，无法在线增量学习。
3. **全局同步**：所有参数的梯度要一起算，和生物大脑的局部、异步学习完全不同。
4. **能源消耗**：训一个 GPT-4 要消耗海量电力（前向 + 反向各一遍）。

## 二、绕过不可微：reparameterization 与 straight-through

### 2.1 重参数化技巧（VAE）
采样 $z\sim\mathcal{N}(\mu,\sigma)$ 不可微。改写成 $z=\mu+\sigma\cdot\epsilon,\ \epsilon\sim\mathcal{N}(0,1)$——把随机性"外移"到 $\epsilon$，对 $\mu,\sigma$ 的路径就**可微**了，梯度能流过采样。

### 2.2 Straight-Through Estimator
量化/离散化前向不可微（argmax），但前向用硬离散、反传用软近似（sigmoid）的梯度"假装"流过。用于二值网络、VQ-VAE。

## 三、绕过"需要完整图"：graph break

`torch.compile` 捕获计算图时，遇到不可捕获部分（调 numpy、`Tensor.item()`）会**断图**（graph break），回退 eager。这是"反传/编译要求可捕获"约束在现代编译栈里的体现。

## 四、替代反传的范式

### 4.1 Forward-Forward Algorithm（Hinton 2022）
用**两次前向**（正样本/负样本各算一个 goodness）替代"前向+反向"。试图绕过反传的"全局可微 + 需要完整图"约束，更接近生物学习。目前精度不如反传，但是对"反传是否唯一可能"的重要探索。

### 4.2 Feedback Alignment
反传要求"前向权重"和"反向权重"是转置关系。Feedback Alignment 用**固定随机矩阵**传反向梯度，证明网络能学会自己对齐——挑战了反传的"精确转置"必要性，更接近生物（突触没有精确转置）。

### 4.3 预测编码（Predictive Coding）
类脑算法，层级预测误差驱动学习，局部更新，无需全局反传。

### 4.4 DPO（Direct Preference Optimization）
用于 LLM 对齐，绕过奖励模型的 RL（REINFORCE 的高方差），直接用偏好数据算可微损失——**本质仍是反传**，但奖励信号来自偏好数据而非奖励模型。

---

## 五、批判性视角：反传还是不可撼动的基石

- 这些替代范式**目前都不如反传**（精度/效率）。反传的"VJP + $m\ll n$"优势太硬。
- 但它们回应了真实担忧：反传的生物不合理性、能耗、对完整图的依赖。
- **短期**：反传仍是唯一主流；**长期**：若硬件范式（类脑/光计算）变化，或发现更高效的学习规则，格局可能变。

> 一句话：**反传是当前深度学习的唯一答案，但不是唯一可能的答案。** 理解它的边界，才知道它为什么赢、以及什么时候可能被替代。

---

## 📌 下一步

恭喜走完「讲透反向传播」全程。你现在能从"链式法则"一路讲到"PyTorch backward 的 mutation 边界"。回到体系：
- 想看 backward 在框架层如何实现 → [讲透PyTorch/01-Autograd](../讲透PyTorch/01-Autograd与计算图.md) + [10-内核精读](../讲透PyTorch/10-PyTorch内核精读.md)
- 想看梯度如何变成"学习" → [讲透泛化](../讲透泛化/README.md)
- 想看每层局部导数 → [讲透激活函数](../讲透激活函数/README.md)

## ✍️ 练习

1. reparameterization 如何让"采样"变得可微？写出 VAE 的重参数化。
2. Forward-Forward 与反传的根本区别是什么？它绕过了反传的哪个约束？
3. 反传为什么"生物不合理"？列出两点。
4. （开放）你认为反传会被替代吗？什么条件下会？

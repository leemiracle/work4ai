# 故事化框架生成器：把任意主题变成可迭代的故事（Prompt 级模板）

> 一句话灵魂：**这是把 `故事即世界迭代器-元理论.md` 做成可执行模板的文件。给我任意主题 X，按本模板填空，即可生成一个符合 L1-L4 四层规范的"讲透X"单元。**

---

## 0. 使用方法

```
输入：主题 X（如"注意力机制""线性回归""gRPC"）
输出：一个 README + N 章 + experiments/ + exercises.md 的完整目录
约束：强制走 L1 故事化 → L3 五层 → L2 费曼 → L4 工程 四道关
```

把下面的模板当**函数签名**，每一节是**参数槽**，填完即出稿。

---

## 1. README 模板（每个"讲透X"的入口）

```markdown
# 讲透 X

> 一句话灵魂：X 的本质是 ___。

承接「讲透 Y」，主题独立：Y 回答 ___，X 回答 ___。

## 学习路径
（Mermaid 图：节点=章节，边=依赖）

## 章节-实验对照表
| 章节 | 文件 | 实验 | 关键数字 |
|---|---|---|---|

## 实证速览
- 关键反直觉数字 1：___（如"参数 393 反而泛化更好"）
- 关键反直觉数字 2：___

## 教学宪法
每个概念按三层呈现：直觉（比喻）→ 数学（公式与边界）→ 代码（bash 跑通的实证）。
诚实标注：已证明 / 经验 / 未解。
```

---

## 2. 单章生成模板（每章固定五段）

```markdown
## N. 章节标题（动词开头，如"为什么 X 能 ___"）

### 直觉层
用一个人能记住的故事开场。模板：
> 想象你是 ___，面对 ___，传统做法是 ___，但 X 让你 ___。

角色 = ___。冲突 = ___。时刻 = ___（一个具体数字/场景）。

### 数学层
把直觉翻译成公式。必须标注公式的**适用边界**：
- 公式：$...$
- 成立条件：___
- 边界外会发生什么：___

### 代码层
紧跟一个能 `python xxx.py` 跑出真实数字的实验。**禁止伪代码**。
\`\`\`bash
$ python experiments/NN_xxx.py
# 输出：___（一个反直觉数字）
\`\`\`

### 不足层
诚实列 X 在此处的局限：
- 已证明的：___
- 经验但未证：___
- 未解：___

### 📌 下一步 + ✍️ 练习
- 下一章预告：___
- 练习：用 X 解决 ___，记录卡壳点（喂给费曼 F2）。
```

---

## 3. 费曼闭环质检清单（L2 质量门）

写完一章后**必须过**这道门，否则打回 L3 重写：

- [ ] **F1 外行复述**：随机找一个非本领域的人（或模拟），让他用自己的话复述。能复述 = 过。
- [ ] **F2 卡壳自曝**：至少挖出 1 个"我其实没懂"的点。挖不出 = 偷懒，强制重读。
- [ ] **F3 术语黑名单**：列出本章所有术语，每个给一句外行翻译。翻译不出 = 没懂。
- [ ] **F4 回炉记录**：v1 → gap → v2 的 diff 写在文件末尾。

**判定规则**：F2 挖不出 gap 的章节，**禁止发布**。这是项目的硬约束。

---

## 3.5 反欺骗门（F5，新增 · 欺骗动力学补丁）

> 承接 [`欺骗动力学-社会进步的隐秘引擎.md`](./欺骗动力学-社会进步的隐秘引擎.md) §5.1 + §6.1。F1-F4 防的是**作者对自己的欺骗**；F5 防的是**作者对读者的欺骗**。

写完一章后**还必须过**这道门：

- [ ] **F5 欺骗动力学视角**：本章最后加一节 `## 🎭 欺骗动力学视角`，回答三个问题：
  1. **X 防的是什么欺骗？**（X 是方法/技术/制度时）
  2. **X 可能被什么欺骗攻破？**（对抗性视角）
  3. **X 沉淀进了哪条反欺骗主链？**（文明定位）

**判定规则**：

- 如果 X 是一项**技术/方法/制度**——F5 必填，缺即打回。
- 如果 X 是一项**纯数学概念**（如群、流形）——F5 可填"X 在反欺骗主链上的位置：___"（如"群论 → 密码学 → 数字纪反诈地基"）。
- 如果 X 与欺骗完全无关——罕见，但允许跳过，必须在文件末注明"X 与欺骗动力学弱相关，跳过 F5"。

**反模式**：
- ❌ 把 F5 写成"X 也可以用来防骗"的牵强附会——必须找到 X **真的在反欺骗主链上**的位置，找不到就跳过。
- ❌ 把 F5 写成阴谋论/政治评论——F5 只讲**制度/技术演进**，不讲"谁在骗谁"。

**为什么这道门重要**：F1-F4 解决"作者懂不懂"，F5 解决"X 在文明里的真实位置"。没有 F5 的"讲透X"是技术内卷，加了 F5 的"讲透X"才能跨文件形成理论一致性。

---

## 4. 工程化收尾模板（L4 保障）

每份"讲透X"的最后必须有：

```markdown
## 反模式 10 条
1. 误把 X 当 ___ 用 → 正解：___
2. 忽略 ___ 边界 → 正解：___
...

## 选型决策树
（Mermaid flowchart）

## 跨系列引用
- 与「讲透 Y」的关系：___
- 与「工程化手册 Z」的关系：___
```

---

## 5. 完整管线（一图收束）

```
主题 X
  │
  ├─[L1 故事化]→ 选角色+冲突+时刻（一句话灵魂）
  │
  ├─[L3 五层]  → 生成 README + N 章 + experiments/
  │
  ├─[L2 费曼]  → 每章过 F1-F4 闭环，不过打回
  │
  ├─[L4 工程]  → 加反模式+选型树+跨引用
  │
  └─→ 发布 v1 → 进入下一轮迭代
```

---

## 📌 收束

> 这份模板本身也是一个"简单可迭代的故事"：它的原核是"角色+冲突+转变+时间"，它的迭代机制是"L1→L3→L2→L4"闭环。用这个故事去生成更多故事，就是用元理论去迭代整个项目。

---

## 6. RL 视角补强：把生成器重写为 policy network

> 承接 [`强化学习视角-元迭代器.md`](./强化学习视角-元迭代器.md)。本节把"五段模板"重写为一个 **policy network 的结构先验**。

### 6.1 五段 = policy 的五个组件

| 五段模板 | RL policy 的组件 | 作用 |
|----------|-----------------|------|
| **直觉层**（故事开场） | **reward shaping 的引子** | 用低熵叙事锚定 reward 方向 |
| **数学层**（公式+边界） | **policy 的形式约束** | 把 reward 限制在可验证的数学空间 |
| **代码层**（跑通实验） | **environment rollout** | 真实环境交互，产生 ground-truth reward |
| **不足层**（局限） | **value function 的下界标注** | 显式标出 $V(s)$ 低的地方 = 下一步 exploration 的靶子 |
| **下一步+练习** | **curriculum + exploration budget** | 给下一轮 episode 安排任务 |

### 6.2 单章生成 = 一次 episode

把"写一章"看作一次 RL episode：

```
状态 s_0 = 主题 X + 已有知识库
  │
  ├─ 动作 a_1: 选故事原核（L1）   → reward r_1 = 故事压缩比
  ├─ 动作 a_2: 写五段（L3）        → reward r_2 = 内部一致性
  ├─ 动作 a_3: 过费曼 F1-F4（L2）  → reward r_3 = 可懂度（核心 reward）
  ├─ 动作 a_4: 加工程化（L4）      → reward r_4 = 跨引用密度 + 反模式覆盖
  │
  终止: 累积 reward ≥ 阈值 → 发布 v1
```

### 6.3 把模板的"约束"翻译成 RL 约束

| 模板约束 | RL 约束 | 为什么 |
|----------|---------|--------|
| "禁止伪代码" | **environment 必须是真实可交互的** | 假环境给的 reward 不可信 |
| "必须标注适用边界" | **policy 必须知道自己的 out-of-distribution 行为** | 边界外 = 泛化失败区 |
| "必须诚实标注已证明/经验/未解" | **value estimate 的置信区间标注** | 防止 over-confident policy |
| "F2 挖不出 gap 禁止发布" | **没有 exploration 的 episode 不算完成** | 防止坍缩 |

### 6.4 用 RL 视角增强模板

在原模板基础上，每个"讲透X"单元建议再加：

```markdown
## RL 视角（新增段）
- **Reward**: 学完本章能独立解决 ___ 类问题（显式 reward）
- **State space**: 本章覆盖的状态集合 = ___
- **Action space**: 学完后能做的决策 = ___
- **Exploration points**: 本章故意留的"可深挖"点 = ___
- **Credit assignment**: 本章最关键的 1 个洞察 = ___（如果只记一件事）
```

这一段把每章从"被动知识"变成"可激活的 RL 单元"——读者知道学完后能做什么决策（action）、该往哪深挖（exploration）、最该记住什么（credit assignment）。

### 6.5 一句话收束（RL 版）

> **故事化框架生成器 = 一个带强结构先验的 policy network。五段模板 = policy 的五个组件，L1→L3→L2→L4 闭环 = 一个完整 episode。用它生成"讲透X"，就是用结构化的 policy 去做知识域的 rollout，并用费曼闭环收 reward。**

---

# 2025-2026 顶会高效 AI 论文精选

> 从 **NeurIPS 2025 / MLSys 2025-2026 / ACL 2026 / arXiv 2026** 录取论文中精选**高效 AI / LLM 推理**方向的高影响力工作。按主题分章，每篇给「核心 + 关键数字 + 对接 work4ai」。
>
> **为什么有这份精选**：2025-2026 高效 AI 的主线清晰聚焦在三点——**① 稀疏 attention / KV 压缩 ② Prefill-Decode 分离式推理服务 ③ 推理时计算扩展（inference-time scaling）**。这份精选把这三条主线的代表作系统化，补 [`高效AI研究参考-MITHANLab`](高效AI研究参考-MITHANLab.md)（单实验室纵深）的横切面。
>
> **整理日期**：2026-08-10 ｜ **来源**：顶会官方 proceedings + arXiv 一手核实

---

## 一、2025-2026 高效 AI 的三大主线

```
主线 1: 稀疏 attention / KV 压缩 ──── 让长上下文 LLM 跑得动
  Twilight / Δ-Attention / MUSTAFAR / DMS / LServe

主线 2: Prefill-Decode 分离式服务 ──── 让推理服务吞吐翻倍
  NVIDIA Disaggregation / Meta 部署 / LAPS / TriInfer / KVServe

主线 3: 推理时计算扩展 ──── 用 KV 压缩换更多 reasoning token
  DMS (Hyper-Scaling) / SparseSpec (Self-Speculation)
```

---

## 二、主题 ①：稀疏 attention / KV 压缩（最热）

> 2025-2026 最高产的方向——长上下文 LLM 的 attention 是 $O(n^2)$，KV cache 是显存大头。所有工作都在"哪些 token 可以跳过/压缩"上做文章。

### LServe（MLSys 2025，HAN Lab）⭐
- **全称**：Efficient Long-sequence LLM Serving with Unified Sparse Attention
- **核心**：**统一混合稀疏 attention**——把静态稀疏（streaming heads，一半头转成 Λ 形 mask）+ 动态稀疏（query-centric KV page 选择）+ KV 量化融合进单个 GPU kernel。关键洞察：静态与动态稀疏**正交**，可叠加。
- **关键数字**：vs vLLM，**prefill 加速 2.9×，decode 加速 1.3-2.1×**，精度无损；KV 常量 page（如 4096）与上下文长度无关。
- **对接**：[`讲透GPU与系统级`](讲透GPU与系统级/)（长上下文推理）/ [`高效AI研究参考-MITHANLab`](高效AI研究参考-MITHANLab.md)（omniserve 仓库）

### Twilight（NeurIPS 2025）
- **核心**：**自适应稀疏预算**——现有 top-k 稀疏用固定 budget，Twilight 借鉴 top-p sampling 做"select-then-prune"分层剪枝。可作为任何稀疏 attention 的**优化器插件**。
- **关键数字**：剪枝达 **98% token 近乎无损**；vs FlashAttention2 **15.8× 加速**（attention 算子）；端到端 decode **1.35× 加速**。
- **对接**：[`讲透基础模型`](讲透基础模型/)（长上下文 attention）

### Δ Attention（NeurIPS 2025）
- **核心**：诊断稀疏 attention 的**分布漂移**问题——稀疏 prefill 导致 query-key 对齐偏移，decode 召回率暴跌（如 MultiKey-3 上 0%）。提出 Δ 校正项，把稀疏输出重新对齐到二次 attention 分布。
- **关键数字**：滑动窗口 + Δ，恢复 **88% 二次 attention 精度**（131K RULER）；**98.5% 稀疏度**；vs FlashAttention2 **32× 加速**（1M prefill）。
- **洞察**：稀疏 attention 不只是"少算"，还要**校正分布偏移**——这是很多稀疏方法精度掉的根因。

### MUSTAFAR（NeurIPS 2025）
- **核心**：**非结构化稀疏** KV cache 剪枝（之前多是结构化）。发现 per-token 幅值剪枝对 K 和 V 都有效（V 虽分布均匀仍受益）。配 bitmap 稀疏格式 + 自定义 attention kernel。
- **关键数字**：**70% 稀疏近乎无损**；KV cache 压到 dense 的 **45%**；吞吐 **2.23×**。

### DMS / Inference-Time Hyper-Scaling（NeurIPS 2025）⭐⭐
- **全称**：Dynamic Memory Sparsification
- **核心**：**用 KV 压缩换推理时计算扩展**——压缩 KV 后，同等算力可生成更多 reasoning token，提升推理准确率。DMS 延迟 evict 决策（隐式合并表征），仅 1K 训练步达 8× 压缩。
- **关键数字**：Qwen-R1 32B 在 AIME 24 **+12.0 点**，GPQA +8.6，LiveCodeBench +9.7（同等内存读取下）。
- **洞察**：这是 **test-time compute scaling 的新范式**——不是生成更长，而是**压缩 KV 腾出预算生成更多**。对接 [`讲透RL`](讲透RL/)（RLVR/inference scaling）。

---

## 三、主题 ②：Prefill-Decode 分离式推理服务（系统侧最热）

> Prefill（compute-bound）和 Decode（memory-bound）特性截然不同——2025-2026 的共识是**物理分离**到不同 GPU 池，各自优化。

### NVIDIA: Pragmatic Disaggregation（MLSys 2026）⭐
- **核心**：**首个数据中心规模**的 PD 分离系统研究。模拟百万设计点，给出"何时分离值得"的判据。
- **关键结论**：
  - **Prefill-heavy（ISL≫OSL）**：分离完胜（如 o1 式长输入短输出）
  - **Decode-heavy**：co-located 也有竞争力
  - **动态 rate matching + 弹性扩缩**是关键
  - MoE 模型比 dense 更受益于分离（Expert Parallelism）
- **技术**：Chunked Pipeline Parallelism (CPP) 降 prefill 通信；NIXL 异步 KV 传输；Dynamo KV Router cache-aware 路由。
- **对接**：[`讲透GPU与系统级`](讲透GPU与系统级/)（推理服务架构）

### Meta: Optimizing Deployment Configurations（MLSys 2026）⭐
- **核心**：Meta 服务 **10 亿月活**的 Llama 推理生产经验。系统化分析部署配置空间（硬件/并行/运行时）。
- **关键洞察**：
  - 在线服务迁移到分离式 runtime，**省 30% 算力**
  - **异构硬件**（prefill 用高 FLOP/s GPU，decode 用高 HBM 带宽 GPU）提 15-25% 成本效率
  - MoE 的 Expert Parallelism 在 scale-out 上有独特优势
- **对接**：[`讲透分布式AI系统`](讲透分布式AI系统/)（生产部署）

### LAPS / PLA-Serve（MLSys 2026）
- **核心**：**长度感知 prefill 分离**——不仅分 prefill/decode，还把 prefill 按**长短**再分（短 prefill 是 memory-bound，长 prefill 是 compute-bound，瓶颈不同）。
- **关键数字**：vs SGLang（PD 分离），prefill 延迟 **-30%**；SLO 违规 **-28%**。

### TriInfer（MLSys 2026）
- **核心**：**多模态 LLM 的三阶段分离**（Encode-Prefill-Decode, EPD）——视觉编码和语言处理解耦，双流并行。
- **关键数字**：vs vLLM/SGLang，多模态吞吐 **2.4×**。

### KVServe（arXiv 2026）
- **核心**：**分离式服务的 KV 通信压缩**——PD 分离后 KV cache 要跨网络传输，成新瓶颈。KVServe 用 Bayesian Profiling + 在线 bandit 自适应选压缩策略。
- **关键数字**：PD 分离 JCT **9.13× 加速**；KV-disaggregated TTFT **32.8× 降低**。

---

## 四、主题 ③：推理加速（speculative decoding 新变体）

### SparseSpec（MLSys 2026）⭐
- **核心**：**Self-speculative decoding + 稀疏 attention**——用同一个模型当 draft（稀疏 PillarAttn）和 target（全 attention）。PillarAttn 巧妙复用 verification 阶段的 attention score 做 critical token 识别（零额外开销）。
- **关键数字**：vs vLLM，推理模型（Qwen3/DeepSeek-R1-distill）吞吐 **2.13×**。
- **对接**：[`讲透基础模型`](讲透基础模型/)（speculative decoding）

---

## 五、主题 ④：KV 量化（极端低比特）

### VecInfer（ACL 2026）
- **核心**：**2-bit KV cache 向量量化**——用 smooth + Hadamard 变换抑制 key cache outlier，让 codebook 覆盖原始分布。配融合 CUDA kernel。
- **关键数字**：**2-bit 性能近似全精度**；大 batch self-attention **2.7× 加速**；单 batch 端到端延迟 **8.3× 降低**（196K 序列）。

---

## 六、趋势洞察（2025-2026 高效 AI 在往哪走）

### 洞察 1 · 稀疏 attention 从"固定"走向"自适应"
2024 的 StreamingLLM/H2O 用固定 mask；2025 的 Twilight/Quest/DMS 用 query-aware 动态稀疏。**核心转变**：预算不再是超参，而是**按 attention 分布自适应**决定（top-p）。

### 洞察 2 · PD 分离成为生产共识
NVIDIA + Meta 两篇 MLSys 2026 都确认：**在线服务用分离式省 30% 算力**。这是从 vLLM 的 continuous batching 范式向 DistServe 的分离范式的产业级迁移。

### 洞察 3 · KV 压缩 = 新的 inference scaling
DMS 的 hyper-scaling 揭示：**压缩 KV 腾出的预算可以生成更多 reasoning token**。这不是省成本，是**用效率换准确率**——把 test-time scaling 推向新维度。

### 洞察 4 · 推理服务从"单 GPU 优化"到"数据中心协同"
Meta/NVIDIA 的论文都把推理当**数据中心级系统**问题（异构硬件/多并行策略/网络）。单卡 kernel 优化（FlashAttention）已成熟，战场转移到**跨节点协同**。

---

## 七、与 work4ai 的对接

| 本精选讲的 | work4ai 深度版 |
|---|---|
| 稀疏 attention / 长上下文 | [`讲透基础模型`](讲透基础模型/)（长上下文）+ [`讲透Transformer`](讲透Transformer/) |
| KV 压缩 / 量化 | [`讲透GPU与系统级`](讲透GPU与系统级/)（量化/KV cache）|
| PD 分离 / 推理服务 | [`讲透GPU与系统级`](讲透GPU与系统级/)（vLLM/服务架构）+ [`讲透分布式AI系统`](讲透分布式AI系统/) |
| Inference-time scaling | [`讲透RL`](讲透RL/)（RLVR/test-time compute）|
| Speculative decoding | [`讲透基础模型`](讲透基础模型/) |

**阅读路径**：读 [高效AI研究参考-MITHANLab](高效AI研究参考-MITHANLab.md)（单实验室纵深）→ 本篇（顶会横切面）→ 对应 work4ai 讲透系列钻原理。

---

## 八、论文清单速查

| 论文 | 会议 | 主题 | 关键数字 |
|---|---|---|---|
| **LServe** | MLSys 2025 | 混合稀疏 serving | prefill 2.9× / decode 2.1× |
| **Twilight** | NeurIPS 2025 | 自适应稀疏 (top-p) | 98% 剪枝 / 15.8× attention |
| **Δ Attention** | NeurIPS 2025 | 稀疏分布校正 | 恢复 88% 精度 / 32× prefill |
| **MUSTAFAR** | NeurIPS 2025 | 非结构化 KV 稀疏 | 70% 稀疏无损 / 2.23× 吞吐 |
| **DMS** | NeurIPS 2025 | KV 压缩换推理扩展 | Qwen-R1 +12 点 (AIME 24) |
| **SparseSpec** | MLSys 2026 | self-speculation + 稀疏 | 推理模型 2.13× 吞吐 |
| **NVIDIA Disagg** | MLSys 2026 | PD 分离系统研究 | 数据中心规模判据 |
| **Meta Deployment** | MLSys 2026 | 生产 LLM 推理配置 | 分离式省 30% 算力 |
| **LAPS** | MLSys 2026 | 长度感知 prefill 分离 | prefill -30% 延迟 |
| **TriInfer** | MLSys 2026 | 多模态 EPD 分离 | 2.4× 多模态吞吐 |
| **KVServe** | arXiv 2026 | PD 分离 KV 通信压缩 | JCT 9.13× / TTFT 32.8× |
| **VecInfer** | ACL 2026 | 2-bit KV 向量量化 | 2-bit 近似全精度 / 8.3× 延迟 |

---

> **数据源**：NeurIPS 2025 / MLSys 2025-2026 / ACL 2026 官方 proceedings + arXiv 一手 abstract（2026-08-10 核实）｜ **维护**：顶会每年更新，可关注 AK(@_akhaliq) + HAN Lab + vLLM/SGLang 团队动态。

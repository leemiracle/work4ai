# 讲透分析·八支子系列生产 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.
> **本计划特殊：Task 1-8 相互独立，必须并行派发（用户明确要求最大化并行度）；每任务一个 fork agent。**

**Goal:** 按 spec v2，用 8 个并行 fork agent 为 讲透分析/ 的八支占位子系列各生产五章（00-04）+ 1 个可跑实验，翻转 README 状态列。

**Architecture:** 结构宪法已在（另一会话的家族层五章+八支占位 README 章目表）；本计划只做内容填充。体例锚=家族层五章实际体例（见 Global Constraints 照录模板）。git 写操作全部集中在主会话 Task 9，agents 只写文件+跑实验。

**Tech Stack:** Markdown 内容 + numpy/scipy 单文件实验（`python xx.py` 出数+断言）。

**Spec:** `docs/superpowers/specs/2026-09-06-讲透分析图鉴-design.md`（v2 合流版，commit 9ef7afbe）

## Global Constraints

1. **agents 禁止一切 git 写操作**（add/commit/push/stage）——8 agent 共享 git index，并行 commit 必打架；全部 commit 由主会话 Task 9 统一执行，每次 commit 前必跑 `git diff --cached --name-only` 确认为空或仅含本批文件
2. **禁区（只读/不碰）**：
   - 另一会话未提交修改（M 状态）：根 README.md、宇宙 `讲透数学/README.md`、`top-math-courses/*`、`讲透数学/讲透Lean4数学/*`、`讲透数学/讲透代数拓扑/*`、`故事化学习法/*`、`知识故事集/*` 等——开工前以 `git status --short` 实时清单为准
   - `讲透分析/讲透实分析/`、`讲透分析/讲透分析进阶/`（已有系列，不动）
   - `讲透分析/` 根的家族层文件（README.md、00~04 章、experiments/）
   - 其他七支的目录（每 agent 只写自己那支）
3. **落点约束**：本任务全部新文件只落在 `讲透数学/讲透分析/讲透X/`（X=本支）内
4. **章体例模板**（照家族层实际体例，每章必须）：
   ```markdown
   # NN · <标题>：<副标题>

   > **本章回答五问之X（本支：讲透X）。** <一两句总纲或戏剧点>
   > （仅 01 章加一行：`检索校准 2026-09-06` 或 `时效声明：模型知识，未逐条检索`）

   ---

   ## 一、<节名>（<括号副题>）
   ## 二、…
   ## Python 层
   ## 不足层
   ## ✍️ 练习
   ## 📌 收束
   ```
   - 每章 150-400 行；≥1 个"反直觉发现"贯穿（宇宙铁律）
   - 📌 收束含宇宙承接（`../<系列>/` 相对链接，自支 README 的关系表可参考）
   - "Python 层"节引用本章实验脚本；"不足层"=讲透范式第四环（本支手艺的边界）
5. **实验约束**：单文件 `experiments/xx_demo.py`；numpy/scipy 优先（无 matplotlib 依赖，或仅可选）；文件头 docstring 注明对应章；跑通出数 + ≥1 个 `assert` 自验证；Agent 交付前必须实际运行一次确认通过
6. **章文件名以本支占位 README 章目表为准**（各支 04 章名可能微差，如 `04-转代码.md`）
7. **01 章检索**：六核心支（Task 1-6）用 Semantic Scholar MCP 各 1-2 查询（英文关键词，`year_range=2021-`，`fields=title,year,venue,abstract` 精简字段）；动力系统/PDE（Task 7-8）标时效声明即可

---

### Task 0: 开工验证（主会话，串行）

**Files:** 无产物（验证性）

- [ ] `git status --short` 快照：记录另一会话 M/?? 清单，作为本批禁区实时依据
- [ ] 确认八支占位 README 在位（`ls 讲透数学/讲透分析/讲透{数学分析,复分析,泛函分析,调和分析,数值分析,变分分析,动力系统,偏微分方程}/`，各含 README.md）
- [ ] 逐支读占位 README 的章目宪法表+教材锚，纳入该支 agent 的 prompt

### Task 1: 讲透数学分析（fork agent，检索✅）

**Files:**
- Create: `讲透数学/讲透分析/讲透数学分析/{00-体系结构,01-近五年创新,02-语言特征,03-可构造与结构,04-转代码}.md`（名从占位表）
- Create: `讲透数学/讲透分析/讲透数学分析/experiments/series_convergence_decision.py`
- Modify: `讲透数学/讲透分析/讲透数学分析/README.md`（章目表 📝→✅ + 实验表补行）

**章骨架（必含要点）：**
- 00：实数三构造（Dedekind 切/柯西列/无穷小数）→ ε-δ 量词机器 → 五大件（极限/连续/微分/Riemann 积分/级数）→ 一致收敛统治权（逐点 vs 一致，交换极限的许可证）→ 多元（偏导/隐函数）。反直觉：Weierstrass 处处连续不可微；中值定理在向量值失效
- 01（检索校准）：Lean mathlib 微积分覆盖度 / AI 助教与自动评分 / 本科教学形式化革新。诚实判断：本体稳定，五年创新在教学与形式化侧
- 02：ε-δ=∀∃交替；不等式放缩文化起点；规范化=完备有序域 R 的唯一性；表达力边界（能谈收敛、不能谈测度——留给实分析）
- 03：实数构造的算法内容（区间套二分可执行）；Heine-Borel 紧性初现；一致收敛=交换极限许可证；三结构（序×代数×拓扑）
- 04：三走廊（符号微分/数值差分/自动微分 AD）——AD=链式法则工程化；工程师落点：AD 是深度学习引擎
- 实验 `series_convergence_decision.py`：对 6+ 个级数（几何/p-级数 α∈{0.5,2}/交错/调和）逐个跑比式+根式判别并输出决策树结论；断言：∑1/n^2 收敛判定正确且部分和与 π²/6 差 < 1e-2（n=10⁵）

### Task 2: 讲透复分析（fork agent，检索✅）

**Files:**
- Create: `讲透分析/讲透复分析/{00..04}.md` + `experiments/residue_contour.py`
- Modify: 本支 README 状态列

**章骨架：**
- 00：复数域三种入场 → 全纯三等价（C-R 方程/幂级数/围道积分零）→ 柯西定理链（矩形→圆盘→同调形式）→ 留数 → 共形映射（Riemann 映照）→ 解析延拓与自然边界 → **多复变一句**（Hartogs：高维全纯奇性可去，一维奇迹断裂）。反直觉：可导⟹解析；Liouville⟹代数基本定理
- 01（检索校准）：复动力系统（Mandelbrot 集局部连通性进展）/ Teichmüller 理论 / 值分布
- 02：刚性语言（恒等定理：一点定全局）；最大模原理=不等式文化复版；单复变=最规范化的分析分支
- 03：Taylor 系数=围道积分（可算）；Schwarz-Christoffel 显式共形构造；解析延拓沿路径可执行
- 04：数值围道积分 / FFT 与 ζ 函数 / 共形网格生成（机翼工程史）/ Mandelbrot 迭代；工程师落点：留数=逆变换的算法
- 实验 `residue_contour.py`：用留数定理数值围道积分算 ∫_{-∞}^{∞} dx/(1+x²)=π，与 scipy.integrate.quad 和解析值三方对照；断言围道法误差 < 1e-6

### Task 3: 讲透泛函分析（fork agent，检索✅）

**Files:** Create `{00..04}.md` + `experiments/fixed_point_power.py`; Modify README

**章骨架：**
- 00：赋范→Banach→Hilbert 三级火箭 → 三大定理（Hahn-Banach/开映射/闭图）→ 对偶与弱收敛 → 谱论 → **算子代数一句定位**（C*/von Neumann=谱论非交换升华，长尾归属）→ 分布归此。反直觉：Hahn-Banach 依赖选择公理（不可构造）；无限维单位球不紧
- 01（检索校准）：Ribe 程序与度量空间几何 / 算子范数估计×量子信息 / Liquid Tensor 支层深读（Clausen-Scholze 形式化了什么：凝聚态数学的泛函分析替换品）
- 02：母语=对偶；非构造性重镇（Zorn 常客）；规范化=拓扑向量空间公理化
- 03：可分情形 Hahn-Banach 可算 vs 一般不可构造；Banach 迭代=构造引擎；无界算子谱的可计算性边界
- 04：Galerkin 离散化 / 幂法与 Arnoldi / RKHS→ML 核方法；工程师落点：大规模特征值计算支柱
- 实验 `fixed_point_power.py`：(a) 压缩映射 x=cos x 不动点迭代，断言误差按压缩率 ~0.67 几何衰减；(b) 随机矩阵幂法最大特征值 vs numpy.linalg.eigvals，断言相对误差 < 1e-8

### Task 4: 讲透调和分析（fork agent，检索✅）

**Files:** Create `{00..04}.md` + `experiments/gibbs_fft.py`; Modify README

**章骨架：**
- 00：Fourier 级数→变换→反演 → L² 与 Plancherel → 奇异积分（Calderón-Zygmund 分解）→ 插值定理（Riesz-Thorin/Marcinkiewicz）→ 时频与小波 → 群上调和一句（Pontryagin）。反直觉：连续函数级数可处处发散（Kolmogorov）；L¹ 反演失败
- 01（检索校准）：**restriction 猜想 2025 三维突破（Wang-Zahl）** / decoupling 后续 / Kakeya 集
- 02：语言=分解（时频/原子/Calderón-Zygmund）；插值链=不等式文化极致；从具体展开到算子理论
- 03：系数可算但 L¹ 反演不Condition；Haar 小波=显式可构造典范；不确定性原理=时频结构刚性
- 04：FFT / 谱方法 / 小波压缩（JPEG2000）/ STFT；工程师落点：FFT 是性能工程祖师爷级 workload
- 实验 `gibbs_fft.py`：(a) 方波 N=11,101,1001 部分和过冲 ≈8.95% (Wilbraham-Gibbs 常数) 不随 N 消失，断言三者过冲 ∈ [8.5%,9.5%]；(b) FFT 卷积 vs 直接卷积 n=2¹² 计时，断言 FFT 路径加速 > 20×

### Task 5: 讲透数值分析（fork agent，检索✅，含逼近论内节）

**Files:** Create `{00..04}.md` + `experiments/runge_chebyshev.py`; Modify README

**章骨架：**
- 00：计算面总图（插值/求积/方程求根/ODE 格式）→ 收敛阶×稳定性双支柱 → **逼近论内节**（Chebyshev/最小最大/Weierstrass 定理）→ 分工声明：线代篇在 `../讲透数值线代/`（线代计算面它管，本支管方程计算面）。反直觉：Runge 现象
- 01（检索校准）：SciML/PINN / 并行-in-time（Parareal）/ 混精度 / randomized 数值方法
- 02：语言=误差（前向/后向/条件/稳定四概念）；IEEE754 浮点=分析的新公理系统（规范化典范）；向后误差=数值解是扰动问题的真解
- 03：本支=分析的构造臂（几乎全可构造）；但舍入不可消除：Wilkinson 病态多项式；条件数是问题的属性≠稳定性是算法的属性
- 04：走廊清单（scipy.integrate.quad/solve_ivp/brentq 全家）；工程师落点：BLAS/LAPACK 栈
- 实验 `runge_chebyshev.py`：f=1/(1+25x²) 在 [-1,1]，n=20 等距 vs Chebyshev 节插值最大误差对照；断言：等距误差 > 1（发散）而 Chebyshev 误差 < 1e-6

### Task 6: 讲透变分分析（fork agent，检索✅）

**Files:** Create `{00..04}.md` + `experiments/direct_method_fem.py`; Modify README

**章骨架：**
- 00：古典线（Euler-Lagrange/Hamilton-Jacobi）→ 直接法（下有界+弱收敛+紧性⟹极小元存在，Tonelli）→ 非光滑分析（次微分）→ 凸分析桥→`../讲透优化/` → Γ-收敛。反直觉：E-L 必要非充分；缺紧性时极小化序列逃逸
- 01（检索校准）：非光滑×ML（ReLU 次微分进训练理论）/ ADMM 复兴 / 平均场博弈 / 最优输运
- 02：语言=极值与下半连续；弱拓扑；表达力：把 PDE 归约为极小化
- 03：直接法离散化可执行（有限元存在性的理论根据）；凸情形次微分可算
- 04：有限元=变分代码化身 / 梯度下降=变分流离散 / ADMM；工程师落点：DL 优化器都是变分后代
- 实验 `direct_method_fem.py`：一维泊松 -u''=f（取 f 使真解 u=x(1-x) 已知），线性有限元直接法离散化，网格 h 从 1/8 加密到 1/256；断言 H¹ 误差随 h 收敛且最细网格误差 < 最粗的 1/10（验证收敛阶）

### Task 7: 讲透动力系统（fork agent，时效声明，含遍历内节+随机跨向一句）

**Files:** Create `{00..04}.md` + `experiments/lorenz_lyapunov.py`; Modify README

**章骨架：**
- 00：ODE 定性理论（不动点/稳定性/分岔）→ 混沌（Lyapunov 指数/奇怪吸引子）→ **遍历理论内节**（Birkhoff/混合率）→ KAM 一句 → **随机分析跨向一句**（Itô 演化归概率书架，链接 `../讲透高维概率/` 方向）。反直觉：Poincaré 回归；三体
- 01（时效声明）：KAM 验证式数值（computer-assisted）/ mixing rates 定量 / SINDy 数据驱动
- 02：相空间流=几何语言；遍历定理=时间平均=空间平均的严格化
- 03：轨道可数值积分但混沌=误差指数放大（长期预测原则上不可能，Lyapunov 时限）；影子引理；统计量可预测
- 04：积分器族（RK45/刚性/辛）/ 分岔图扫描 / Benettin 算法；工程师落点：一切物理仿真内核
- 实验 `lorenz_lyapunov.py`：Lorenz（σ=10,ρ=28,β=8/3）Benettin 重正交化估计最大 Lyapunov 指数；断言 λ₁ ∈ [0.85, 0.95]（文献值 ≈0.906）；附带双初值差 1e-8 的轨道分离演化演示

### Task 8: 讲透偏微分方程（fork agent，时效声明，含 SPDE/正则性结构内节）

**Files:** Create `{00..04}.md` + `experiments/heat_cfl.py`; Modify README

**章骨架：**
- 00：三大类（椭圆/抛物/双曲）→ 适定性（Hadamard 良置三条件）→ 弱解与 Sobolev → 正则性（De Giorgi-Nash-Moser/Schauder 阶梯）→ 守恒律（激波/熵条件）→ 与变分章互链（弱解的变分出身）→ **正则性结构/SPDE 内节**（Hairer：KPZ 普适性，长尾归属）→ 几何分析入口一句（Ricci 流）→ 位势论（与复分析双归属）。反直觉：弱解比经典解更自然；先验估计=存在性引擎（连续性方法）
- 01（时效声明）：Euler 有限时间爆破解（Chen-Hou 计算机构造→严格化）/ 2025 微观动力学→流体严格推导（希尔伯特第6问部分落定，链著名问题全景）/ 最优输运正则性 / PINN
- 02：语言=估计（先验估计文化）；能量估计与 Sobolev 嵌入=不等式极致；Sobolev 空间=PDE 母语
- 03：FDM/FEM=可构造化身；能量守恒格式；但 NS 一般存在性（千禧）无封闭构造
- 04：四代走廊（FDM/FEM/谱/PINN）；CFL=稳定性的代码形态；工程师落点：CFD/气象
- 实验 `heat_cfl.py`：热方程显式格式，N=41 网格：(a) Δt=0.45Δx²（超 CFL 阈值 0.5）跑 200 步，断言解的 ∞-范数发散（>10×初值最大值）；(b) Δt=0.4Δx²，断言稳定且与解析解（傅里叶衰减）误差 < 1e-3

### Task 9: 收口（主会话，串行）

- [ ] 核对八支 README 章目表全 ✅、实验表补行
- [ ] 全部 8 个实验逐一重跑（`python <file>`），确认断言全过——证据先于断言
- [ ] 链检：`讲透数学/讲透分析/` 内新增 md 的相对链接目标存在性检查：
      ```bash
      cd 讲透数学/讲透分析 && grep -rhoE '\]\((\.\./)+[^)#]+' --include='*.md' 讲透数学分析 讲透复分析 讲透泛函分析 讲透调和分析 讲透数值分析 讲透变分分析 讲透动力系统 讲透偏微分方程 | sed 's/^\](//' | sort -u | while read p; do [ -e "$p" ] || echo "断链: $p"; done
      ```
      输出为空=ALL OK
- [ ] 家族 README `讲透分析/README.md`：若已入库（非另一会话未提交状态），更新 📌下一步段+子系列表状态列；否则记入 spec 待办，不写盘
- [ ] 分支 scoped commits（每支一个，共 8 个 + 收口 1 个），每次 commit 前 `git diff --cached --name-only` 必查为空
- [ ] TaskList 翻转，向用户报告交付清单

---

## Self-Review

1. **Spec 覆盖**：D2' 八支=Task 1-8 ✅；D3' 五章+体例=Global 4+各骨架 ✅；D4' 实验每支1个=各任务 Files ✅；长尾消化（逼近论→T5、算子代数→T3、多复变→T2、随机→T7、正则性→T8、可计算→呼应家族04）✅；D5' 并行=Task 1-8 并行派发 ✅；§5 收口边界=Task 9 ✅；§6 教训=Global 1 ✅
2. **Placeholder 扫描**：骨架到小节级、实验给到断言级（含具体数值），无 TBD ✅
3. **一致性**：章文件名声明"以占位表为准"处理微差；实验文件名全文唯一 ✅

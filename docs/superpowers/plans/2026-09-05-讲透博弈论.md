# 讲透博弈论系列 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建成「讲透博弈论」中型系列：20 篇章节（00+18 章+50，五卷）+ 12 个教学程序（7 Windows 实验 + 2 WSL 进阶 + 3 可玩 CLI），全部挂网进 README。

**Architecture:** 主题线性制（方案 A）：卷一静态均衡→卷二动态信息→卷三重复演化合作→卷四机制设计→卷五学习与 AI。库作仪器：nashpy=卷一、pygambit=卷二/四、axelrod/numpy=卷三、手写 CFR+OpenSpiel(WSL)=卷五。每章内嵌五幕（直觉→数学→代码→不足→应用），每章"代码"节互引对应 lab 的 E 编号。

**Tech Stack:** Python 3.14（Windows 本机）+ nashpy + pygambit + axelrod + numpy/scipy/matplotlib；WSL Ubuntu-22.04 + open_spiel。文风对齐 `讲透优化/`（见 `讲透优化/01-统一框架.md` 样本：`>` 引言块、`---` 分隔、`## 一、二、三` 编号节、锚点加粗、表格密集、章尾互链）。

**Spec:** `docs/superpowers/specs/2026-09-05-讲透博弈论-design.md`

## Global Constraints

- 中文为主；代码/库名/产品名保留英文（AGENTS.md 写入规范）
- README 篇目表 = 目录宪法；每个新文件必须进 README（孤儿文件 = 死亡内容）
- lab 脚本头部规范（照抄 `讲透优化/experiments/sgd_lab.py` 模式）：`#!/usr/bin/env python3` + 中文 docstring（讲透博弈论实验 N + E1/E2/E3 一行摘要 + 产出 png）+ `matplotlib.use("Agg")` + `plt.rcParams["font.sans-serif"] = ["Noto Sans CJK SC", "WenQuanYi Zen Hei", "sans-serif"]` + `rng = np.random.default_rng(42)`
- 诚实校准：控制台并列打印**理论值 vs 实验值**（延续讲透优化传统）；理论锚点数值已给在本计划各任务中，不得虚构
- 网络一律先镜像（`pip install -i https://mirrors.aliyun.com/pypi/simple/ ...`），失败再探测本地代理（7890/10809）
- png 产出物随代码一起 commit（讲透优化惯例）
- git commit 消息中文 feat/chore 风格；不 push（用户未要求）
- 若 pygambit 在 Python 3.14 无 wheel 且源码编译失败 → 后备：`py -3.12 -m venv .venv-gt` 用 Astral 3.12.13 建 venv 装全部依赖，README 注明

---

### Task 1: 骨架 + README 目录宪法 + 00 开场 + 50 收尾

**Files:**
- Create: `讲透博弈论/README.md`
- Create: `讲透博弈论/00-开场：策略的宇宙.md`（文件名不含冒号！Windows 非法字符——用全角冒号或破折号。**统一规则：本系列文件名一律 `NN-短名.md` 不含任何标点**）
- Create: `讲透博弈论/50-批判收尾.md`

**Interfaces:**
- Produces: README 篇目表（其他任务据此挂网）；系列宪法块（每章开头引用）；00 章定义三原型矩阵（后续章节沿用同一套记号）

- [ ] **Step 1: 写 README.md**。结构照 `讲透优化/README.md`：标题行「讲透博弈论：策略的科学与工程（五卷 · 20 篇）」；宪法引言块（系列一句话定义：策略互动 Strategic Interaction——纳什均衡的相互最优、机制设计的激励相容、演化的稳定策略、AI 自博弈的收敛全是它的方言）；五卷篇目表（# / 标题 / 状态 / 核心锚点 / 仪器 五列，初始状态全部 ◆，Task 1 完成后 00/50 翻 ✅）；教学程序表（experiments 7 + wsl_labs 2 + cli 3，列：脚本/服务章节/内容）；「运行环境」节（Windows：`pip install -i https://mirrors.aliyun.com/pypi/simple/ nashpy pygambit axelrod`；WSL：`wsl -d Ubuntu-22.04` + `pip install open_spiel`；每个 lab `python experiments/labXX.py`）；「与姊妹系列互链」节（讲透优化 25 在线优化章=no-regret 桥、讲透Agent=multi-agent 桥、讲透信息论=信息设计桥）
- [ ] **Step 2: 写 00-开场.md**。锚点：① 三原型矩阵（用统一记号：囚徒困境 T=5,R=3,P=1,S=0；性别战争 (2,1)/(0,0)/(0,0)/(1,2)；鹰鸽 v=2,c=4）；② 博弈论五要素（玩家/策略/收益/信息/时序）；③ 理性层级与 k-level 思维（0-100 猜数游戏预告 01 章）；④ 全系列地图（五卷导览图，每卷一段）；⑤ 工具地图（nashpy/pygambit/OpenSpiel/axelrod 分工表）；⑥ 五幕阅读法说明
- [ ] **Step 3: 写 50-批判收尾.md**。锚点：① 理性批判（行为经济学：k-level 实测 1~2 级、quantal response、最后通牒实验）；② 计算复杂性（找均衡 PPAD-完全——Daskalakis 线；均衡存在≠多项式可找）；③ 均衡存在 ≠ 可达/可学（学习动力学可能循环不收敛）；④ 均衡不唯一 → 预测力短板；⑤ 博弈论的认识论地位：理解策略互动的语法，不是预测水晶球；⑥ 与讲透优化 NFL 定律的"谦逊收尾"互链
- [ ] **Step 4: README 中 00/50 状态翻 ✅**
- [ ] **Step 5: Commit** `feat: 讲透博弈论骨架——README目录宪法+00开场(三原型/五要素/工具地图)+50批判收尾(PPAD/理性批判/均衡可达性)`

### Task 2: 卷一上——01 占优 + 02 纯策略均衡 + 03 混合策略 + lab01

**Files:**
- Create: `讲透博弈论/01-理性基石与占优.md`, `讲透博弈论/02-纯策略纳什均衡.md`, `讲透博弈论/03-混合策略.md`
- Create: `讲透博弈论/experiments/lab01_dominance.py`（产出 `讲透博弈论/experiments/ne_basic.png`）

**Interfaces:**
- Consumes: 00 章三原型记号（T5 R3 P1 S0 等）
- Produces: 三原型 NE 手算表（lab01 打印对照）；`lab01_dominance.py` 可独立运行

- [ ] **Step 1: 写 01 章**。锚点：偏好公理（完备/传递/连续）→ 效用存在（vNM 期望效用定理陈述）→ 占优（严格/弱，弱占优剔除陷阱：可剔除后消失的均衡）→ 重复剔除 IESDS（0-100 猜数 ×0.7：唯一存活 0，k-level 与公共知识）→ 蓝眼睛岛谜题（公共知识 ≠ 共同知识）→ "占优可解的博弈不需要均衡概念；猜拳无占优——下一章"。nashpy 工具箱小节（`nash.Game(A, B)`, `support_enumeration()`）
- [ ] **Step 2: 写 02 章**。最优反应对应 → NE 定义（相互最优反应的不动点视角）→ 三原型 NE 手算表：囚徒困境唯一 (D,D)（占优均衡）；性别战争两个纯 NE（+对称混合预告 03）；鹰鸽为**反协调博弈——两个非对称纯 NE (H,D)/(D,H) + 唯一对称混合 x*=v/c**（ESS 主场，预告 11 章）；真正无纯 NE 的例子用猜拳/匹配硬币承担 → 纯 NE 存在性（不动点需混合，引 Nash 定理陈述：有限博弈必有混合 NE，证明主脉在 03）→ NE 的三层含义（规范/实证/进化解释，进化解释引 11 章）→ 协调失败与聚点预告 05
- [ ] **Step 3: 写 03 章**。混合 = 对手无差异原理（你混合是为让对方猜不透，均衡里双方互为无差异）→ 2×2 混合公式手推：若 A=[[a,b],[c,d]]，行玩家均衡概率 x*=(d−c)/(a−b−c+d) → 罚点球 2×2（Kicker×Keeper，得分概率矩阵 [[0.65,0.95],[0.95,0.55]]，行=射门方向，列=扑救方向同侧低）手算混合均衡 → 支撑集法一般流程（猜支撑集→解无差异方程组→验证非负）→ Nash 定理证明主脉链：Sperner 引理 → Brouwer 不动点 →（Kakutani 陈述）→ Nash 1950；附录式给 Sperner→Brouwer 两步证明梗概 → 混合策略的四种解释（故意随机/种群比例/信念/经验频率）
- [ ] **Step 4: 写并跑 lab01**。E1 猜数博弈模拟：100 个 k-level 代理（level-0 均匀随机，level-k = 0.7×level-(k−1) 均值，k 截断分布），打印胜者 level 与均衡 0 对照；E2 三原型 + 附加 4 个 2×2 用 nashpy `support_enumeration()` 打印全部 NE，与三原型手算表逐项对拍（打印「手算 vs nashpy：一致 ✓」）；E3 IESDS 演示矩阵 + 弱占优陷阱一例（打印剔除序列）。产出 ne_basic.png（1×3 子图：三原型收益网格热图 + NE 红点标注）。运行 `python 讲透博弈论/experiments/lab01_dominance.py`（Windows 控制台路径可先 `cd 讲透博弈论`；以下同）。**前置：nashpy 已装**（若后台安装未完成则等待，见全局约束）
- [ ] **Step 5: README 状态翻 ✅（01/02/03 + lab01 行）**
- [ ] **Step 6: Commit** `feat: 讲透博弈论卷一上——01占优(IESDS/猜数/公共知识)+02纯均衡(三原型手算表)+03混合(无差异原理/Sperner→Brouwer主脉)+lab01(nashpy对拍+猜数模拟)`

### Task 3: 卷一下——04 零和 minimax + 05 均衡选择 + lab02

**Files:**
- Create: `讲透博弈论/04-零和与minimax.md`, `讲透博弈论/05-均衡选择与相关均衡.md`
- Create: `讲透博弈论/experiments/lab02_mixed_penalty.py`（产出 `mixed.png`）

- [ ] **Step 1: 写 04 章**。零和 = 严格竞争 → 安全策略（maximin）→ von Neumann 1928 定理：minmax = maxmin = 博弈值 → **LP 对偶视角**（行玩家问题与其对偶恰好是列玩家问题；给 LP 标准形与对偶表）→ 用 scipy.linprog 手解罚点球博弈 → 诈唬的数学（简化诈唬矩阵示例：strong/weak hand × call/fold）→ 与讲透优化 09 线性规划章互链（LP 对偶=影子价格=minimax 定理同一件事）
- [ ] **Step 2: 写 05 章**。协调博弈多均衡 → 收益占优 vs 风险占优（猎鹿 2×2：[[4,1],[3,2]] 的风险占优方=野兔侧，给风险占优判定式 (u11−u21)(u22−u12)>0）→ Schelling 聚点（文化/标签选择）→ 相关均衡（红绿灯=交通博弈的相关装置；CE 定义=无人想单方偏离信号建议；LP 刻画）→ CE vs NE 关系（NE ⊂ CE；CE 可达更高和）→ 防串谋一句话展望
- [ ] **Step 3: 写并跑 lab02**。E1 罚点球：手算混合均衡（03 章公式）vs nashpy `vertex_enumeration`/`support_enumeration` 对拍打印（差异 < 1e−8）；E2 scipy.linprog 解行玩家 minimax LP，值 vs nashpy 均衡值对拍；E3 真实数据对比：Chiappori-Grosclausde-Levitt (2002) 罚点球汇总统计（近似：射 L≈0.45 / C≈0.15 / R≈0.40；扑同侧率等）与 2×2 简化理论预测并排条形图，诚实标注"模型简化，方向性一致"。产出 mixed.png（2×2 子图：单纯形混合轨迹示意 + 真实 vs 理论条形图）。运行并核对理论值打印
- [ ] **Step 4: README 翻 ✅（04/05 + lab02）**
- [ ] **Step 5: Commit** `feat: 讲透博弈论卷一下——04零和(vN定理/LP对偶/scipy手解)+05均衡选择(风险占优/聚点/相关均衡LP)+lab02(三路对拍:手算/nashpy/linprog)`

### Task 4: 卷二上——06 扩展式 + 07 子博弈完美 + lab03

**Files:**
- Create: `讲透博弈论/06-扩展式与逆向归纳.md`, `讲透博弈论/07-子博弈完美与承诺.md`
- Create: `讲透博弈论/experiments/lab03_centipede_qre.py`（产出 `extensive.png`）

**Interfaces:**
- Produces: pygambit 建模范式（`gambit.Game.new_tree` / Players / GameTree nodes），lab04 沿用

- [ ] **Step 1: 写 06 章**。博弈树（节点/枝/信息集——信息集引出 08 章不完全信息）→ 行为策略 vs 策略式（Kuhn 纯化定理一句话）→ 逆向归纳法（Zermelo 1913：国际象棋要么先手胜要么和——有限完美信息博弈必可解）→ **蜈蚣博弈悖论**（n=6 版：每节点 take/pass 收益 (i, i−1) vs 传下去翻倍；SPE=节点 1 立即 take；真实人类 pass 3~5 轮 → level-k 与社会偏好解释）→ 最后通牒（SPE 出价→0；实验：40~50% 极低出价被拒；公平效用函数 Fehr-Schmidt 一段）→ pygambit 工具箱（Game.new_tree 建树 + `.solve()`）
- [ ] **Step 2: 写 07 章**。子博弈完美 SPE 定义（每个子博弈上都构成 NE）→ 不可信威胁（进入威慑：在位者"打价格战"威胁 → 子博弈中不敢执行 → 容纳）→ **Stackelberg 先动优势**（双寡头：先手 c₁ 与后手 c₂ 反应函数对比；先手产量 > Cournot，利润更高——给两代数解）→ 承诺装置（烧钱信号/公开合同/桥烧掉=破釜沉舟）→ 承诺的机制设计回响（引 13/14 章 Stackelberg 均衡 = 机制设计原始形态；引 18 章 Stackelberg alignment）
- [ ] **Step 3: 写并跑 lab03**。E1 pygambit 建蜈蚣博弈 n=6，`gambit-enumpure`/solve 逆向归纳 → 打印 SPE（预期：节点 1 即 take，收益 (1,0) 按构造）；E2 QRE：用 pygambit logit 求解器（`gambit-logit` / python API `gambit.nash.logit_solve` 或等价）扫 λ∈[0.1, 100]，记录第一个 take 节点随 λ 前移曲线（预期：λ→∞ 收敛到 SPE 节点 1；λ 小时行为贴近"合作/pass"——连接 06 章行为解释）；E3 最后通牒模拟：responder 带不公平厌恶参数（拒绝 iff offer < θ，θ~U 截断分布），proposer 最优出价 vs SPE 0 对照打印。产出 extensive.png（2×2：QRE take 节点 vs λ 曲线 + ultimatum 出价分布直方图）。**前置：pygambit 已装**
- [ ] **Step 4: README 翻 ✅（06/07 + lab03）**
- [ ] **Step 5: Commit** `feat: 讲透博弈论卷二上——06扩展式(逆向归纳/蜈蚣悖论/最后通牒)+07子博弈完美(不可信威胁/Stackelberg/承诺)+lab03(pygambit建树+QREλ扫描)`

### Task 5: 卷二下——08 贝叶斯 + 09 信号 + lab04

**Files:**
- Create: `讲透博弈论/08-不完全信息与贝叶斯均衡.md`, `讲透博弈论/09-信号传递与精炼.md`
- Create: `讲透博弈论/experiments/lab04_bayes_auction.py`（产出 `auction.png`）

- [ ] **Step 1: 写 08 章**。不完全信息（不知道对方类型）→ **Harsanyi 转换**（引入"自然"玩家先抽类型 → 不完全信息 → 完美但不对称信息）→ 类型/信念/策略 → BNE 定义（每类型对信念最优）→ 例：一级价格密封拍卖 n 人估值 U[0,1]，对称均衡 b(v)=(n−1)/n·v 推导（给 n=2 的 b(v)=v/2 完整推导；一般 n 陈述）→ 二价拍卖占优策略 b=v → 两条路通向 13 章拍卖理论
- [ ] **Step 2: 写 09 章**。Spence 教育信号模型完整版：能力 H/L、教育成本 c_H < c_L（单交叉）、工资 = 期望能力 → 分离均衡（教育门槛 e* 使 L 自暴）与混同均衡并存 → 精炼标准一览（直觉标准 Cho-Kreps 一段：烧钱信号=07 章承诺连接）→ 廉价磋商 Crawford-Sobel（无成本话能传递信息当且仅当利益够一致）→ **AI 连接**：LLM 通信=廉价磋商（对齐=让"话"可信：诚实训练/验证器=信号成本）；model evaluation 中的信号（benchmark 防污染=昂贵信号）
- [ ] **Step 3: 写并跑 lab04**。E1 FPSB n=2 蒙特卡洛 10⁵ 场：bidder 按 b=v/2 出价，打印平均赢者支付（理论 E=1/3）与平均利润（理论 (1/2)²·? 由执行者现场推导验证：E[利润]=E[v_win]−E[支付]=2/3−1/3=1/3）；E2 四拍卖同分布蒙特卡洛：英式≈二价（实现=二价）、荷兰≈一价（实现=一价）、一价、二价，打印四路平均收入并排——收益等价验证（全部 ≈ (n−1)/(n+1)=1/3，容差 0.01）；E3 赢家诅咒：共同价值"钱包拍卖"（钱包值=全场信号均值，每人信号=值+噪声），naive 出价=自己信号 → 打印赢者平均利润（显著为负）vs 理性调整后出价（signal 减去 order-statistics 修正）利润（≈0）。产出 auction.png（1×3：收入对比条形/赢者支付分布/诅咒利润曲线）。**前置：pygambit（08 章离散例子可选不用库，核心蒙特卡洛纯 numpy**——pygambit 缺席时 lab04 仍可跑，只跳过可选小节）
- [ ] **Step 4: README 翻 ✅（08/09 + lab04）**
- [ ] **Step 5: Commit** `feat: 讲透博弈论卷二下——08贝叶斯(Harsanyi转换/FPSB均衡推导)+09信号(Spence/单交叉/廉价磋商→LLM通信)+lab04(收益等价蒙特卡洛+赢家诅咒)`

### Task 6: 卷三——10 重复 + 11 演化 + 12 合作 + lab05

**Files:**
- Create: `讲透博弈论/10-重复博弈与无名定理.md`, `讲透博弈论/11-演化博弈.md`, `讲透博弈论/12-合作博弈.md`
- Create: `讲透博弈论/experiments/lab05_ipd_evolution.py`（产出 `ipd_evo.png`）

- [ ] **Step 1: 写 10 章**。阶段博弈 vs 重复博弈（δ 折扣）→ **无名定理**（任何个体理性可行收益向量，δ 够大皆为 SPE 收益——直觉：未来收益阴影撑起合作）→ grim trigger 合作条件手推：δ ≥ (T−R)/(T−P)（PD 数值 T5 R3 P1 → δ≥2/4=0.5）→ 以牙还牙 TFT 四美德（善良/报复/宽容/清晰，Axelrod 1980 锦标赛叙事）→ 声誉=单玩家版未来阴影 → 无名定理的阴暗面（任何可怕惩罚也均衡 → 均衡集爆炸，连回 50 章批判）→ axelrod 工具箱（`axelrod.Tournament`）
- [ ] **Step 2: 写 11 章**。演化视角切换：策略=物种，收益=适应度 → 复制动态方程 ẋᵢ = xᵢ(fᵢ − f̄) 推导与直觉 → **ESS 两条件**（入侵者严格劣势；或持平时同伴对抗占优）→ 鹰鸽 ESS：混合 x*=v/c=2/4=0.5 手推 → ESS 与 NE 关系（ESS ⊂ NE 的精炼）→ RPS 无 ESS/极限环（连 15 章学习循环与 17 章非传递性）→ 有限种群（漂变/Moran 一段）→ **AI 连接**：训练分布的复制动态（数据生态=策略生态；RLHF 中的"策略漂移"；evolutionary dynamics of LLM agents 文献一句）
- [ ] **Step 3: 写 12 章**。合作博弈=联盟收益分配（v(S) 特征函数，非策略互动）→ **Shapley 值公式** φᵢ=Σ_S |S|!(n−|S|−1)!/n!·[v(S∪{i})−v(S)] + 四公理（有效/对称/哑元/可加）→ 手算例：三人投票博弈（51% 规则任意两人赢 v=1）→ φ=(1/3,1/3,1/3)；机场跑道成本分摊（跑道成本 100/180/220 三机型）完整手算 → 核（无阻塞联盟的分配集；三人例核可能为空——投票博弈核=空）→ **Gale-Shapley 稳定匹配**（算法步骤、稳定性定理、男方求婚对男方 DSIC）→ 市场设计：NRMP 医院匹配/择校/肾交换（引 14 章）→ **AI 连接**：Shapley 值 = 特征归因 SHAP 的数学母体（可解释 ML）；多智能体信用分配
- [ ] **Step 4: 写并跑 lab05**。E1 axelrod 锦标赛：TFT/GRIM/WSLS/Random/Prober/Alternator 等约 10 策略 round-robin（ repetitions=50，turns=200），打印排名表；E2 Moran 生态：TFT 入侵 ALLD 固定概率 vs 初始比例（axelrod `MoranProcess`）；E3 复制动态手写（欧拉积分）：鹰鸽 v=2,c=4 从 x₀=0.1/0.5/0.9 三条轨线收敛到 0.5 + RPS 极限环演示（同一积分器）。理论锚点打印：x*=v/c=0.5，轨线终值 vs 理论 ±0.01。产出 ipd_evo.png（1×3：锦标赛得分条形/Moran 固定概率/复制动态相图）。**前置：axelrod 已装**
- [ ] **Step 5: README 翻 ✅（10/11/12 + lab05）**
- [ ] **Step 6: Commit** `feat: 讲透博弈论卷三——10重复(无名定理/TFT/δ阈值手推)+11演化(复制动态/ESS/RPS环)+12合作(Shapley公理/核/Gale-Shapley→SHAP)+lab05(axelrod锦标赛+Moran+手写复制动态)`

### Task 7: 卷四——13 拍卖 + 14 机制设计 + lab06

**Files:**
- Create: `讲透博弈论/13-拍卖理论.md`, `讲透博弈论/14-机制设计与显示原理.md`
- Create: `讲透博弈论/experiments/lab06_matching_shapley.py`（产出 `coop.png`）

- [ ] **Step 1: 写 13 章**。四标准拍卖（英式升价/荷兰降价/一价密封/二价密封）→ 私人价值 vs 共同价值模型 → **收益等价定理**（风险中性+对称+私人价值 → 卖方期望收入只依赖分配规则不依赖支付规则；U[0,1] n 人 → E[R]=(n−1)/(n+1)；直觉：赢者支付≈次高估价的函数）→ 二价=VCG 特例（占优策略诚实）→ **VCG 通用机制**（社会剩余最大化+外部性收费；组合拍卖一段）→ 赢家诅咒（油田/钱包拍卖；为何荷兰式更凶）→ 保留价与最优拍卖一段（Myerson 一句话）→ lab04 回指
- [ ] **Step 2: 写 14 章**。机制设计=反向博弈论（设计规则让自利者实现设计者目标）→ 环境（类型/社会选择函数）→ **IC/IR 约束** → **显示原理**（任何机制任何均衡 ⇔ 说真话的直接机制；证明梗概：把均衡策略包进机制）→ VCG 再访（唯一同时 IC+有效+无预算赤字的通用类，GVA）→ 肾交换（cycles+chains 的匹配算法市场设计——UNOS 真实系统）→ 择校（Boston 机制可操纵 vs 稳定匹配机制；真实改革史一段）→ **AI 连接**：RLHF=机制设计（奖励模型=社会选择函数；激励相容的标注者）；数据/模型市场；自动化机制设计（AMD，Conitzer）
- [ ] **Step 3: 写并跑 lab06**。E1 Shapley 数值验证：三人投票博弈 φ=(1/3,1/3,1/3) 公式计算 + 公理逐条数值验证（对称性：交换标签 φ 不变；哑元：加第四个无权玩家 φ₄=0）；机场跑道（100/180/220）：6 种排列枚举增量 → 打印 A≈33.3/B≈? /C≈?（执行者现场算出并与公式对拍）；E2 Gale-Shapley 手写：4×4 随机偏好，打印匹配 + 稳定性检查（枚举所有阻塞对=0）+ 男求婚 vs 女求婚差异展示；E3 谎报实验：对每个参与者枚举谎报偏好重跑，统计"谎报严格改进"次数（预期：求婚方 0 次=DSIC 验证；被求婚方可改进 ≥1 例）。产出 coop.png（1×3：机场增量瀑布/两方向匹配对比/谎报改进计数）。纯 numpy，无外部依赖
- [ ] **Step 4: README 翻 ✅（13/14 + lab06）**
- [ ] **Step 5: Commit** `feat: 讲透博弈论卷四——13拍卖(收益等价/VCG/赢家诅咒)+14机制设计(显示原理/肾交换/择校→RLHF机制视角)+lab06(Shapley公理数值验证+Gale-Shapley谎报实验)`

### Task 8: 卷五上——15 博弈学习 + 16 CFR 与扑克 + lab07 + kuhn_poker CLI

**Files:**
- Create: `讲透博弈论/15-博弈学习经典.md`, `讲透博弈论/16-CFR与扑克AI.md`
- Create: `讲透博弈论/experiments/lab07_learning_cfr.py`（产出 `learning.png`；**内含可 import 的 `KuhnCFR` 类**，CLI 复用）
- Create: `讲透博弈论/cli/kuhn_poker.py`

**Interfaces:**
- Produces: `lab07_learning_cfr.KuhnCFR`（方法：`train(iterations)`, `average_strategy()`, `converged_value()`），`cli/kuhn_poker.py` 导入它（`sys.path` 处理：`sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "experiments"))`）
- Consumes: 15 章遗憾匹配（16 章 CFR 是其扩展式推广）

- [ ] **Step 1: 写 15 章**。学习视角：均衡不是从天上掉下来——谁在学？→ **fictitious play**（对历史平均最优反应；收敛定理：零和/势博弈收敛；RPS 信念循环、平均策略→均衡中心）→ **no-regret 框架**（外部遗憾定义；Hedge/指数权重 O(√T)；连讲透优化 25 在线优化章——同一数学两种方言）→ **遗憾匹配 RM**（比例于正遗憾随机化）→ **定理：平均遗憾→0 ⇒ 经验分布收敛到粗相关均衡 CCE**（证明主脉一段）→ CCE vs CE vs NE 层级回顾 → 博弈学习的收敛性地图（哪些博弈类哪些算法必收敛——引 17 章非传递困境）
- [ ] **Step 2: 写 16 章**。扑克=不完美信息博弈的果蝇 → Kuhn 扑克规则（3 张牌 J/Q/K，各 1 筹码 ante，一轮流：bet/call/fold/check/raise）→ **反事实价值与遗憾的扩展式推广**（到达概率加权；给 CFR 递归伪代码框）→ 手推 Kuhn 均衡要点（博弈值 = −1/18 ≈ −0.0556 对 P1；均衡策略族：P1 以 1/3 概率用 J 诈唬-bet 等——正文给一个代表性均衡，全族标注文献）→ CFR+ 改进（交替更新+regret matching+ 一段）→ **Libratus(2017)/Pluribus(2019) 脉络**（无提前抽象、自博弈+实时平衡策略；Pluribus 仅 ~20 万美元算力击败人类多桌）→ open_spiel 对拍预告（lab08）
- [ ] **Step 3: 写并跑 lab07**。E1 FP 轨迹：PD（收敛到 (D,D)）与 RPS（信念三角循环 + 平均趋中心）两图，打印最终频率；E2 regret matching on RPS：平均遗憾 vs T 的 log-log 图（理论斜率 −1/2），打印 T=10⁵ 时平均遗憾数值（<0.02 量级）；E3 **手写 CFR 类 `KuhnCFR`**（vanilla CFR，chance 结点用 Kuhn 三牌排列枚举；`train(200000)` 后打印：平均策略的博弈值 vs 理论 −1/18（容差 0.01 ✓）+ P1 各信息集平均策略表）。产出 learning.png（1×3：FP 三角/RPS regret log-log/Kuhn CFR 收敛曲线 value vs 迭代）。纯 numpy
- [ ] **Step 4: 写并跑 cli/kuhn_poker.py**。`--demo`：CFR bot vs CFR bot 500 手，打印统计（P1 平均收益 ≈ −1/18 ± 抽样波动，P2 ≈ +1/18）；交互模式：人类 P1 vs CFR bot P2，每手打印手牌/公共动作序列，结束揭示 bot 当时的真实混合策略与"诈唬频率"点评；退出打印总分。运行 `python cli/kuhn_poker.py --demo` 冒烟
- [ ] **Step 5: README 翻 ✅（15/16 + lab07 + kuhn_poker CLI）**
- [ ] **Step 6: Commit** `feat: 讲透博弈论卷五上——15学习(FP/no-regret/Hedge→CCE桥)+16CFR(Kuhn手推/Libratus脉络)+lab07(手写CFR收敛-1/18)+kuhn_poker可玩CLI(CFR机器人对赌)`

### Task 9: 卷五下——17 自博弈 MARL + 18 LLM×博弈

**Files:**
- Create: `讲透博弈论/17-自博弈与多智能体RL.md`, `讲透博弈论/18-博弈论与LLM对齐.md`

- [ ] **Step 1: 写 17 章**。完美信息侧：**MCTS**（选择=UCB1、扩展/模拟/回传四步；强项=无需领域知识渐进变强）→ **AlphaZero 循环**（自博弈生成→网络训练→评估替换，三步闭环；Zero=从零规则学习）→ 不完美信息侧：**NFSP**（RL 最佳反应 + FP 平均策略双流混合，2016 DeepMind）→ **PSRO**（策略人口+oracle 扩张，处理非传递）→ **非传递性困境**：RPS 环→单一 self-play 会绕圈（"向新手投降"现象）；人口方法=演化视角回归（连 11 章）→ 多智能体学习的难题地图（非平稳性：对手在学→MDP 假设破产；连 50 章）→ OpenSpiel 工具箱（`pyspiel.load_game`, python algorithms 目录）→ lab09 回指
- [ ] **Step 2: 写 18 章**。LLM 博弈能力（GTBench 2024 等：LLM 在完全信息博弈显著弱于不完美信息博弈的策略推理；行为偏差系统性：不随机化/被 exploit）→ multi-agent debate = 议价博弈（发言=信号=09 章廉价磋商；收敛性存疑文献）→ self-play for LLM（SPIN/SPPO：生成器vs判别器军备竞赛——连 17 章 PSRO 直觉）→ **对齐=机制设计**（人类 principal—LLM agent；RLHF 的机制视角：标注者激励/奖励黑客=agent 的策略性应对；constitution=社会选择函数）→ **algorithmic collusion**（定价算法自发串谋文献——机制设计者视角的噩梦与监管）→ 诚实框：领域月度演化，本章是 2026-09 快照
- [ ] **Step 3: README 翻 ✅（17/18）**
- [ ] **Step 4: Commit** `feat: 讲透博弈论卷五下——17自博弈(MCTS/AlphaZero循环/NFSP/PSRO/非传递环)+18LLM×博弈(GTBench/debate=廉价磋商/对齐=机制设计/算法定价串谋)`

### Task 10: WSL 进阶——lab08 OpenSpiel CFR 对拍 + lab09 MCTS 自博弈

**Files:**
- Create: `讲透博弈论/wsl_labs/lab08_openspiel_cfr.py`（产出 `openspiel_cfr.png`）
- Create: `讲透博弈论/wsl_labs/lab09_selfplay_mcts.py`（产出 `mcts_selfplay.png`）
- Create: `讲透博弈论/wsl_labs/README.md`（WSL 环境搭建 6 行：wsl -d Ubuntu-22.04 → apt/pip 镜像 → pip install open_spiel matplotlib numpy；运行方式；产物如何拷回 Windows 侧自动同盘可见）

**Interfaces:**
- Consumes: lab07 `KuhnCFR` 的收敛值（对拍基准 −1/18）
- Produces: 17/16 章的 WSL 实验回指目标

- [ ] **Step 1: WSL 环境验证**：`wsl -d Ubuntu-22.04 -- bash -lc "python3 --version && pip3 --version"`；若无 pip 走 apt（先查清华 apt 镜像源再装）；`pip3 install -i https://pypi.tuna.tsinghua.edu.cn/simple open_spiel matplotlib`（open_spiel 依赖自带 tensorflow? 否——纯 python API 不需要；若安装失败：报告错误并降级 lab08/09 为 numpy 手写扩展版+文档标注）。验证：`python3 -c "import pyspiel; g=pyspiel.load_game('kuhn_poker'); print(g)"`（预期打印 game name/type）
- [ ] **Step 2: 写 lab08**。用 `open_spiel.python.algorithms.cfr`（`CFRSolver`）跑 Kuhn 10⁵ 迭代，每 10³ 记录 exploitability/平均策略值；打印最终值 vs −1/18（容差 0.01）**vs lab07 手写最终值**（三方对拍表：理论 / open_spiel / 手写）；再跑 CFR+（`CFRPlusSolver`）同曲线对比收敛速度。产出 openspiel_cfr.png（值+exploitability 双对数收敛曲线，双算法）
- [ ] **Step 3: 写 lab09**。MCTS（纯 python：UCB1 选择+随机 rollout）自博弈井字棋：训练循环 0/10²/10³/10⁴ 次模拟/步 的 agent 依次互赛 + vs 纯随机，打印胜率矩阵（预期：模拟次数↑胜率单调↑；10⁴ 时对随机近乎全胜、对 10² 显著优）；一段"AlphaZero 最小化说明"：把 rollout 换成价值网络+策略先验即 Zero 循环（代码骨架注释展示，不实装 torch）。产出 mcts_selfplay.png（棋力等级分随自博弈计算量的 Elo 式曲线）。纯 python 标准库+numpy/matplotlib（open_spiel 版井字棋对拍可选加分项：`load_game('tic_tac_toe')` + `open_spiel.python.algorithms.mcts` 若 API 可用则双实现对照，不可用则手写版+注释说明）
- [ ] **Step 4: 两 lab 在 WSL 实跑**（`wsl -d Ubuntu-22.04 -- bash -lc "cd /mnt/c/workspace/work4ai/讲透博弈论/wsl_labs && python3 lab08_openspiel_cfr.py"` 等），核对 png 生成在 Windows 侧同目录可见
- [ ] **Step 5: README 翻 ✅（wsl_labs 两行）+ wsl_labs/README.md 挂网**
- [ ] **Step 6: Commit** `feat: 讲透博弈论WSL进阶——lab08 OpenSpiel官方CFR/CFR+对拍手写(-1/18三方对拍)+lab09 MCTS自博弈井字棋力曲线(AlphaZero最小化说明)`

### Task 11: 可玩 CLI——ipd_arena + auction_house

**Files:**
- Create: `讲透博弈论/cli/ipd_arena.py`, `讲透博弈论/cli/auction_house.py`

**Interfaces:**
- Consumes: axelrod（ipd_arena）；lab04 拍卖逻辑模式（auction_house 自带精简实现，不 import lab04——CLI 独立可运行）
- Produces: 完成 12 程序清单

- [ ] **Step 1: 写 cli/ipd_arena.py**。`--demo`：axelrod 十策略循环赛打印积分榜（冒烟=无人工输入）；交互：玩家输入轮数与对手选择（编号列出 TFT/GRIM/WSLS/Random/Alternator/Prober…含一句中文简介），玩家每轮输入 C/D，实时显示累计得分与对手上一手，结束打印总分+你的策略性格点评（合作率/报复率统计）。运行 `python cli/ipd_arena.py --demo` 冒烟
- [ ] **Step 2: 写 cli/auction_house.py**。三种拍卖（一价/二价密封、英式升价逐轮加价）：`--demo`：均衡机器人（估价~U[0,100]，一价出 (n−1)/n·v、二价出 v、英式坚持到次高价）自拍 2000 场，打印三种平均卖价（理论全部 ≈ E[次高]≈？n=4 U[0,100] → 60±2 验证）；交互：随机抽你的真实估价，与 3 个均衡机器人竞拍，你的匿名报价，成交后揭示所有人估价与最优反应点评（"二价里报真实估价就是最优——你偏离了 X 元，期望损失 Y"）。运行 `--demo` 冒烟
- [ ] **Step 3: README 翻 ✅（cli 两行）**
- [ ] **Step 4: Commit** `feat: 讲透博弈论可玩CLI——ipd_arena(对打axelrod生态+策略性格点评)+auction_house(三拍卖人机竞拍+最优反应揭示)`

### Task 12: 总验 + 挂网核对 + 系列收官

**Files:**
- Modify: `讲透博弈论/README.md`（全部状态 ✅、运行环境实测更新、互链复核）
- Create: `讲透博弈论/总总结.md`（账本式收官，照讲透优化总总结体例：全系列一张账本+跨卷统一视角）

- [ ] **Step 1: 全量重跑**：Windows 侧 7 lab + 3 CLI --demo 逐个运行，收集控制台输出；理论锚点核对清单：猜数→0 ✓、nashpy=手算 <1e−8 ✓、LP=nashpy 值 ✓、FPSB E[支付]=1/3 ✓、收益等价四路 ≈1/3 ✓、δ阈值 0.5 ✓、x*=0.5 ✓、Shapley 1/3 ✓、谎报 DSIC=0 ✓、CCE 收敛 ✓、Kuhn −1/18 ✓（lab07 与 lab08 双侧）、MCTS 单调棋力 ✓、拍卖 demo ≈次高价 ✓。任何 ✗ → 修复后重跑（禁止放宽理论值）
- [ ] **Step 2: WSL 侧重跑 lab08/lab09** 核对
- [ ] **Step 3: 挂网核对**：README 篇目表 20 章行 + 12 程序行全部 ✅；抽查每章正文至少一处互链（00↔各卷导览、04↔讲透优化09/25、09↔18、11↔15/17、12↔14、14↔18、50↔讲透优化50）；grep 检查孤儿（README 未收录文件）
- [ ] **Step 4: 写 总总结.md**：一张账本（每章一句"你带走了什么"）；跨卷统一视角三条：①"均衡"四副面孔（规范/实证/演化/学习不动点）；②"信息"主线（完美→不完美→不完全→信号→机制=信息设计）；③"理性"的降级史（全理性 NE→k-level→QRE→演化→学习——理论与现实和解之路）
- [ ] **Step 5: Commit** `feat: 讲透博弈论全量收官——20章+12程序全绿(13项理论锚点诚实校准)+总总结(均衡四副面孔/信息主线/理性降级史)`

## Self-Review 结论

1. **Spec 覆盖**：spec §2 的 20 篇 → Task 1-9 全覆盖；§3 的 7+2+3 程序 → Task 2-8/10/11 全覆盖；§4 验证 → Task 12；§5 不做清单未越界（无 web/notebook/自研求解器）
2. **占位符扫描**：无 TBD/TODO；机场跑道 Shapley 的 B/C 分摊值标注"执行者现场计算并与公式对拍"——这是实验设计（对拍本身是验证内容），非遗漏
3. **类型一致性**：`KuhnCFR.train/average_strategy/converged_value` 在 Task 8 定义、Task 8 CLI 与 Task 10 对拍沿用同一数值锚点 −1/18；三原型记号 T5R3P1S0 在 Task 1 定义、Task 2/6 沿用；罚点球矩阵 [[0.65,0.95],[0.95,0.55]] 在 Task 2/3 一致

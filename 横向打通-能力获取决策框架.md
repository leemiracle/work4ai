# 横向打通：AI 能力获取决策框架

> 这是整个 work4ai 的**总纲**。前面五个「讲透」系列分别讲了基础模型、微调、RAG、Prompt 工程（+激活函数地基）。这一篇把它们**横向打通**：当你面对一个 AI 需求，该用哪个？怎么组合？

---

## 1. 先理清：AI 能力的「造、调、用」三层

把所有方法放进一个生命周期，瞬间清晰：

```
【造能力】预训练(讲透基础模型 00-02)
   从海量数据造出"通才基座". 一次造, 反复用. 成本最高(千万美元级).
      │
【调能力】后训练(讲透基础模型 05 + 讲透微调)
   SFT/DPO/LoRA 把基座调教成"听话专才". 改行为/风格/格式. 成本中(百~万美元).
      │
【用能力】推理时方法(讲透RAG + 讲透Prompt + Agent)
   不改模型, 在推理时注入知识/给指令/自主规划. 成本最低(推理费). 实时灵活.
```

**关键洞察**：大部分日常需求（知识问答、文档处理、对话）根本不需要造/调模型，**推理时方法（Prompt/RAG）就够**。造/调模型是重投入，要反复用才回本。

---

## 2. 四极谱系：Prompt / RAG / 微调 / Agent

"用/改能力"的四种主流方法，按"改不改模型 + 注不注入外部信息"分：

| 方法 | 改模型？ | 注外部信息？ | 核心动作 | 延迟 | 成本 |
|---|---|---|---|---|---|
| **Prompt** | ❌ | ❌（除上下文） | 用自然语言指令控制输出 | 最低 | 最低 |
| **RAG** | ❌ | ✅（检索文档） | 检索相关知识注入 prompt | 中（检索+生成） | 中 |
| **微调** | ✅ | ❌ | 用数据改权重固化能力 | 低（推理同模型） | 高（训练） |
| **Agent** | ❌ | ✅（工具/多轮） | 自主规划+多步工具调用 | 高（多轮） | 高 |

> Agent 横跨"推理时"，但它用工具 + 多轮，复杂度最高，单独成一极（讲透 Agent 待做）。

---

## 3. 决策矩阵：按「需求类型」选方法

这是最实用的部分。先问"你要解决的是什么问题"：

| 你的需求 | 首选 | 理由 |
|---|---|---|
| 让模型**知道新事实/最新信息** | **RAG** | 微调学知识又贵又易幻觉、不可溯源；RAG 实时可更新 |
| 让模型**固定输出格式/风格/语气** | **微调(LoRA)** | Prompt 难稳定固化行为；微调写入权重可靠 |
| **临时**适配任务、要**可溯源** | **Prompt** | 零成本、可引用上下文 |
| **复杂多步**任务、要**自主规划+工具** | **Agent** | 单次 RAG/Prompt 搞不定，需多轮 |
| **全新底层能力**（新语言/新模态） | **预训练/继续预训练** | 推理时方法和微调都学不会全新能力 |
| **高频生产任务**、要**极致精度** | **微调** | 固化能力比每次 prompt 稳 |
| **多任务热切换** | **Prompt / RAG**（不改模型） | 微调要多份权重 |

---

## 4. 组合策略（实战常态）

真实系统几乎都是**组合**，不是单选：

| 组合 | 场景 | 怎么配 |
|---|---|---|
| **Prompt + RAG** | 知识问答、客服 | 检索文档塞 prompt，prompt 指令"基于文档答" |
| **微调 + RAG** | 领域专家系统 | LoRA 调出领域语感 + RAG 注入领域最新知识 |
| **微调 + Prompt** | 生产对话 | 微调调好对话行为，部署时 prompt 给具体任务 |
| **Agent + RAG + 工具** | 研究型助手 | Agent 规划，RAG 当检索工具，函数调用执行动作 |
| **全栈** | 企业级 | 预训练基座 → 微调领域 → RAG+Prompt+Agent 部署 |

**铁律**：**改行为用微调，注知识用 RAG，临时用 Prompt，复杂用 Agent**。四者正交，常组合。

---

## 5. 决策树（一步步选）

```
你的需求是什么?
│
├─ 要"知道新事实/最新信息"? ──是──→ RAG (微调学知识是反模式)
│
├─ 要"固定格式/风格/语气"? ────是──→ 微调(LoRA)
│
├─ 要"临时适配/可溯源"? ──────是──→ Prompt
│
├─ 要"多步推理/自主/工具"? ───是──→ Agent
│
├─ 要"全新底层能力"? ─────────是──→ 预训练(重投入, 慎选)
│
└─ 都不像 / 先验证 ───────────────→ 从 Prompt 开始(最便宜, 不行再升级)
```

**起点建议**：永远从 **Prompt** 开始（成本最低、迭代最快）。Prompt 不够 → 加 RAG（注知识）。RAG 不够 → 考虑微调（固化行为）。微调都不行 → Agent 或重新评估。

---

## 6. 成本-效果-延迟三角

```
            效果(能力上限)
                ↑
         Agent ●
              /
       微调 ●
            /
     RAG ●
          /
   Prompt ●
        /
      └─────────────→ 延迟/成本
```

- **Prompt**：便宜快，能力受限于模型本身。
- **RAG**：加知识，效果显著提升，多一次检索。
- **微调**：固化行为，效果稳，但要训练。
- **Agent**：能力最强（自主+工具），但多次 LLM 调用，最贵最慢。

**没有银弹**：最强 ≠ 最适合。Agent 解决一切会很贵很慢；简单问答用 Agent 是杀鸡用牛刀。

---

## 7. 五个系列的对应关系（一张图）

```
讲透激活函数 ──── 神经网络的地基(非线性), 是上面一切的前提
讲透基础模型 ──── 预训练(造能力) + 注意力/规模/涌现/位置/对齐/部署
     │
     ├─ 讲透微调 ── 后训练(调能力): LoRA/QLoRA/数据/失败/实战
     │
     └─ 推理时(用能力):
          ├─ 讲透RAG ──── 注入知识(检索)
          ├─ 讲透Prompt ── 控制输出(指令/Few-shot/CoT/结构化)
          └─ 讲透Agent(待做) ── 自主规划+工具
```

**本框架（横向打通）就是把这些串起来的"调度器"**：知道每层解决什么，按需调用。

### 7.1 三个"垂直深挖"系列（2026-08 新增）

上面是"造-调-用"的**横向能力链**。还有三个**垂直深挖**系列，每一个都横切多个层级，是 07-AI for AI 的三向延伸：

```
讲透数据 ────────── 横切"造/调能力": 用什么数据训?
                    数据墙/Chinchilla/合成数据/Model Collapse
                    ↳ 配套 07 第③层 (AI 训练 AI)

讲透可解释性 ────── 横切"理解所有层": 训完的模型内部是什么?
                    probing/attribution/mechanistic/SAE/steering
                    ↳ 配套 07 第⑥层 (AI 理解 AI)

讲透科学的现代性 ── 横切"反思所有层": AI 改变了什么叫科学?
                    第五范式/可重复性危机/科学哲学
                    ↳ 配套 07 元反思层 (AI 算理解吗)
```

**为什么单列**：这三个不是"造-调-用"流程里的某一环，而是**横切反思**——分别问"用什么训"、"内部是什么"、"这意味着什么"。和横向能力链**正交**，是 work4ai 反思层的骨架。

---


### 7.2 造能力链（2026-08-04 审查补充）

上面是"造-调-用"的横向能力链（偏调/用侧）。"造能力"侧也有完整链条：

```
预训练（讲透基础模型 00-02）
    │ 数据从哪来？计算怎么扩？
    ▼
数据（讲透数据）→ 数据墙/合成/Model Collapse
    │ 单卡装不下？
    ▼
分布式（讲透分布式AI系统）→ DDP/FSDP/ZeRO/TP
    │ 怎么跑更快？
    ▼
GPU/系统（讲透GPU与系统级）→ FlashAttention/CUDA/Triton
    │ 怎么高效服务？
    ▼
推理服务（讲透KV Cache）→ PagedAttention/vLLM/量化
```

**这条链是 AI Infra 工程师的主干线**——从"造模型"到"部署模型"的完整工程路径。和上面的"调/用能力链"互补，构成 work4ai 的**全栈能力图谱**。

## 8. 一句话总结

> **AI 能力获取分三层：预训练(造)、后训练微调(调)、推理时方法(用)。日常需求多靠推理时方法解决——改行为用微调，注知识用 RAG，临时用 Prompt，复杂用 Agent，四者正交常组合。决策永远从最便宜的 Prompt 开始，不够再升级(RAG→微调→Agent)。没有银弹，最强不等于最适合，按需求/成本/延迟三角选。**

---

## ✍️ 实战练习

针对以下 8 个需求，判断用哪种方法（或组合），并说理由：
1. 公司内部规章制度问答系统
2. 让模型始终用某品牌口吻发微博
3. 帮用户查实时航班 + 订餐
4. 把中文新闻翻译成英文
5. 让模型懂一门全新的人造语言
6. 法律合同风险点提取（固定格式输出）
7. 科研文献综述（要引用）
8. 自动写代码 + 调试 + 部署

（参考答案见思考：1-RAG 2-微调 3-Agent 4-Prompt 5-预训练 6-微调(+RAG) 7-RAG(+Agent) 8-Agent）

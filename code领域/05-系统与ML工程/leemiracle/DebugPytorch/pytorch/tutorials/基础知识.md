# 快速入门
- 流程：使用数据、创建模型、优化模型参数（loss及优化器）、保存（加载）模型

device = torch.accelerator.current_accelerator().type if torch.accelerator.is_available() else "cpu"

# 张量
- 创建：来源python对象 numpy、张量（ torch.ones_like、  torch.rand_like）
- 属性
  - shape 是一个张量维度的元组
  - dtype 
  - 设备
- 操作：超过 1200 种（如 https://pytorch.ac.cn/docs/stable/torch.html#tensors）
- 与numpy的相互转换
# 数据
- dataset
- dataloader
- torch.utils.data API
#  变换（transforms）
 对数据进行一些处理，使其适合训练。
- transform 用于修改特征，target_transform 用于修改标签 
- torchvision.transforms API
# 构建模型
- 用于训练的设备：
- 定义类：继承 nn.Module 来定义我们的神经网络
  - 模型层
  - 参数
# 自动微分
- https://pytorch.ac.cn/docs/stable/autograd.html#function
- 雅可比矩阵  海森矩阵
- torch.autograd.Function
  - function.FunctionCtx
  - 自定义 Function 工具类: function.BackwardCFunction等
- 数值梯度检查
- 分析器torch.autograd.profiler
  - torch.autograd.profiler.profile
  - 模式：
    - 仅限 CPU 使用 profile；
    - 基于 nvprof（同时记录 CPU 和 GPU 活动）使用 emit_nvtx；
    - 基于 vtune 分析器使用 emit_itt
- 调试和异常检测：torch.autograd.detect_anomaly
- Autograd 计算图
  - Tensor 的 grad_fn 属性持有一个 torch.autograd.graph.Node
    - 操作需要在前向传播期间保存中间结果，以便执行后向传播。这些中间结果被保存为 grad_fn 上的属性，并且可以访问
  - hook
    - autograd.graph.saved_tensors_hooks： pack_hook / unpack_hook 钩子来控制 保存的张量如何打包/解包
      - y.grad_fn._raw_saved_self.register_hooks(pack_hook, unpack_hook)
    - autograd.graph.GradientEdge
# 优化
- 超参数：训练轮数 (Epochs) - 遍历数据集的次数

批量大小 (Batch Size) - 在更新参数之前通过网络传播的数据样本数量

学习率 (Learning Rate) - 在每个批量/训练轮中更新模型参数的幅度。较小的值会产生较慢的学习速度，而较大的值可能导致训练期间出现不可预测的行为。

- 优化循环：
  训练循环 - 遍历训练数据集并尝试收敛到最优参数。

验证/测试循环 - 遍历测试数据集，检查模型性能是否正在提高。

- 损失函数
- 优化器

# 保存或加载模型
- 通用检查点


# YouTube 系列教程
- 使用tensorboard
- **captum**理解模型（可解释性）

# 示例
基本概念

# 创建模型（nn）

# tensorboard

# 其他
- 各场景下的应用
  - 语音等
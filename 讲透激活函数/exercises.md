# 练习题 · 讲透激活函数

> 「输出倒逼输入」。以下练习按章节组织，难度递进。建议先独立完成，再对照各章的「实证脚本」验证。带 ⭐ 的是推荐必做。

---

## 第 00 章 · 为什么需要激活函数

**Q0.1** ⭐ 如果激活函数 $f(z)=2z$（放大的线性函数），三层网络还能拟合 $\sin$ 吗？用一句话和一行公式说明。

**Q0.2** 修改 `experiments/00_why_activation.py`，把 ReLU 换成 `nn.Identity()`（恒等映射），观察拟合 sin 的 MSE 是否与纯线性网络一样差，并解释。

**Q0.3** ReLU 在 $x=0$ 处不可导，神经网络却能用它训练。框架是怎么处理这个「不可导」点的？（一句话）

---

## 第 01 章 · ReLU 自动微分与反向传播

**Q1.1** ⭐ 手算：输入 $X=[-1, 2, -3, 4]$，上游梯度 $g=[5,6,7,8]$，写出 ReLU 反向后的 $\frac{\partial L}{\partial X}$。再用 PyTorch 验证。

**Q1.2** ReLU 反向时框架缓存「输入 X」还是「掩码 M」？后者比前者省多少显存？为什么？

**Q1.3** 把 $x=0$ 的次梯度约定从 0 改成 1，会改变训练结果吗？为什么没人纠结？

**Q1.4** ⭐ （进阶）ReLU 的掩码 $M=(X>0)$ 是不可导的阶跃函数，autograd 却能工作——它是怎么「绕过」不可导点的？

---

## 第 02 章 · 为 0 与为 1 发生了什么

**Q2.1** ⭐ 5 层纯 sigmoid 网络，假设每层局部导数都取最大值 0.25，最浅层收到多少比例的梯度？换成 ReLU 呢？

**Q2.2** 修改 `experiments/02_dead_relu.py`，把负偏置从 $-100$ 改成 $-1$，并用 `LeakyReLU(0.1)` 替换 ReLU，观察「死神经元」是否复活。

**Q2.3** ⭐ Adam 优化器在梯度恒为 0 的死神经元上，权重会怎样变化？（提示：Adam 分母是二阶矩的滑动平均）

**Q2.4** 为什么 ReLU 的稀疏性（大量输出 0）本身是一种**正则化**？

---

## 第 03 章 · 激活函数家族综述

**Q3.1** 给定一张导数图：最大值 0.25、对称、两端趋于 0。这是哪个激活函数？为何不适合隐藏层？

**Q3.2** ⭐ ReLU6 为什么对量化友好？若不量化，ReLU6 比 ReLU 有劣势吗？

**Q3.3** 用 `experiments/04_activation_zoo.py` 的框架，加画 ELU（$x>0$ 时 $f=x$，否则 $f=\alpha(e^x-1)$）的曲线与导数，观察其零中心化特性。

**Q3.4** ⭐ SwiGLU 比 GELU 多了「门控相乘」，为何能提升 LLM 参数效率？

---

## 第 04 章 · 量化中的激活函数

**Q4.1** ⭐ 张量最大绝对值 50，对称 INT8 量化，$S$ 是多少？若其中有个值 0.3，量化后是几？反量化误差多大？

**Q4.2** 为什么 ReLU6 比 ReLU 更适合 INT8？模型训练时用 ReLU，部署换 ReLU6 需重训吗？

**Q4.3** ⭐ 修改 `experiments/05_quantization_demo.py` 把 INT8 改 INT4（`n_bits=4`），观察「离群点 underflow 比例」如何变化。

**Q4.4** 你为自己的 NPU 写 EP，硬件只支持 INT8 整数，模型用了 GELU。你会选 LUT 还是反量化？各自代价？

---

## 第 05 章 · 替换与动态切换

**Q5.1** ⭐ 为何 ReLU→ReLU6 几乎无损，ReLU→GELU 会崩？（从「分布是否改变」「权重是否见过新输入」答）

**Q5.2** 把 GELU 的 BERT 部署到只支持 INT8 ReLU 的 DSP，用场景 B 蒸馏思路，写 Teacher/Student 定义与训练循环伪代码。

**Q5.3** ⭐ 退火 Leaky ReLU 时，$\alpha$ 线性衰减好还是余弦衰减好？为什么？

**Q5.4** 设计实验：用 ReLU 训练 MLP 到收敛记录精度；换 GELU 直接推理（不微调）看掉多少；再微调 3 epoch 看能否恢复。（用第 00 章 sin 框架改造）

---

## 第 06 章 · SwiGLU 底层 Kernel

**Q6.1** ⭐ 手算：$X=[1,2]$，$W_{\text{gate}}=[[1],[0]]$，$W_{\text{up}}=[[0],[1]]$，求 SwiGLU（$\sigma(1)\approx0.731$）。

**Q6.2** 朴素 SwiGLU 有几次 HBM 写、几次读？融合后呢？为何「$X$ 只读一次」在 GPU 上收益巨大？

**Q6.3** ⭐ 在不支持 CUTLASS 的自研 NPU 上实现 SwiGLU，无法做寄存器 Epilogue 融合，退而求其次能做什么？

**Q6.4** 为什么 SwiGLU 的门控相乘能提升参数效率？写一段话解释它与「动态稀疏激活 / 条件计算」的关系。

---

## 综合项目题（挑战）

**P.1** ⭐⭐ 实现一个 `MyReLU` 自定义 autograd Function（继承 `torch.autograd.Function`），手写 `forward`（缓存 mask）和 `backward`（mask ⊙ grad），与 `torch.relu` 对拍，要求梯度完全一致。再实现 `MySwiGLU` 同样对拍。

**P.2** ⭐⭐⭐ 复现「退火 Leaky ReLU」训练：在 CIFAR-10 的一个小 CNN 上，比较 (a) 全程 ReLU (b) 全程 LeakyReLU(0.1) (c) $\alpha$ 从 0.1 余弦退火到 0，三者最终精度 + 推理速度 + INT8 量化后精度掉幅。写一份 1 页实验报告。

**P.3** ⭐⭐ 用 NumPy 裸实现对称 INT8 量化 + 反量化 + QLinearGELU 的 LUT 版本，对一个随机张量对比浮点 GELU 与 LUT-GELU 的最大误差，并统计 LUT 在哪些输入区间误差最大。

---

## 自测清单（全部能答 = 讲透）

- [ ] 能用一句话解释为什么没有激活函数网络就坍缩为线性
- [ ] 能默写 ReLU 反向公式 $\frac{\partial L}{\partial X}=\frac{\partial L}{\partial Y}\odot M$
- [ ] 能解释 Dead ReLU 的成因，并说出「梯度为 0 但权重仍变」的两种优化器特例
- [ ] 能算出 $0.25^{20}$ 并解释为何 sigmoid 深层网络不可训练
- [ ] 能说出四代激活函数的代表和各自解决的痛点
- [ ] 能说出量化误差的两大真因（舍入 + 离群点），并解释为何激活函数只是放大器
- [ ] 能解释为何 ReLU→GELU 不能零样本替换，而 ReLU→ReLU6 可以
- [ ] 能画出 SwiGLU 朴素 vs 融合的访存差异，说出 Epilogue 融合的本质

# 00 — 为什么 Prompt 是控制信号？

> 这是「讲透 Prompt 工程」第一篇。Prompt 工程常被当成"玄学技巧"，但它的本质极其清晰：**LLM 是条件概率模型 P(输出|输入)，Prompt 就是那个"条件"——改 prompt 就是改输出分布**。理解这一点，prompt 工程就从"碰运气"变成"有依据的调试"。
>
> 配套实验：`experiments/00_why_prompt.py`。

---

## 1. Prompt 的本质：它是条件概率里的「条件」

LLM（解码器）生成文本的数学定义就是条件概率链：

$$P(\text{输出} \mid \text{输入}) = \prod_t P(y_t \mid y_{<t}, \underbrace{\text{输入}}_{\text{Prompt}})$$

**Prompt 就是那个"输入/条件"**。改 prompt，整个输出分布就变。模型权重一个参数都没动，但输出可以截然不同。

### 实验（`00_why_prompt.py` Part 1）

用一个固定的小 trigram 模型，不同 prompt（条件）→ 不同输出分布：

```
P(next | 'cats are')  = {animals: 1.0}
P(next | 'cars are')  = {machines: 1.0}
P(next | 'roses are') = {plants: 1.0}
```

**模型没变，只因条件（prompt）不同，输出完全不同。** 这就是 prompt 的本质——它是控制 LLM 输出分布的旋钮。

---

## 2. Prompt 的三要素

真实 prompt 不是简单的前缀，而是三部分组合：

| 要素 | 作用 | 例子 |
|---|---|---|
| **指令 (Instruction)** | 定任务 | "把句子翻译成英文" |
| **上下文 (Context)** | 给背景/知识 | RAG 检索到的文档、背景说明 |
| **例子 (Examples)** | 示范格式/模式 | Few-shot 的"输入A→输出B" |

三者拼成条件。**Prompt 工程就是调这三者**，让输出分布落在你想要的区域。

> 注意：RAG（讲透RAG 00）本质就是把"上下文"这一要素做强（检索注入知识）。所以 RAG 是 Prompt 工程的一个子集/强化。

---

## 3. Prompt = 用自然语言「编程」

| | 传统编程 | Prompt 工程 |
|---|---|---|
| 语言 | 代码（Python/C） | 自然语言 |
| 执行器 | 编译器/解释器 | LLM |
| 输出 | 确定 | 概率 |
| 调试 | 改代码 | 改 prompt |

**Prompt 工程师 = 用自然语言写"程序"的人**。模型是解释器，prompt 是代码。好 prompt 像好代码：清晰、无歧义、给足必要信息、有适当"约束"。

不同点：代码精确，prompt 是概率的——所以 prompt 需要像调代码一样反复测试、迭代。

---

## 4. 为什么 Prompt 工程值得专门学

1. **零成本切换任务**：同一个模型，换 prompt 就能做翻译/摘要/代码/推理——不用重训（对比微调的成本）。
2. **是其他技术的接口**：RAG 把检索结果塞进 prompt；Agent 把工具说明写进 prompt；微调后的模型仍要靠 prompt 驱动。**Prompt 是人机交互的统一接口。**
3. **投入产出比极高**：改一行 prompt 可能让效果翻倍，而改模型要几天训练。

---

## 5. 局限与诚实声明

- **概率性**：prompt 不是确定性程序，同一 prompt 多次运行可能不同（温度>0）。需要采样多次/设温度 0 提稳定性。
- **依赖模型能力**：复杂 prompt 技术（Few-shot ICL、CoT）依赖大模型的**涌现能力**，小模型上效果差（讲透基础模型 03）。
- **本实验局限**：trigram 只演示"prompt 是条件"这个最底层原理；真实的指令遵循/ICL/CoT 需要大模型，后续篇用文献证据 + 大模型现象讲透。

---

## 6. 一句话总结

> **Prompt 的本质是条件概率 P(输出|输入) 里的"条件/输入"——改 prompt 就是改输出分布，模型权重不变。它由指令+上下文+例子三要素组成，RAG 就是把"上下文"做强。Prompt 工程是用自然语言"编程"LLM：投入产出比极高（换一行 prompt 顶几天训练），但概率性 + 依赖模型涌现能力是其局限。**

---

## ✍️ 练习（`00_why_prompt.py`）

1. **条件威力**：在 trigram 里加新句子 "rocks are minerals"，测 `P(next|'rocks are')`——亲手感受"训练数据决定 prompt 能激发什么"。
2. **三要素拆解**：拿一个真实 prompt（如 ChatGPT 的系统提示），拆出指令/上下文/例子三部分。
3. **概率性**：思考为什么同一 prompt 多次回答可能不同，这对工程化（如固定格式输出）意味着什么。

---

## 「讲透 Prompt 工程」系列规划

| # | 主题 |
|---|---|
| **00** | **为什么 Prompt 是控制信号** ✅ 本篇 |
| 01 | Few-shot 与 In-Context Learning（不更新权重学新任务的奇迹） |
| 02 | CoT 思维链（分步推理为何比直接答准） |
| 03 | 结构化输出与函数调用（JSON / 工具使用） |
| 04 | 上下文工程与 Prompt 评估 |

---

**下一篇**：`01-Few-shot与ICL` — 只在 prompt 里放几个例子，模型就能学新任务，且**不更新任何权重**。这个叫 In-Context Learning 的奇迹是怎么来的？

---

## 费曼回炉记录（L2 自检 · 已迭代）

- **F2 卡壳点**：
  - **卡点 A**：长期把 prompt 工程当成"玄学技巧"——感觉调 prompt 像念咒语，灵了不知道为什么灵、崩了不知道为什么崩。重读第 1 节那个 trigram 实验才钉死：**LLM 的数学定义就是 $P(\text{输出}|\text{输入})$，prompt 是那个"条件"**——改 prompt 就是改输出分布，模型权重一个数都没动。从"碰运气"升级为"调条件分布"，调试方向就有了。
  - **卡点 B**：一直把 prompt 当成"一句话"，没意识到真实 prompt 是三要素组合（指令+上下文+例子）。重读第 2 节才分清：RAG 是把"上下文"做强、Few-shot 是把"例子"做强、系统提示是把"指令"做强——**调这三要素就是 prompt 工程的全部**。

- **F3 术语翻译**：
  - "条件概率 $P(y|x)$" → **给定前半句，后半句最可能长什么样**——prompt 就是那个"前半句"，它决定了"后半句"的分布。
  - "In-Context Learning（ICL）" → **不更新权重，只在 prompt 里塞几个例子就学会新任务**——好像模型当场"看明白了"，其实是因为大模型在预训练时学到了"照例子类推"这种元能力。
  - "温度（Temperature）" → **模型"听话还是任性"的旋钮**——温度 0 是永远挑概率最高那个字（确定性、保守），温度高是更愿意挑概率低的字（多样、冒险）。

- **F4 回炉**：
  - **v1（错误直觉）**：以为 prompt 工程是"写好词、说漂亮话"，觉得会说话的人就会调 prompt。
  - **v2（修正后）**：钉死 prompt 工程的数学本质——**改 prompt = 改条件分布**。第 1 节实验里同一模型、不同 prompt（'cats are' / 'cars are' / 'roses are'）输出完全不同——这就是"条件"的物理含义。diff 在于把 prompt 从"自然语言表达问题"升级为**用自然语言"编程"的概率机器**——好 prompt 像好代码（清晰、无歧义、有约束），调试要像调代码（反复测试、迭代），而不是"写得有文采"。

<!--
元理论引用：故事即世界迭代器-元理论.md §断言 3
L2 不达标 = KL 散度未修复 = 章节在漂移而非迭代
-->

---


---

## 🎭 欺骗动力学视角：模型表面顺从

> 承接 [`欺骗动力学-社会进步的隐秘引擎.md`](../欺骗动力学-社会进步的隐秘引擎.md) §5。

### 三问

1. **讲透Prompt 防的是什么欺骗？** → Sycophancy（模型假装顺从而实际出错）的最小单位。
2. **被什么攻破？** → prompt 注入 / 越狱 / 上下文污染。
3. **沉淀进哪条主链？** → AI 安全主链——prompt 安全是 LLM 时代反欺骗的第一道门。

### 一句话

> Prompt 工程的反面是 prompt 安全：前者让模型听话，后者防模型被骗、骗人。

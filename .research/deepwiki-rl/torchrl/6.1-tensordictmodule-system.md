# deepwiki torchrl §6.1 tensordictmodule-system
> 来源: https://deepwiki.com/pytorch/rl/6.1-tensordictmodule-system

$/$
$?

/$

DeepWiki

DeepWiki
pytorch/rl

$?

/$

Last indexed:  14 December 2025  (eaaa11)

  - Overview
  - Core Data Infrastructure
  - TensorDict System
  - TensorSpec System
  - Environments
  - EnvBase and Environment Specifications
  - Environment Transforms
  - Backend Integrations
  - Batched and Parallel Environments
  - Data Collection
  - Collector Architecture
  - Distributed Collection Strategies
  - Replay Buffers
  - ReplayBuffer Architecture
  - Advanced Replay Buffer Features
  - Modules and Models
  - TensorDictModule System
  - Actors, Critics, and Value Networks
  - Probability Distributions
  - Neural Network Architectures
  - Exploration Strategies
  - Learning Algorithms
  - Loss Module Architecture
  - On-Policy Algorithms
  - Off-Policy Algorithms
  - Value-Based Algorithms
  - Value Estimators
  - Training Infrastructure
  - Trainer System
  - Helper Utilities
  - LLM Integration
  - LLM Wrapper System
  - LLM Data Structures
  - LLM Training Objectives
  - LLM Environments and Tools
  - Multi-Agent Reinforcement Learning
  - Examples and Tutorials
  - Development and Deployment
  - Build System
  - Documentation System

Menu

## TensorDictModule System

Relevant source files

  - test/test_actors.py

  - test/test_tensordictmodules.py

  - torchrl/modules/tensordict_module/actors.py

  - torchrl/modules/tensordict_module/common.py

  - torchrl/modules/tensordict_module/probabilistic.py

  - torchrl/modules/tensordict_module/rnn.py

  - torchrl/modules/tensordict_module/sequence.py

### Purpose and Scope

The TensorDictModule System provides a standardized interface for wrapping PyTorch `nn.Module` objects to work seamlessly with TensorDict data structures. This system enables composable, type-safe neural network modules where inputs and outputs are accessed via named keys rather than positional arguments. The core abstraction uses `in_keys` and `out_keys` to define module I/O, with optional spec-based validation for enforcing output constraints.

This document covers the core TensorDictModule wrappers (`SafeModule`, `SafeProbabilisticModule`, `SafeSequential`) and their specialized variants for RL use cases (`Actor`, `ValueOperator`, `QValueModule`, RNN modules). For information about the underlying TensorDict data structure, see TensorDict System. For model architectures (MLP, ConvNet), see Neural Network Architectures. For probability distributions, see Probability Distributions.

### Core Concepts

#### TensorDict Integration

TensorDictModules bridge standard PyTorch modules with TensorDict-based data flow. Instead of calling `output = module(input_tensor)`, modules operate on TensorDict objects by reading inputs via `in_keys` and writing outputs via `out_keys`:

```

```

This design enables:

  - Named I/O : Clear, self-documenting interfaces

  - Composability : Modules can be chained without manual key management

  - Batching : Automatic batch dimension handling via TensorDict

  - Functional programming : Support for vmap, functional modules, parameter sharing

Sources: torchrl/modules/tensordict_module/common.py97-192

#### in_keys and out_keys Interface

The fundamental interface for TensorDictModules consists of two attributes:

| Attribute | Type | Description |
| in_keys | List[NestedKey] | Keys to read from input TensorDict and pass to the wrapped module |
| out_keys | List[NestedKey] | Keys to write to the TensorDict with the module's outputs |

NestedKey format supports hierarchical access: `"key"` or `("parent", "child")` for nested TensorDicts.

```

```

Sources: torchrl/modules/tensordict_module/common.py97-192 test/test_tensordictmodules.py79-116

#### Spec-based Safety

TorchRL's `SafeModule` extends `TensorDictModule` with optional spec-based output validation. When `safe=True`, outputs are automatically projected back into the valid domain using `TensorSpec.project()`:

```

```

The spec system supports:

  - Bounded : Continuous ranges with low/high bounds

  - Unbounded : No constraints

  - Composite : Multiple specs for multiple outputs

  - OneHot/Categorical : Discrete action spaces

Sources: torchrl/modules/tensordict_module/common.py97-192 test/test_tensordictmodules.py142-193

### Architecture Overview

#### Class Hierarchy

```

```

Hierarchy Description:

  - TensorDictModuleBase : Foundation class from tensordict library providing in_keys / out_keys interface

  - SafeModule : TorchRL's extension adding spec and safe parameters for output validation

  - Specialized modules : High-level abstractions with RL-specific defaults (e.g., Actor defaults to out_keys=["action"] )

  - RNN modules : Handle recurrent state management with is_init tracking

Sources: torchrl/modules/tensordict_module/common.py97-192 torchrl/modules/tensordict_module/actors.py34-125 torchrl/modules/tensordict_module/probabilistic.py24-183

#### Data Flow Pattern

```

```

Execution Steps:

  - Read : Extract tensors from TensorDict using in_keys

  - Forward : Pass tensors to wrapped nn.Module.forward()

  - Write : Store outputs in TensorDict using out_keys

  - Validate (if safe=True ): Project outputs using spec.project()

Special handling for `"_"` key: outputs mapped to `"_"` are ignored (not written to TensorDict).

Sources: torchrl/modules/tensordict_module/common.py55-94 test/test_tensordictmodules.py104-116

### Core Classes

#### SafeModule

`SafeModule` is the foundational TensorDictModule wrapper with spec-based validation.

Key Parameters:

| Parameter | Type | Description |
| module | nn.Module | The PyTorch module to wrap |
| in_keys | List[NestedKey] | Input keys to read from TensorDict |
| out_keys | List[NestedKey] | Output keys to write to TensorDict |
| spec | TensorSpec | Composite | None | Output domain specification |
| safe | bool | If True, validates and projects outputs (default: False) |
| inplace | bool | str | Whether to modify TensorDict in-place (default: True) |

Spec Handling:

  - Single TensorSpec : Automatically wrapped in Composite with first out_key

  - Composite : Must have keys matching out_keys

  - None : No validation (even if safe=True , raises error)

Multiple Outputs:

```

```

Sources: torchrl/modules/tensordict_module/common.py97-293 test/test_tensordictmodules.py79-103

#### SafeProbabilisticModule

`SafeProbabilisticModule` extends `ProbabilisticTensorDictModule` for stochastic policies with spec-based safety.

Key Additions:

| Parameter | Type | Description |
| distribution_class | Type | Distribution class (e.g., Normal , Categorical ) |
| distribution_kwargs | dict | Additional kwargs for distribution |
| default_interaction_type | InteractionType | How to sample: MODE, MEAN, RANDOM (default: DETERMINISTIC) |
| return_log_prob | bool | Whether to compute and store log-probabilities |
| log_prob_key | NestedKey | Key for storing log-prob (default: "sample_log_prob") |

Interaction Types:

  - DETERMINISTIC / MODE : Use distribution mode

  - MEAN : Use distribution mean

  - RANDOM : Sample from distribution

  - Can be overridden globally via set_exploration_type()

Example:

```

```

Sources: torchrl/modules/tensordict_module/probabilistic.py24-288 test/test_tensordictmodules.py194-269

#### SafeSequential

`SafeSequential` chains multiple TensorDictModules, automatically managing key dependencies.

Features:

  - Aggregates specs from all submodules into a Composite

  - Infers execution order from in_keys / out_keys dependencies

  - Supports partial_tolerant mode for optional keys

  - Implements __getitem__ , __setitem__ , __delitem__ for module access

Subsequence Selection:

```

```

Partial Tolerance:
When `partial_tolerant=True`, modules are only executed if their `in_keys` are present in the TensorDict. Useful for handling `LazyStackedTensorDict` where different stack elements have different keys.

Sources: torchrl/modules/tensordict_module/sequence.py15-133 test/test_tensordictmodules.py271-550

### Specialized RL Modules

#### Actor and ProbabilisticActor

High-level wrappers for RL policies with sensible defaults.

Actor (Deterministic):

```

```

ProbabilisticActor (Stochastic):

```

```

Usage Pattern:

```

```

Composite Actions:
For multi-head policies, use `CompositeDistribution`:

```

```

Sources: torchrl/modules/tensordict_module/actors.py34-393 test/test_actors.py55-168

#### ValueOperator

Wrapper for value functions (V or Q) with automatic key naming.

Key Naming:

  - Input includes "action" → output is "state_action_value" (Q-function)

  - Input excludes "action" → output is "state_value" (V-function)

```

```

Sources: torchrl/modules/tensordict_module/actors.py395-466

#### QValueModule

Specialized module for discrete action selection from Q-values.

Supported Action Spaces:

| Action Space | Description | Output |
| "one_hot" | One-hot encoded actions | [0, 0, 1, 0, 0] |
| "categorical" | Integer action indices | 2 |
| "mult_one_hot" | Multiple one-hot vectors | [1, 0, 1, 0] (2 actions) |
| "binary" | Binary actions | Not implemented |

Outputs:

  - action : Selected action

  - action_value : Input Q-values (unchanged)

  - chosen_action_value : Q-value of selected action

Action Masking:

```

```

Usage:

```

```

Sources: torchrl/modules/tensordict_module/actors.py468-672 test/test_actors.py211-651

### RNN Modules

#### LSTMModule

TensorDict-compatible LSTM wrapper with automatic hidden state management.

Key Features:

  - Automatically reshapes hidden states to match TensorDict batch size

  - Supports both single-step and multi-step (recurrent) execution via set_recurrent_mode()

  - Handles trajectory resets via is_init key

  - Compatible with torch.vmap when using python_based=True

Constructor Arguments:

| Parameter | Description |
| input_size | Input feature dimension |
| hidden_size | Hidden state dimension |
| num_layers | Number of stacked LSTM layers |
| in_key / in_keys | Input key(s): [input, h_0, c_0] |
| out_key / out_keys | Output key(s): [output, ("next", h_0), ("next", c_0)] |
| python_based | If True, uses pure PyTorch implementation (vmap-compatible) |
| default_recurrent_mode | Initial recurrent mode setting |

Default Keys:

```

```

Recurrent Mode:

```

```

TensorDictPrimer Integration:
Use `make_tensordict_primer()` to automatically add hidden state specs to environment:

```

```

Trajectory Resets:
The `is_init` key (automatically added to `in_keys`) signals when to reset hidden states:

```

```

Python-based vs CuDNN:

```

```

Sources: torchrl/modules/tensordict_module/rnn.py311-660 test/test_tensordictmodules.py659-1055

#### GRUModule

Similar to `LSTMModule` but for GRU cells. Identical API except:

  - Single hidden state: in_keys = [input, hidden] , out_keys = [output, ("next", hidden)]

  - Default keys: "recurrent_state" instead of separate h/c

Sources: torchrl/modules/tensordict_module/rnn.py661-1020

### Helper Functions

#### is_tensordict_compatible()

Checks if a module can be used as a TensorDictModule.

Returns:

  - True : Module is already TensorDictModule or has in_keys / out_keys + single-argument forward()

  - False : Module is a regular nn.Module without TensorDict interface

  - Raises TypeError : Ambiguous case (has in_keys / out_keys but multi-argument forward() )

```

```

Sources: torchrl/modules/tensordict_module/common.py295-358 test/test_tensordictmodules.py552-604

#### ensure_tensordict_compatible()

Wraps a module in `TensorDictModule` if needed.

Behavior:

  - If already compatible: Returns module unchanged

  - If regular nn.Module : Wraps in TensorDictModule (or specified wrapper_type )

  - Validation : Checks in_keys match forward() signature

```

```

Sources: torchrl/modules/tensordict_module/common.py361-412 test/test_tensordictmodules.py607-657

#### VmapModule

Wrapper to vmap over a batch dimension, enabling functional programming patterns.

```

```

Sources: torchrl/modules/tensordict_module/common.py415-476

### Usage Patterns

#### Basic Module Wrapping

```

```

#### Chaining Modules

```

```

#### Probabilistic Policy

```

```

#### Actor-Critic Architecture

```

```

Sources: test/test_tensordictmodules.py79-269 test/test_actors.py753-799

### Summary

The TensorDictModule System provides a unified interface for integrating PyTorch modules with TensorDict-based RL workflows:

| Component | Purpose |
| SafeModule | Base wrapper with spec-based validation |
| SafeProbabilisticModule | Stochastic policies with distribution sampling |
| SafeSequential | Chain modules with automatic dependency resolution |
| Actor/ProbabilisticActor | High-level policy abstractions with RL defaults |
| ValueOperator | Value/Q-function wrapper with automatic naming |
| QValueModule | Discrete action selection from Q-values |
| LSTMModule/GRUModule | Recurrent modules with hidden state management |

Key Design Principles:

  - Named I/O : in_keys / out_keys replace positional arguments

  - Composability : Modules chain via shared keys

  - Type Safety : Optional spec-based output validation

  - RL-Optimized : Specialized classes with sensible defaults

  - Functional Support : Compatible with vmap, parameter sharing

Sources: torchrl/modules/tensordict_module/common.py1-530 torchrl/modules/tensordict_module/actors.py1-800 torchrl/modules/tensordict_module/probabilistic.py1-322 torchrl/modules/tensordict_module/sequence.py1-133 torchrl/modules/tensordict_module/rnn.py1-1020



#### On this page

  - TensorDictModule System
  - Purpose and Scope
  - Core Concepts
  - TensorDict Integration
  - in_keys and out_keys Interface
  - Spec-based Safety
  - Architecture Overview
  - Class Hierarchy
  - Data Flow Pattern
  - Core Classes
  - SafeModule
  - SafeProbabilisticModule
  - SafeSequential
  - Specialized RL Modules
  - Actor and ProbabilisticActor
  - ValueOperator
  - QValueModule
  - RNN Modules
  - LSTMModule
  - GRUModule
  - Helper Functions
  - is_tensordict_compatible()
  - ensure_tensordict_compatible()
  - VmapModule
  - Usage Patterns
  - Basic Module Wrapping
  - Chaining Modules
  - Probabilistic Policy
  - Actor-Critic Architecture
  - Summary

$!/$$/$

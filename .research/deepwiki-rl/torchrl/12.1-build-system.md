# deepwiki torchrl §12.1 build-system
> 来源: https://deepwiki.com/pytorch/rl/12.1-build-system

$/$
$?

/$

DeepWiki

DeepWiki
pytorch/rl

$?

/$

Last indexed:  14 December 2025  (eaaa11)

  - Overview
  - Core Data Infrastructure
  - TensorDict System
  - TensorSpec System
  - Environments
  - EnvBase and Environment Specifications
  - Environment Transforms
  - Backend Integrations
  - Batched and Parallel Environments
  - Data Collection
  - Collector Architecture
  - Distributed Collection Strategies
  - Replay Buffers
  - ReplayBuffer Architecture
  - Advanced Replay Buffer Features
  - Modules and Models
  - TensorDictModule System
  - Actors, Critics, and Value Networks
  - Probability Distributions
  - Neural Network Architectures
  - Exploration Strategies
  - Learning Algorithms
  - Loss Module Architecture
  - On-Policy Algorithms
  - Off-Policy Algorithms
  - Value-Based Algorithms
  - Value Estimators
  - Training Infrastructure
  - Trainer System
  - Helper Utilities
  - LLM Integration
  - LLM Wrapper System
  - LLM Data Structures
  - LLM Training Objectives
  - LLM Environments and Tools
  - Multi-Agent Reinforcement Learning
  - Examples and Tutorials
  - Development and Deployment
  - Build System
  - Documentation System

Menu

## Build System

Relevant source files

  - .github/scripts/pre-build-script-win.sh

  - .github/scripts/pre-build-script.sh

  - .github/scripts/td_script.sh

  - .github/scripts/version_script.bat

  - .github/unittest/windows_optdepts/scripts/environment.yml

  - .github/workflows/build-wheels-aarch64-linux.yml

  - .github/workflows/build-wheels-linux.yml

  - .github/workflows/build-wheels-m1.yml

  - .github/workflows/build-wheels-windows.yml

  - setup.py

  - test/smoke_test.py

  - version.txt

This document describes TorchRL's build system, which handles compilation of C++ extensions, wheel generation for multiple platforms, and version management. The build system is centered around `setup.py` for local development and GitHub Actions workflows for CI/CD wheel building across Linux, Windows, macOS M1, and aarch64 architectures.

For information about the documentation build system, see Documentation System.

### Overview

TorchRL's build system consists of three main components:

  - Local Build Configuration : setup.py with platform-specific C++ extension compilation

  - CI/CD Workflows : GitHub Actions workflows for automated wheel building

  - Build Scripts : Shell and batch scripts for environment setup and dependency installation

The build system compiles a single C++ extension module (`torchrl._torchrl`) from sources in `torchrl/csrc/` and packages it with the Python code into distributable wheels for PyPI.

```

```

Sources: setup.py1-112 version.txt1 .github/workflows/build-wheels-linux.yml1-50 .github/workflows/build-wheels-windows.yml1-54 .github/workflows/build-wheels-m1.yml1-51 .github/workflows/build-wheels-aarch64-linux.yml1-53

### setup.py Architecture

The `setup.py` file uses PyTorch's `torch.utils.cpp_extension` infrastructure to build C++ extensions with platform-specific compiler flags.

#### Main Functions

```

```

Sources: setup.py94-111

#### get_extensions() Function

The `get_extensions()` function is responsible for configuring C++ extension builds with appropriate compiler flags for each platform:

```

```

Compiler Flags by Platform:

| Platform | Optimization | Standard | Additional Flags |
| Windows (Release) | /O2 | /std:c++17 | /EHsc (exception handling) |
| Windows (Debug) | /Od | /std:c++17 | /Zi (debug info), /DEBUG (link) |
| Unix (Release) | -O3 | -std=c++17 | -fdiagnostics-color=always |
| Unix (Debug) | -O0 | -std=c++17 | -g , -fno-inline |

Sources: setup.py12-91

#### C++ Extension Configuration

The extension is configured with the following parameters:

  - Name : torchrl._torchrl - the compiled extension is imported as from torchrl import _torchrl

  - Sources : All .cpp files in torchrl/csrc/ directory

  - Include Directories : Current directory ( . ) plus optional PYTHON_INCLUDE_DIR from environment

  - Build Command Class : BuildExtension.with_options() from torch.utils.cpp_extension

The extension sources are discovered dynamically using `glob.glob(os.path.join("torchrl/csrc", "*.cpp"))` setup.py74

Sources: setup.py71-91

### Platform-Specific Wheel Building

TorchRL uses GitHub Actions workflows that invoke PyTorch's centralized `test-infra` workflows for building wheels across multiple platforms. Each platform has a dedicated workflow file.

#### Workflow Trigger Configuration

All wheel building workflows share the same trigger pattern:

```

```

Sources: .github/workflows/build-wheels-linux.yml3-14 .github/workflows/build-wheels-windows.yml3-14 .github/workflows/build-wheels-m1.yml3-14

#### Workflow Architecture

Each platform workflow follows a two-job pattern:

```

```

Sources: .github/workflows/build-wheels-linux.yml20-50

#### Platform-Specific Configurations

| Platform | Workflow File | OS Matrix | Pre-Script | Env-Script | Post-Script |
| Linux x86_64 | build-wheels-linux.yml | os: linux | pre-build-script.sh | td_script.sh | None |
| Linux aarch64 | build-wheels-aarch64-linux.yml | os: linux-aarch64 | pre-build-script.sh | td_script.sh | None |
| Windows | build-wheels-windows.yml | os: windows | pre-build-script-win.sh | version_script.bat | packaging/wheel/relocate.py |
| macOS M1/M2 | build-wheels-m1.yml | os: macos-arm64 | pre-build-script.sh | td_script.sh | None |

Key Configuration Parameters:

  - repository : pytorch/rl

  - package-name : torchrl

  - smoke-test-script : test/smoke_test.py

  - runner-type (M1 only): macos-m2-15

Sources: .github/workflows/build-wheels-linux.yml28-50 .github/workflows/build-wheels-windows.yml28-54 .github/workflows/build-wheels-m1.yml28-51 .github/workflows/build-wheels-aarch64-linux.yml29-53

### Build Scripts

Build scripts handle environment setup, dependency installation, and version configuration.

#### td_script.sh (Linux/macOS)

This script sets up the environment for Unix-based builds:

```

```

Key Environment Variables:

  - TORCHRL_BUILD_VERSION : Set to 0.10.0

  - SMOKE_TEST_SCRIPT : If set, triggers torch installation

  - CONDA_RUN : Prefix for conda environment execution

  - PIP_INSTALL_TORCH : Command to install PyTorch

  - ARCH : Architecture detection (e.g., aarch64 )

Dependencies Installed:

  - setuptools (upgraded)

  - pybind11[global] (if torch is installed)

  - git+https://github.com/pytorch/tensordict.git (from main branch, no deps)

Sources: .github/scripts/td_script.sh1-31

#### version_script.bat (Windows)

This batch script configures the Windows build environment:

```

```

Environment Configuration:

  - Visual Studio detection using vswhere.exe

  - Automatic detection of VS 2017, 2019, or 2022

  - Intel oneAPI setup for XPU builds

  - Fixed setuptools version: 72.1.0

Sources: .github/scripts/version_script.bat1-57

#### Pre-Build Scripts

Two pre-build scripts prepare the environment before compilation:

pre-build-script.sh (Linux/macOS):

```

```

pre-build-script-win.sh (Windows):

```

```

Sources: .github/scripts/pre-build-script.sh1-8 .github/scripts/pre-build-script-win.sh1-6

### Version Management

TorchRL's version is managed through multiple files:

```

```

The version string `0.10.0` is duplicated across:

  - version.txt 1

  - .github/scripts/td_script.sh 3

  - .github/scripts/version_script.bat 2

  - .github/scripts/pre-build-script-win.sh 5

The `TORCHRL_BUILD_VERSION` environment variable is read by the build system to determine the wheel version.

Sources: version.txt1 .github/scripts/td_script.sh3 .github/scripts/version_script.bat2 .github/scripts/pre-build-script-win.sh5

### Smoke Testing

After wheel building, a smoke test verifies basic functionality:

```

```

The smoke test validates:

  - Core data structures (replay buffers, specs)

  - Environment system (transforms, gym-like)

  - Module system (SafeModule)

  - Training objectives (LossModule)

  - Basic instantiation of a prioritized replay buffer

Sources: test/smoke_test.py1-20

### Build Process Flow

The complete build process from source to distributable wheel:

```

```

Sources: setup.py1-112 .github/workflows/build-wheels-linux.yml1-50 test/smoke_test.py1-20

### Local Development Build

For local development, build TorchRL using:

```

```

Debug Build: Set `DEBUG=1` environment variable to compile with debug symbols and no optimization:

```

```

Debug builds use `-O0 -g` (Unix) or `/Od /Zi` (Windows) compiler flags instead of optimization flags setup.py57-69

Sources: setup.py12-91



#### On this page

  - Build System
  - Overview
  - setup.py Architecture
  - Main Functions
  - get_extensions() Function
  - C++ Extension Configuration
  - Platform-Specific Wheel Building
  - Workflow Trigger Configuration
  - Workflow Architecture
  - Platform-Specific Configurations
  - Build Scripts
  - td_script.sh (Linux/macOS)
  - version_script.bat (Windows)
  - Pre-Build Scripts
  - Version Management
  - Smoke Testing
  - Build Process Flow
  - Local Development Build

$!/$$/$

# 06 · 模型差异矩阵：Claude / GPT / Gemini / Llama / Qwen

> **核心论点**：同一个 prompt 在不同模型上**真的**表现不同。这不是玄学，是**训练数据 + 对齐方式 + 架构**的真实差异。
> **本文是什么**：2026-08 一手核实的 5 大主流模型 prompt 偏好对比。

---

## 🎯 主流模型 prompt 偏好对比（2026-08）

| 维度 | Claude（Anthropic）| GPT（OpenAI）| Gemini（Google）| Llama（Meta）| Qwen/DeepSeek |
|---|---|---|---|---|---|
| **结构格式** | **XML 标签**（native）| **Markdown** + 系统 prompt | **编号步骤** | 简洁 | 中文自然 |
| **长上下文** | 200K-1M，**数据在前 + 查询在后**（30% 改进）| Bookend：指令在前 + 后 | 数据在前 + 查询在后 | OK | OK |
| **JSON 输出** | XML 包裹 + prefill `{` | **JSON mode**（最可靠，schema 强制）| Schema 配置 | 需明示 + retry | 需明示 + retry |
| **CoT** | extended thinking（Opus 4/5）| reasoning_effort | thinkingBudget | 普通 CoT | R1 thinking |
| **few-shot** | 3-5 个多样化 | 起 0，按需加 | **必加例子** | **必加例子** | **必加例子** |
| **negative 约束** | 严格遵循（Constitutional AI）| 中等 | 中等 | 弱 | 弱 |
| **系统 prompt 权重** | **高**（视为契约）| 中等（用户可覆盖）| 中等 | 弱 | 弱 |
| **temperature** | 默认即可 | 任务相关 | **保持 1.0**（低会循环）| 任务相关 | 任务相关 |
| **prefill**（预填 assistant）| ✅ native | ❌ | ❌ | 部分 | 部分 |
| ** refusal 率** | 较高（Constitutional AI）| 较低（软处理）| 中等 | 中等 | 较低 |
| **中文友好度** | 好 | 好 | 好 | 中 | **最好** |

---

## 🔍 Claude（Anthropic）详细偏好

### 核心特性
- **XML 标签是 native**：训练时大量用了 XML
- **长上下文（200K-1M）**：long document 是强项
- **Constitutional AI**：内置对齐价值观

### Claude 专属技巧

#### 1. XML 标签（最重要）
```xml
<context>You are helping debug Python code.</context>

<instructions>
1. Identify the bug
2. Suggest a fix with code
3. Explain why the fix works
</instructions>

<code>
{{user_code}}
</code>

<output_format>
Return JSON: {"bug": "...", "fix": "...", "explanation": "..."}
</output_format>
```

#### 2. 长文档：数据在前，查询在后（30% 改进）
```
<documents>
  <document index="1">
    <source>file1.pdf</source>
    <content>{{doc1}}</content>
  </document>
</documents>

<query>
分析以上文档，提取关键事实。
</query>
```

#### 3. Prefill 强制格式
```python
response = claude.messages.create(
    messages=[
        {"role": "user", "content": "Extract facts..."},
        {"role": "assistant", "content": "{"}  # 强制以 { 开始
    ]
)
```

#### 4. Extended Thinking（Opus 4 / Sonnet 4）
```
不需要手动 CoT。直接问：
"分析这段代码的安全问题。输出 JSON。"
模型自己 thinking。
```

#### 5. 正面表述（Constitutional AI 偏好）
- ❌ "不要用 markdown"
- ✅ "用流畅散文"

---

## 🔍 GPT（OpenAI）详细偏好

### 核心特性
- **Markdown 友好**：训练数据中 markdown 占比高
- **JSON mode 最可靠**：schema 强制
- **System prompt + User + Assistant 分离**

### GPT 专属技巧

#### 1. Markdown 结构
```markdown
# Context
You are debugging Python code.

# Task
1. Identify the bug
2. Suggest a fix

# Code
```python
{{user_code}}
```

# Output Format
JSON: {"bug": "...", "fix": "..."}
```

#### 2. JSON Mode（最强）
```python
response = openai.chat.completions.create(
    model="gpt-5",
    response_format={"type": "json_object"},  # 强制 JSON
    messages=[...]
)

# 或用 Structured Output（schema 强制）
from pydantic import BaseModel
class BugReport(BaseModel):
    bug: str
    fix: str

response = openai.chat.completions.create(
    model="gpt-5",
    response_format=BugReport,  # schema 强制
    messages=[...]
)
```

#### 3. Bookend 结构（指令在前 + 后）
```
[System] You are a code reviewer. Focus on bugs and security.

[User]
Review this code:
{{code}}

Remember: only return JSON.
```

#### 4. Reasoning Effort（o1 / o3）
```python
response = openai.chat.completions.create(
    model="o1",
    reasoning_effort="high",  # low / medium / high
    messages=[...]
)
```

---

## 🔍 Gemini（Google）详细偏好

### 核心特性
- **超长上下文**（2M token）
- **多模态优先**（图 / 视频 / 音频原生）
- **temperature 必须 1.0**（低会循环）

### Gemini 专属技巧

#### 1. 编号步骤（最爱）
```
Task:
1. Read the input carefully
2. Identify the main entity
3. Extract facts about that entity
4. Return as JSON

Input: {{input}}
```

#### 2. 多模态
```python
response = gemini.generate_content([
    "What's in this image?",
    PIL.Image.open('photo.jpg')
])
```

#### 3. Thinking Budget
```python
response = gemini.generate_content(
    "...",
    generation_config=GenerationConfig(
        thinkingBudget=8000  # token budget for thinking
    )
)
```

#### 4. Google Search grounding
```python
response = gemini.generate_content(
    "What's the latest news on X?",
    tools=[google_search_tool]
)
```

---

## 🔍 Llama（Meta）详细偏好

### 核心特性
- **开源**：preference 不强
- **prompt 要更具体**
- **few-shot 重要**

### Llama 专属技巧

#### 1. 必加 few-shot
```
Llama 对 zero-shot 不如 GPT/Claude。加 3-5 个例子。

Examples:
Input: ...
Output: ...

Input: ...
Output: ...

Now:
Input: {{real}}
Output:
```

#### 2. 明确格式 + retry
```python
for attempt in range(3):
    response = llama.invoke(prompt)
    try:
        return json.loads(response)
    except json.JSONDecodeError:
        prompt += "\nReturn ONLY valid JSON, no other text."
```

#### 3. 系统提示前置
```python
# Llama 系统提示权重弱，重要约束两边都写
system = "You are X. Always return JSON."
user = f"Do Y. Return JSON only. {input}"
```

---

## 🔍 Qwen / DeepSeek（中文友好）详细偏好

### 核心特性
- **中文 prompt 最自然**
- **DeepSeek R1 推理强**（GRPO + RLVR）
- **价格便宜**

### Qwen/DeepSeek 专属技巧

#### 1. 中文任务用中文 prompt
```
# 比 GPT/Claude，Qwen 处理中文 prompt 更稳
你是一个 Python 代码审查专家。
任务：找出代码中的 bug。
输出 JSON: {"bug": "...", "fix": "..."}
```

#### 2. DeepSeek R1 thinking
```python
# R1 自带 thinking，少 prompt
response = deepseek.invoke("Solve this math problem. Output JSON.")
# R1 会自动 thinking，不用 "Let's think step by step"
```

---

## 📊 5 模型在不同任务上的推荐（2026-08）

| 任务 | 首选 | 备选 |
|---|---|---|
| 长文档分析（>200K）| **Claude Opus 4** | Gemini 2.5 Pro |
| 多步推理（数学 / 代码）| **o1 / Claude Opus 4** | DeepSeek R1 |
| 严格 JSON 输出 | **GPT-5 JSON mode** | Claude prefill |
| 多模态（图 / 视频）| **Gemini 2.5 Pro** | GPT-5 Vision |
| 中文任务 | **Qwen / DeepSeek** | Claude |
| 开源部署 | **Llama 3.3 70B** | Qwen 2.5 |
| 便宜生产 | **DeepSeek V3** | Llama 3.3 |
| Agent / Tool calling | **Claude Sonnet 4** | GPT-5 |

---

## ⚠️ 跨模型的 5 个真实陷阱

### 1. JSON 不可靠（除 GPT 外）
**问题**：除 GPT 外，"输出 JSON" 没强制。
**对策**：用 schema enforcement（OpenAI structured output / Claude prefill / Gemini schema）。

### 2. 温度不一致
**问题**：GPT-5 temp=0.7 ≠ Claude temp=0.7。
**对策**：各自标定。生产推荐 temp=0.0-0.3。

### 3. few-shot 顺序敏感
**问题**：Llama 对顺序敏感（recency bias），GPT 不敏感。
**对策**：随机化 + 跑多次。

### 4. 指令位置
**问题**：Claude 指令放后，GPT 放前。
**对策**：**不要直接复制 prompt**，按模型调整。

### 5. system prompt 优先级
**问题**：Claude 系统 prompt > 用户；GPT 用户 > 系统。
**对策**：安全约束两边都写。

---

## 📌 本周必做

1. [ ] 用 Promptfoo 跑同一 prompt 跨 3 个模型，看差异
2. [ ] 给你最常用的 prompt 写 **Claude 版**（XML）和 **GPT 版**（Markdown）
3. [ ] 测试 **Gemini temp=1.0 vs temp=0.7**，看循环问题

---

## 📚 推荐深读

- **Anthropic Prompt Engineering**: `docs.anthropic.com/en/docs/build-with-claude/prompt-engineering`
- **OpenAI Prompt Engineering Guide**: `platform.openai.com/docs/guides/prompt-engineering`
- **Google Gemini Prompting Strategies**: `ai.google.dev/gemini-api/docs/prompting-strategies`
- **Meta Llama Prompt Format**: `llama.com/docs/model-cards-and-prompt-formats/llama3`

---

**版本**：v1.0（2026-08-13）
**核心隐喻**：**Claude 像 XML 党，GPT 像 Markdown 党，Gemini 像编号党。用对"方言" = 一半工作。**

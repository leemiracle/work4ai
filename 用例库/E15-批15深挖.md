# 扩展分册 E15 · 批 15 深挖（10 仓，DeepWiki+keyfiles 双源）

> 仓库：transitive-bullshit/agentic, google/magika, TransformerOptimus/SuperAGI, AsyncFuncAI/deepwiki-open, Infrasys-AI/AISystem, 1jehuang/jcode, leon-ai/leon, dair-ai/ML-YouTube-Courses, rowboatlabs/rowboat, xx025/carrot

### transitive-bullshit/agentic
- **DeepWiki**：30 章；Overview 双系统全景（stdlib 旧架构 + Agentic Platform 新市场）
- **架构模式**：pnpm monorepo 双演化路径共存：legacy `stdlib/`（40+ AI 工具 TS 标准库，`@aiFunction` 装饰器 + `Msg` 接口 stdlib/core/src/message.ts:8-51，DW 2.1/2.2）→ 新平台"The App Store for LLM Tools"（Next.js 15 + Hono OpenAPI 后端 + Cloudflare Workers MCP Gateway + PostgreSQL/Drizzle + Stripe 计费，DW 3.1-3.2）。**2026-02 已归档**：README 头部 IMPORTANT 自述"failed to gain traction"（readme L1-2）。
- **核心数据结构**：错误层次 `BaseError→HttpError→{RateLimitError(429+getRateLimitHeaders 注入限流响应头), JsonRpcError(jsonRpcErrorCode+jsonRpcId), ZodValidationError(zod-validation-error 包装)}`（本地 packages__platform-core__src__errors.ts:6-108）；API 面约 50 个 `registerV1*` 路由覆盖 auth/consumers/deployments/projects/teams/users/storage/Stripe webhook（本地 apps__api__src__api-v1__index.ts:8-53），public/private 双路由器 + Bearer JWT securitySchemes（L58-72）。
- **核心算法/协议**：路由注册顺序敏感设计——通配符路由必须前置，四处行内注释显式标注顺序约束（api-v1/index.ts:55-56,104,110,115,128）；消费者计费流：create-consumer→checkout-session→Stripe webhook→billing-portal-session；MCP Gateway 在边缘统一处理 auth/billing/rate-limiting/caching（DW 3.2, publishing/page.tsx:54-57）。
- **设计模式与工程决策**：① 归档自评三大遗产：HTTP↔MCP 兼容层、TS stdlib of AI functions、跨所有主流 LLM SDK 兼容（readme L2）；② "quality, not quantity"人工策展 + 持续集成测试（readme L19/28）；③ MCP 一等公民原语而非附加层（readme L30）。
- **教学映射**：透视Agent系统工程（工具市场/网关计费位面）+ 软件即熵治理（商业冷启动失败标本）
- **一句话本质**：给 LLM 工具做 App Store 未遂的归档化石，证明策展+计费网关救不了双边市场冷启动。

### google/magika
- **DeepWiki**：17 章；Overview（含四语言实现矩阵与 Content Type System/Models and Versioning 章节）
- **架构模式**：单模型多语言绑定：Rust CLI+lib 为核心推理实现，Python/JS(TS)/Go 共享同一份模型资产（assets/models/standard_v3_3，DW 2.2 Model Synchronization）；~100M 样本 200+ 内容类型训练、~99% 精度、模型仅数 MB、CPU 单核 ~5ms/文件、近常数推理时间（只读文件有限子集，README L15-39）。
- **核心数据结构**：Python 公共导出面九类型：`ContentTypeInfo/ContentTypeLabel/MagikaError/MagikaPrediction/MagikaResult/OverwriteReason/PredictionMode/Status`（本地 python__src__magika____init__.py:20-46）；per-content-type threshold 系统 + 三档 `PredictionMode{high-confidence, medium-confidence, best-guess}` 控制容错（README L38-39）。
- **核心算法/协议**：按内容而非扩展名/magic bytes 检测；Go 绑定管线 `NewScanner(assetsDir, modelName)→Scan(reader, len)→ct.Label`，资产目录与模型名经 `MAGIKA_ASSETS_DIR/MAGIKA_MODEL` 环境变量外置注入（本地 go__cli__cli.go:12-32, 42-47）——模型是数据文件不是代码。
- **设计模式与工程决策**：① Google 生产规模背书：Gmail/Drive/Safe Browsing 每周数百亿样本路由到安全扫描器，外溢集成 VirusTotal 与 abuse.ch（README L23）；② ICSE 2025 论文 + web demo 纯浏览器本地推理；③ "trusted/unknown"双出口设计：低置信度回落 Generic 标签而非硬猜。
- **教学映射**：世界模型（把文件类型判断这个 Unix file(1) 问题神经化）+ 多模态边缘工程位面
- **一句话本质**：用几 MB 的 1D-CNN 替换 file(1) 的 magic 表，在文本类内容识别上取得代际优势。

### TransformerOptimus/SuperAGI
- **DeepWiki**：24 章（索引冻结于 2025-04-18——项目已停更）；Overview + Agent Execution Flow
- **架构模式**：FastAPI + Celery 异步执行 + PostgreSQL/SQLAlchemy + Redis TaskQueue + Next.js GUI；双层工作流状态机：AgentWorkflowStep（外层步骤图）×IterationWorkflowStep（内层迭代），执行状态持久化为 DB 行而非内存循环。
- **核心数据结构**：`AgentExecution{status: RUNNING/WAITING_FOR_PERMISSION/COMPLETED, permission_id, current_agent_step_id, iteration_workflow_step_id}`；`AgentExecutionFeed{feed, role, feed_group_id}` 为对话持久层（本地 agent_iteration_step_handler.py:201-210）；main.py 挂载 29 个 router 覆盖 org/project/budget/toolkit/vector_db/marketplace（本地 main.py:109-137）——平台化全家桶。
- **核心算法/协议**：execute_step 八拍（agent_iteration_step_handler.py:46-121）：权限恢复检查→prompt 变量替换（goal/instruction/constraints/tools + TaskQueue 变量）→消息构建→TokenCounter 差额预算调用 LLM→JSON 解析 tool 名→output_handler.handle→状态迁移（COMPLETE/WAITING_FOR_PERMISSION/next_step_id 递进，L101-118）。HITL：APPROVED 则重放工具、拒绝则把用户反馈写成 user feed 回注对话（L184-218）。
- **核心算法（记忆）**：LTM 递归摘要——`_split_history` 按 3:1 切 past/current（token 预算 3/4 给近期，agent_message_builder.py:44-45），past 交给"你 are GPT Prompt writer"摘要；摘要超预算时用 previous_ltm_summary + 增量消息做递归再摘要（agent_recursive_summary.txt，L91-134）——2023 年早期 agent 记忆压缩的代表实现。
- **设计模式与工程决策**：① ThinkingTool 恒注入工具表首位（L154）；② ResourceSummarizer 懒创建资源摘要，存在才解锁 QueryResourceTool（L159-161）；③ model_api_key 按模型从 DB 配置取，配合 llm_model_factory 多 LLM 工厂（DW 6.2）。
- **教学映射**：记忆（递归 LTM 摘要历史标本）+ 人机协同（WAITING_FOR_PERMISSION 三态审批机）
- **一句话本质**：把 AutoGPT 式自由循环装进数据库状态机，用工作流步骤表驯服 autonomous agent。

### AsyncFuncAI/deepwiki-open
- **DeepWiki**：27 章；深读 3.2 RAG Engine & Vector Retrieval
- **架构模式**：Next.js 前端(3000) + FastAPI 后端(8001) 双进程解耦（api/main.py:50-54）；AdaFlow 组件框架 + FAISS 向量检索；WikiTask 异步任务 + SSE 进度流（submitWikiTask/subscribeWikiTask，src/app/[owner]/[repo]/page.tsx:165-170）；数据持久化 docker volume `~/.adalflow`。
- **核心数据结构**：`RAG` class 一仓一实例（provider+model+FAISSRetriever，DW 3.2）；`DialogTurn{id, UserQuery, AssistantResponse}` + `CustomConversation(list[DialogTurn])`——注释明言为修复 adalflow 原生 Conversation 的 list assignment index error（本地 api__rag__rag.py:75-85）；`MAX_EMBEDDING_TOKENS=8192`（pipeline.py:21）。
- **核心算法/协议**：acall() 五步（memory→retriever→prompt render→generator→add_dialog_turn，DW 3.2 rag.py:313-325）；`_RAG_PREPARE_SEMAPHORE = asyncio.Semaphore(DEEPWIKI_MAX_CONCURRENT_RAG=4)` 并发闸门（本地 rag.py:30-34）；检索前 embedding 维度校验 + 7500 token 过滤；分 provider 计数策略：openai 用 text-embedding-3-small encoding、google/ollama/bedrock 全部用 cl100k_base 粗估、tiktoken 失败退 `len(text)//4`（pipeline.py:51-70）三级精度降级。Ask 三模式：Fast 直连 / Deep Research ≤5 轮 agentic loop / Codemap 两阶段 skeleton+enrichment（DW Overview, Ask.tsx:35-41）。
- **设计模式与工程决策**：① repo 索引以 `.pkl` 存在性为闸，缺失抛 RepoNotIndexedError（DW 3.2）；② 10 语言 i18n + 10 embedding providers + 8 LLM providers 全可换；③ 会话记忆从 ChatCompletionRequest.messages 手工重放重建（research.py:107-116）。
- **教学映射**：RAG（生产级工程细节百科：并发闸/维度校验/token 降级）+ 透视Agent系统工程（文档生成为任务流）
- **一句话本质**：用几百行 RAG 把任意 git 仓变成可问答 wiki 的 DeepWiki 自助复刻。

### Infrasys-AI/AISystem（内容考古卡）
- **DeepWiki**：22 章（1-Overview + 硬件/编译/推理/框架四模块树全索引）；keyfiles 空
- **分类法**：目录即课程本体，五模块自底向上"全栈逆向"：01Introduction（AI 系统全栈概述）→02Hardware 七子目录（计算体系→芯片基础→GPU→NVIDIA Tensor Core/NVLink 详解→国外 TPU/特斯拉→国内寒武纪/燧原→黄金十年思考）（readme L51-57）→03Compiler（传统 GCC/LLVM→AI 编译器→前端算子融合→后端 AutoTuning→PyTorch2.0 编译栈，多面体"待更 ing"）（L59-68）→04Inference（推理系统→MobileNet 轻量网→压缩 4 件套量化蒸馏剪枝二值化→转换→Kernel 优化）（L70-78）→05Framework（框架基础→自动微分→计算图→分布式训练）（L80-86）。
- **结构证据**：三载体并行：md 文字（infrasys-ai.github.io/aisystem-docs）+ PDF/PPT 开源 + B站/YouTube 双平台视频（readme L7-9）；仓库体积 10GB——README 备注明示"git clone 会非常慢，请走 Releases 下载"（L94），制作过程与高清原图全量入仓，仓库即出版数据库。
- **工程决策**：大模型内容整体分流出走姊妹仓 AIFoundation（L3）——知识域溢出触发仓分裂；受众定位本科高年级/硕博/从业者三级（DW Purpose）。
- **教学映射**：透视Agent系统工程（推理引擎/分布式训练底层课）+ 软件即熵治理（域溢出→新仓演化）
- **一句话本质**：一份从晶体管讲到分布式训练的中文 AI 系统全栈活教材，拿 git 仓当出版流水线。

### 1jehuang/jcode
- **DeepWiki**：17 大章 60+ 小节；深读 6.1 Compaction Manager
- **架构模式**：纯 Rust Cargo workspace 多 crate 分层（jcode-agent-runtime/jcode-embedding(本地 ONNX)/jcode-tui/jcode-harness-api(稳定外接 API)/jcode-desktop2(wgpu GPU 渲染)/jcode-swarm-core，DW Overview Cargo.toml:8-93）；server-client 模型：Unix socket 常驻 server 管会话，TUI/CLI/desktop/iOS 全是客户端；idle 客户端 RAM 27.8MB（readme RAM 对比表 L79-90，自称最省内存 harness）。
- **核心数据结构**：`Agent{session, provider, registry, cache_tracker, locked_tools, provider_session_id}`（agent.rs 模块群，compaction.rs:4-9 揭示字段联动）；`SoftInterruptQueue` 软中断队列（agent.rs:50-53）；`CompactionEvent{tokens_saved, duration_ms}`（DW 6.1 turn_loops.rs:132-142）；消息哈希只取 cache-relevant projection——注释明言原始 Message 含 timestamp/ReasoningTrace 等不传输元数据，直接哈希会触发伪 `harness:_prefix_changed` KV-cache miss（agent.rs:89-96）。
- **核心算法/协议**：**四模式 compaction**（DW 6.1, conversation_state.rs:29-39）：Reactive（超阈值触发）/Proactive（EWMA 预测 token 增长提前压缩）/Semantic（话题漂移触发）/Hard（413 应急丢弃）；上下文限错误恢复链（compaction.rs:110-130）：OpenAI encrypted_content 超限→payload_too_large 先剥内联 base64 图（注释：token 记账故意低估图片，普通 compaction 缩不掉字节级 413）→`is_context_limit_error` 对 11 种错误字符串做模式匹配（L90-104）→同步 hard compact 后立即可重试；compaction 应用即 `cache_tracker.reset()+locked_tools=None`（KV 前缀失效必须联动，compaction.rs:4-9）。
- **设计模式与工程决策**：① selfdev 自举：`JCODE_NATIVE_TOOLS=["selfdev","communicate"]`，agent 可重建/测试/热替换自身二进制（agent.rs:55, DW 11）；② swarm 多 agent 同仓协作防文件冲突；③ byte 级 413 与 token 级溢出双通道诊断分离；④ 与 OpenAI 原生加密 compaction blob 互操作、失败回落 complete_simple 手工摘要（DW 6.1）。
- **教学映射**：记忆（compaction 四模式 + KV cache 失效联动）+ 透视Agent系统工程（Rust harness 性能工程标杆）
- **一句话本质**：把上下文工程做成带预测与自愈的内存管理器，顺手教会 agent 重编自己。

### leon-ai/leon
- **DeepWiki**：26 章（索引 2025-09）；Overview + Voice Processing
- **架构模式**：1.x 四层：React/Vite 前端（Socket.io）+ Node.js Brain 编排（HTTP+Socket 双 server）+ Python TCP Server 独立进程（ASR/TTS/spaCy NLP，编译为二进制 PYTHON_TCP_SERVER_BIN_PATH 分发）+ 双语言 skill 桥（Node/Python bridge）。**2.0 Developer Preview（develop 分支）整体转向 agentic**：README 明言"no longer just a classic intent-classification assistant like its first release in 2019"（readme L54），分层链 `Skills→Actions→Tools→Functions(→Binaries)`（L64），smart/controlled/agent 三运行模式（L58）。
- **核心数据结构**：三 NLP 模型路径常量：MAIN_NLP_MODEL_PATH/GLOBAL_RESOLVERS/SKILLS_RESOLVERS（constants.ts:232-245，DW 2.1）；LLM 环境变量矩阵 `LEON_LLM/LEON_LLM_PROVIDER/LEON_LLM_NLG/LEON_LLM_ACTION_RECOGNITION`（constants.ts:247-339）；本地 LLM 走 node-llama-cpp，门槛 LLM_MINIMUM_FREE_VRAM=8GB。
- **核心算法/协议**：ASR 供者-解析器映射 `PROVIDERS_MAP` 目前只启用 Local（Faster Whisper），Google/Watson/Coqui 三行全被注释（本地 asr-provider.ts:11-16）——语音云化路线回退纯本地；转写确认式清理：transcribe→socket emit('recognized')→等客户端 ack 'string-received' 才 deleteAudios（L99-151），音频文件零残留。
- **设计模式与工程决策**：① master（稳定 legacy）/develop（2.0 preview）双分支长期共存，README 顶部置顶迁移告示（L31-44）；② 分层记忆 durable preferences/day-to-day/recent context（L62）；③ 有界 proactive pulse 系统防上下文自我淹没（L66）。
- **教学映射**：多模态（离线语音全管线）+ 记忆（分层记忆 + 模型资产本地化）
- **一句话本质**：一个用七年从 intent 分类器逐步进化成 agent 框架的开源助手活体演化标本。

### dair-ai/ML-YouTube-Courses（内容考古卡）
- **DeepWiki**：DW:FAIL（页面仅 Loading 骨架，主页面与 /1-overview 两试皆空）；keyfiles 空——readme 全量 895 行为唯一源
- **分类法**：~60 门课程按 12 域分层：Machine Learning（Caltech CS156/Stanford CS229/Tübingen 双课…）→Deep Learning（Karpathy Zero to Hero/CS230/CMU 11-785/CS182/NYU SP21/Foundation Models…）→Scientific ML（Parallel Computing + SciML）→Practical ML（LLMOps/Prompt Engineering/LangChain 短课族/Full Stack LLM Bootcamp/MLOps/DCAI）→NLP（CS224N/CS25 Transformers United/CMU 三版 Advanced NLP…）→CV→RL（DeepMind×UCL/CS234/Abbeel Foundations）→Graph ML（Stanford/AMMI Geometric DL）→Multi-Task & Meta（CS330）→Others（生命科学/自动驾驶/伯克利机器人）（readme L5-92 锚点目录即索引）。
- **条目格式**：课程简介 + 主题 bullet 清单 + 🔗 playlist 链接；部分含双链接（CS230 同时给 playlist+syllabus 材料页 L187）；MIT DCAI 给 Course Website/Videos/Lab Assignments 三轨（L290-294）；CMU Advanced NLP 保留 2021/2022/2024 三版链接并置（L519-523）——版本层积不删旧版。
- **工程决策**：维护者（elvis/dair-ai）明言规划"summarize the lectures, include notes, additional reading, difficulty"但注明"It will take a bit of time"未实施（L893）——策展债显式挂账；课程笔记已分流姊妹仓 ML-Course-Notes（L895）。
- **教学映射**：本体即"课程元课程"——学习路径规划与课程知识图谱的一手语料
- **一句话本质**：一份把 YouTube 免费课程按域分层的巨型书目，公开欠着摘要债。

### rowboatlabs/rowboat
- **DeepWiki**：10 章 50+ 节；深读 3.1 Knowledge Graph
- **架构模式**：一仓三形态：Desktop（Rowboat X，Electron，local-first Obsidian 兼容 vault）+ Web（多租户 agent workflow 平台：docker-compose 编排 rowboat/rag-worker/jobs-worker/Qdrant/Firecrawl，DW Overview）+ CLI（rowboatx，Hono HTTP server）；YC S24。核心哲学 local-first：数据=本机纯 Markdown、无锁定（README L146-152）。
- **核心数据结构**：知识图谱即文件系统本体：`~/.rowboat/knowledge/{People,Organizations,Projects,Topics,Meetings,Voice Memos,Agent Notes}/` + `config/{tags.json,notes.json,note_creation.json}` + `knowledge_graph_state.json`（DW 3.1 build_graph.ts:27-46）；`AgentScheduleEntry{type: cron/window/once, enabled}`；run 日志为 jsonl RunEvent 流。
- **核心算法/协议**：build_graph 15 秒后台循环四步（DW 3.1）：① mtime+content hash 双校验变更检测省 token；② knowledge_index 实体消歧——每次 agent 运行前重建内存索引、formatIndexForPrompt 注入全部现存 people/aliases/orgs 防重复建卡；③ note_creation agent 双规则：Email Reply Gate（用户未回复过的邮件线程不许新建 People/Org 卡）+ Batch Isolation（关系仅可由单一源文件内的证据支撑，防批量共现伪造关系）；④ noise tag 过滤跳过构建。调度器：cron-parser 算 nextRunAt，window 类型在时间窗内随机取分钟（runner.ts:69-97 防整点惊群）+ interruptibleSleep/wakeResolve 立即唤醒触发（L24-46）。
- **设计模式与工程决策**：① schema-on-write：实体模板 seed 进 config/notes.json 而非运行时推断；② CLI 主入口 `app()` 第一行即 `throw new Error("Not implemented")` 后接整段注释掉的实现（本地 apps__cli__src__app.ts:44-70）——CLI 层已化石化但 jsonl run 事件溯源设计保留；③ 力导向图可视化自写斥力+弹簧模拟（graph-view.tsx:36-42）。
- **教学映射**：记忆（知识图谱=外置结构化长期记忆）+ 世界模型（把日常工作坍缩为可检查的实体关系）
- **一句话本质**：把 Obsidian 仓库当数据库、把"你回没回过邮件"当外键约束的本地优先 AI 同事。

### xx025/carrot（内容考古卡）
- **DeepWiki**：9 章（2 分类系统/3 工具类目/4 导航发现）；全部内容=单 README 1934 行；keyfiles 空
- **分类法**：11 个一级类目：推荐位（已注释化）+ ChatGPT 镜像站 + 大模型生态 + 创意 AI + 生产力/开发工具 + 导航发现（DW Overview, README L15-1923）；条目五元组表格：序号/favicon/工具名+超链接/一句话描述/可达性标签/🔗（DW Tool Entry Format）；标签体系=免费/免登录/国内可访问等"网络环境可达性"众包信号（README L52-55）。
- **结构证据**：README 为数据源，发布站 cc.ai55.cc 为渲染出口（readme L8）；失效反馈与投稿全走 GitHub issues（L10）；文末更新时间戳标记数据新鲜度（L1929-1932，DW Repository Maintenance）；本地 readme 头部存在大量 `[//]: #` 注释掉的推荐表格（L15-70）——被下架条目以注释墓碑形式留存而非删除。
- **工程决策**：镜像站高死亡率倒逼"注释考古学"（保留历史而非破坏性删除）；条目 favicon 走自建缓存域名 st2.ai55.cc（readme L28）。
- **教学映射**：软件即熵治理（清单维护=对抗链接腐烂的持续战）+ 群体智能（可达性众包监测网络）
- **一句话本质**：一份用注释墓碑对抗链接腐烂的免费 GPT 镜像存活地图。

## 组内横断发现

1. **三种"死法"标本同批出现**：agentic 归档时自述失败原因（readme L1-2）、SuperAGI 被 DeepWiki 冻结在 2025-04 停更快照、rowboat CLI 入口直接 `throw new Error("Not implemented")`（app.ts:44）——开源 AI 项目衰变的完整光谱（体面退场/无声停更/局部化石化）本身就是教学材料。
2. **记忆压缩的三年代际差**：SuperAGI 2023 年用"LLM 递归摘要注入 assistant 消息"（agent_message_builder.py:91-134）vs jcode 2026 年用四模式 compaction + KV-cache 前缀失效联动 + 字节/token 双通道错误分离（compaction.rs:90-130）——同一问题从 prompt 技巧演进为操作系统级内存管理。
3. **"文件系统当数据库"共识**：rowboat 知识图谱=Obsidian 目录本体（DW 3.1）、magika 模型=外置资产文件（cli.go:12-15）、leon 2.0 上下文=Markdown——三者不约而同绕过数据库，把数据/模型/记忆落为纯文件，换取可检查、可迁移、无锁定。
4. **清单类仓库的 README 即数据库 + 注释墓碑模式**：carrot 1934 行/ML-YouTube-Courses 895 行/AISystem 10GB 目录树，共性是单载体承载数据本体；对抗熵增的两板斧=条目注释化留存（carrot L15-70）与姊妹仓分流（ML-Course-Notes、AIFoundation）。
5. **中文在地化生态位**：AISystem（中文全栈系统课 + 国内芯片专章 02Hardware/06Domestic）与 carrot（"国内可访问"可达性标签）共同构成中文网络环境特有的 AI 资源在地化层——可达性本身成为一级数据维度。

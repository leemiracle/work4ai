# 端侧 AI 系统架构参考

> 基于对**主流端侧 AI 系统**（移动设备全套 AI 能力）的架构调研，提炼出端侧 AI 的**功能域分类 + 部署工程模式 + memory/搜索架构 + 可迁移性分析**。
>
> ⚠️ **完全脱敏声明**：本文档**不指向任何具体厂商、产品、SDK、类名、模型文件名、内部代号或逆向证据**。所有内容为**通用端侧 AI 架构概念**，可公开传播。涉及的模型架构（CRNN/DBNet/YOLO/MobileNet/FastText 等）均为**公开学术模型**，非任何厂商专有。

---

## 一、端侧 AI 系统的全景

现代移动设备的端侧 AI 系统通常覆盖 **11 大功能域**，从感知（视觉/语音）到认知（NLU/memory）。一个完整的端侧 AI 系统往往有 **上百个模型文件、数百 MB**，分布在多个系统应用中：

| 功能域 | 代表能力 | 典型公开模型架构 |
|---|---|---|
| **OCR / 文档** | 文字检测 + 识别、文档边界检测、聊天头像打码 | DBNet（检测）+ CRNN（识别）|
| **目标检测** | 多类物体检测、二维码、场景物体 | YOLO 系列 |
| **场景分类** | 室内外 / 物体 / 色彩分类 | MobileNet 系列 |
| **人脸 / 人体** | 人脸特征、人体骨架、表情 | 专用轻量网络 |
| **图像描述生成** | 图像 → 自然语言（无障碍 / 相册摘要）| ViT-based caption |
| **抠图 / 显著性** | 人像抠图、智能裁剪 | SegNeXt / U²-Net 类 |
| **NLP / NER** | 实体识别、SMS 场景分类 | FastText / 轻量 NER |
| **语音 / 唤醒 / 声纹** | 唤醒词、声纹识别、ASR | 加密专有（通常不可迁移）|
| **行为画像** | App 使用行为向量化 | 轻量时序模型 |
| **词典 / Trie / AC** | 分词词典、实体词典、地址 / 省份匹配 | 双数组 Trie / AC 自动机 |
| **知识库 / 数据库** | NLU 模式库、SMS 模板、卡片正则 | SQLite + 规则库 |

> 🎯 **核心观察**：端侧 AI 不是"一个大模型"，而是**上百个专业小模型的集合**——这是它与云端 LLM 的根本差异。端侧追求**低延迟 + 隐私 + 离线**，用模型专业化换效率。

---

## 二、端侧部署的 5 大通用工程模式

### 模式 1：私有模型格式 + 专用加载器

端侧 AI 系统通常有**自研的私有模型格式**（而非直接用通用 ONNX/TFLite）+ 配套的专用 native 推理引擎。原因：
- **芯片深度优化**（针对自家 NPU/DSP 的算子融合）
- **模型加密防提取**（IP 保护）
- **体积压缩**（fp16/int8 量化固化在格式里）

**通用形态**：私有格式模型文件 + native 推理引擎（动态库）。

### 模式 2：量化 + 芯片专用编译

同一模型常有多个版本并存：
- **fp16 版**（通用，精度高）
- **int8 量化版**（体积小、推理快）
- **芯片专用编译版**（针对特定代芯片的指令集优化）

> 同一功能可能因芯片代际不同有多个编译版本——这是端侧硬件碎片化的必然代价。

### 模式 3：本地 + 云端混合

能力分层：
- **本地（端侧）**：低延迟、隐私敏感、高频（OCR / 唤醒 / 检测）
- **云端**：大模型能力、复杂推理（图像描述生成 / 对话）

通常采用"本地优先 + 云端 fallback"策略——能在端上做的绝不走云。

### 模式 4：多供应商 SDK 集成

端侧 AI 系统常集成多个**专业供应商 SDK**：
- 视觉能力（人脸 / 人体）—— 通常来自专业视觉厂商
- 语音能力（唤醒 / ASR / 声纹）—— 通常来自专业语音厂商
- 向量检索引擎 —— 可能基于开源项目（如 FAISS 类）的定制 fork

这些 SDK 各自加密保护，**通常不可跨设备迁移**。

### 模式 5：加密 + 云端引擎（不可迁移类）

某些能力（尤其语音唤醒 / 声纹）采用**加密模型格式 + 云端引擎**——模型文件即使被提取出来也无法独立运行（解密 key 在云端或硬件安全区）。这是供应商保护 IP 的手段，也是"不可迁移"的根本原因。

---

## 三、端侧 memory + 搜索系统的通用架构

完整的端侧 AI 系统通常有**多层 memory + 多种搜索**——这是"个性化"的根基。

### 3.1 memory 的 5 个层次

| memory 层 | 内容 | 典型实现 |
|---|---|---|
| **行为画像** | App 使用行为向量化 | 轻量时序模型（行为序列 → 特征向量）|
| **多模态实体 RAG** | 实体（人 / 地 / 物）的向量索引 | 向量检索引擎 + 多模态编码器 |
| **对话历史** | 多轮对话上下文 | SQLite（对话表 + 多模态索引表）|
| **生活场景** | 停车 / 出行 / 日程等结构化记忆 | SQLite 结构化表 |
| **长期知识** | 分词词典 / 实体词典 / 规则库 | Trie / AC 自动机 / SQLite |

### 3.2 搜索的 6 种范式

| 搜索类型 | 用途 | 技术 |
|---|---|---|
| **NLU 意图搜索** | 用户输入 → 意图 | tag_pattern 模式匹配（数万级模式库）|
| **Agent 意图搜索** | Agent 任务 → 子意图 | 意图解析管理器 |
| **本地 + 网页搜索** | 信息检索 | 搜索处理器 + 网页检索 |
| **全局桌面搜索** | 应用 / 文件 / 设置 | 快速搜索索引 |
| **图文跨模态搜索** | 图文互检 | CLIP 类编码 + 向量检索 |
| **Trie 前缀搜索** | 输入法 / 补全 | 双数组 Trie |

### 3.3 向量检索引擎（端侧 RAG 的核心）

端侧 RAG 通常基于**轻量向量检索引擎**（开源 FAISS 类的定制 fork 或自研）：

- **索引类型**：IndexFlat（精确检索）+ 内积 / 余弦度量
- **支持操作**：写入 / 删除 / 按属性过滤 / 按表达式过滤
- **核心算法**：L2 归一化 + 内积 KNN + 堆结果处理（HeapResultHandler）

> 这是端侧"多模态实体 RAG"的根基——把人 / 地 / 物编码成向量，检索时按内积排序召回。

---

## 四、可迁移性分析（通用维度）

端侧 AI 系统的各部分，可迁移性差异极大。按"公开化程度"排序：

| 类别 | 可迁移性 | 迁移方式 |
|---|---|---|
| **词典 / 规则库**（Trie / AC / SQLite）| ✅ 1:1 可迁移 | 公开格式直接读（双数组 Trie / SQLite 标准库）|
| **NLP**（分词 / FastText）| ✅ 开源等价 | 用开源分词 + FastText 重新训练 |
| **视觉**（分类 / 检测 / OCR）| 🟡 架构等价 | 用公开模型架构（YOLO / CRNN / MobileNet）重新训练 |
| **图像描述**（caption）| 🟡 架构等价 | 用 ViT-caption 开源等价模型 |
| **私有格式模型** | 🟡 需加载器 | 需专用加载器，或用开源模型等价替代 |
| **加密 + 云端**（语音 / 声纹）| 🔴 不可迁移 | 加密 + 云端引擎，提取也无法独立运行 |

**核心规律**：**越公开（开源架构 + 公开格式 + 静态数据）越可迁移；越专有（私有格式 + 加密 + 云端）越不可迁移。** 做端侧 AI 复刻时，按"公开化程度"排优先级最有效。

---

## 五、关键洞察

### 洞察 1 · 端侧 AI 是"多模型 + 多格式 + 多供应商"的异构集合

不是一个大模型，而是**上百个小模型 + 多种格式 + 多个供应商 SDK** 的集合。端侧追求低延迟 + 隐私 + 离线，用**模型专业化**换效率——每个功能一个专用小模型，而不是一个通用大模型。

### 洞察 2 · 私有格式 + 加密是端侧 IP 保护的双层防线

自研格式防提取，加密防独立运行。这让端侧 AI 的"核心能力"（尤其语音 / 声纹）几乎不可被第三方复刻——这是厂商护城河，也是可迁移性的根本边界。

### 洞察 3 · memory 是端侧 AI 的"隐性主战场"

表面看端侧 AI 是感知（视觉 / 语音），但**memory + 搜索**（行为画像 / 实体 RAG / 对话历史 / 生活场景）才是"个性化"的根基。完整的端侧 AI memory 通常是 **5 层异构系统**（向量库 + 对话库 + 结构化库 + 词典 + 规则库）。

### 洞察 4 · 可迁移性 ≈ "公开化程度"

越公开越可迁移，越专有越不可迁移。这是端侧 AI 复刻的**第一性原理**——按公开化程度排优先级，而非按功能重要性。

---

## 六、与 work4ai 的对接

| 本文档讲的 | work4ai 深度版 |
|---|---|
| 端侧 OCR（CRNN / DBNet）| [`讲透NLP`](../讲透NLP/)（分词 / NER）+ 未来 CV 系列 |
| 端侧 NLP（FastText / 分词）| [`讲透NLP/03-N元语法`](./讲透NLP/03-N元语法语言模型.md) + [`讲透NLP/05-词嵌入`](./讲透NLP/05-词嵌入-word2vec与GloVe.md) |
| 端侧向量检索（FAISS 类）| [`讲透RAG`](../讲透RAG/)（检索原理）|
| 端侧部署（量化 / 私有格式）| [`讲透GPU与系统级`](../讲透GPU与系统级/)（推理引擎 / 量化）|
| 端侧 memory 架构 | [`讲透Agent/04-记忆机制`](./讲透Agent/04-记忆机制.md) |

---

## 七、文档说明

- 本文档基于对端侧 AI 系统的**架构级调研**，已**完全脱敏**
- **不包含**任何具体厂商、产品名、SDK 名、类名、模型文件名、内部代号、逆向证据、APK 类列表
- 保留的是**通用端侧 AI 架构概念**（功能域分类 + 部署模式 + memory/搜索架构 + 可迁移性）
- 涉及的模型架构（CRNN / DBNet / YOLO / MobileNet / FastText / SegNeXt 等）均为**公开学术模型**，非任何厂商专有
- 通用工具（Trie / AC 自动机 / SQLite / FAISS）均为**公开开源工具**

> 来源: [https://deepwiki.com/deepseek-ai/FlashMLA/4.5-sparse-attention-implementation](https://deepwiki.com/deepseek-ai/FlashMLA/4.5-sparse-attention-implementation)
> DeepWiki deepseek-ai/FlashMLA

# Sparse Attention Implementation

  Relevant source files 
 - [README.md](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1)
 - [csrc/sm90/decode/sparse_fp8/splitkv_mla.h](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/csrc/sm90/decode/sparse_fp8/splitkv_mla.h)
 - [csrc/sm90/prefill/sparse/fwd.h](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/csrc/sm90/prefill/sparse/fwd.h)
 - [docs/20250929-hopper-fp8-sparse-deep-dive.md](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/docs/20250929-hopper-fp8-sparse-deep-dive.md?plain=1)
 - [flash_mla/flash_mla_interface.py](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py)
 
  
## Purpose and Scope

 This document explains how FlashMLA implements token-level sparse attention, which powers DeepSeek Sparse Attention (DSA) as introduced in the DeepSeek V3.2 paper. Sparse attention allows the model to attend to only a selected subset of tokens, reducing computational cost while maintaining accuracy.

 For low-level kernel implementations, see [SM90 Kernels](https://deepwiki.com/deepseek-ai/FlashMLA/5.1-sm90-kernels-(hopper)) and [SM100 Kernels](https://deepwiki.com/deepseek-ai/FlashMLA/5.2-sm100-kernels-(blackwell)). For FP8 quantization details specific to sparse decode, see [FP8 KV Cache Format and Quantization](https://deepwiki.com/deepseek-ai/FlashMLA/4.2-fp8-kv-cache-format-and-quantization).

 Sources: [README.md1-53](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L1-L53) [docs/20250929-hopper-fp8-sparse-deep-dive.md1-52](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/docs/20250929-hopper-fp8-sparse-deep-dive.md?plain=1#L1-L52)

 
## Overview: Token-Level Sparse Attention

 FlashMLA implements **token-level sparse attention**, where each query token attends to a specific subset of key-value tokens selected via an `indices` tensor. This differs from other sparsity patterns (e.g., block-sparse, sliding window) by allowing arbitrary per-token selection.

 The sparse attention mechanism is used in two phases:

 
| Phase | Kernel | Primary Use Case | KV Cache Format |
|---|---|---|---|
| Sparse Decode | sparse_decode_fwd | Token generation with long context | FP8 quantized (656 bytes/token) |
| Sparse Prefill | sparse_prefill_fwd | Initial prompt processing | Direct bfloat16 computation |

 The key insight is that by attending to only `topk` tokens (typically 2048-32768) instead of all tokens in context, the kernel can maintain high throughput even with 128K token contexts while preserving accuracy through careful token selection.

 Sources: [README.md7-21](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L7-L21) [flash_mla/flash_mla_interface.py53-212](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L53-L212)

 
## The Indices Tensor: Core Data Structure

 The `indices` tensor is the central mechanism for specifying which tokens each query should attend to. Its format differs between decode and prefill operations.

 
### Decode Phase Indices Format

 
```
Shape: (batch_size, seq_len_q, topk)
Dtype: torch.int32
```

 For decode operations with paged KV cache, the indices encode both the page block location and the offset within that block:

 
```
indices_in_kvcache[b][q][k] = block_index * page_block_size + offset_in_block
```

 Where:

 
 - `block_index`: Index into the block table (which pages are used).
 - `offset_in_block`: Position within the token storage.
 - `page_block_size`: Number of tokens per page (typically 16-32).
 
 **Invalid entries** are marked with `-1` to indicate padding.

 Sources: [README.md125-131](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L125-L131) [flash_mla/flash_mla_interface.py85-87](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L85-L87)

 
### Prefill Phase Indices Format

 
```
Shape: (s_q, h_kv, topk)
Dtype: torch.int32
```

 For prefill operations, indices directly reference token positions in the contiguous KV tensor:

 
```
indices[i][j][k] = position of k-th attended token for i-th query, j-th KV head
```

 Invalid entries can be set to `-1` or any value `>= s_kv`.

 Sources: [README.md142-149](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L142-L149) [flash_mla/flash_mla_interface.py189-191](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L189-L191)

 
## Sparse Decode Implementation Flow

 The sparse decode path is taken when the `indices` parameter is provided to `flash_mla_with_kvcache`.

 
```

```

 **Sparse Decode Requirements:**

 The sparse decode path enforces strict requirements at [flash_mla/flash_mla_interface.py152-160](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L152-L160):

 
 - `is_fp8_kvcache` must be `True` (no bfloat16 sparse decode).
 - `causal` must be `False` (incompatible with sparse indices).
 - `indices` shape must match `(b, s_q, topk)`.
 - FP8 KV cache must use the 656-byte per token format.
 
 Sources: [flash_mla/flash_mla_interface.py53-173](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L53-L173)

 
## Sparse Prefill Implementation Flow

 Sparse prefill is invoked directly via `flash_mla_sparse_fwd` without the decode-specific metadata machinery.

 
```

```

 **Key Differences from Decode:**

 
 - No paged memory management (direct tensor indexing).
 - No FP8 quantization (operates on bfloat16).
 - Returns `max_logits` in addition to `out` and `lse`.
 - No batching support (must reshape inputs for multi-batch).
 
 Sources: [flash_mla/flash_mla_interface.py176-212](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L176-L212) [README.md139-173](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L139-L173)

 
## Index Resolution and Memory Access Pattern

 The sparse attention kernels must efficiently resolve indices to memory locations, especially for the decode phase with paged KV cache.

 
### Decode: Paged Memory Resolution

 
```

```

 **Important Implementation Detail:**

 The sparse decode kernels encode the block table lookup directly into the indices at [README.md129](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L129-L129):

 
> "Since the index of the page block has already been encoded into `indices_in_kvcache`, the kernel does not require the `block_table` parameter."

 This means the indices tensor pre-computes `block_table[b][block_idx] * page_block_size + offset`, allowing the kernel to directly compute physical addresses without an extra indirection.

 Sources: [README.md125-131](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L125-L131) [flash_mla/flash_mla_interface.py74-90](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L74-L90)

 
### Prefill: Direct Indexing

 For prefill, the memory access pattern is simpler:

 
```

```

 The kernel implements this gather operation efficiently using:

 
 - Coalesced global memory loads where possible.
 - Shared memory staging for reused KV tokens.
 - TMA operations for bulk data movement (SM90/SM100).
 
 Sources: [README.md154-169](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L154-L169)

 
## Configuration Validation and Metadata Caching

 The `FlashMLASchedMeta` class caches configuration to avoid recomputation across decode steps:

 
```

```

 The validation at [flash_mla/flash_mla_interface.py137-149](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L137-L149) ensures:

 
 - Batch size `b` remains constant.
 - Query sequence length `s_q` remains constant.
 - Number of query heads `h_q` remains constant.
 - Page block size remains constant.
 - Sparsity configuration (`topk`, `is_fp8_kvcache`) remains constant.
 
 This allows the scheduler metadata and split-KV configuration to be reused across decode steps, eliminating redundant computation.

 Sources: [flash_mla/flash_mla_interface.py8-51](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L8-L51) [flash_mla/flash_mla_interface.py115-150](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L115-L150)

 
## Optional Features for Sparse Attention

 
### Dynamic TopK Length

 Both sparse decode and prefill support dynamic `topk_length` per query:

 
```

```

 When provided, only the leftmost `topk_length[i]` indices are processed for the i-th query, saving computation when actual attention breadth varies.

 Sources: [flash_mla/flash_mla_interface.py68-69](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L68-L69) [flash_mla/flash_mla_interface.py183](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L183-L183) [flash_mla/flash_mla_interface.py198-199](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L198-L199)

 
### Attention Sink Mechanism

 The `attn_sink` parameter modifies the final output scaling:

 
```

```

 When provided, output is scaled by:

 
```
output *= exp(lse) / (exp(lse) + exp(attn_sink))
```

 Setting `attn_sink[i] = +inf` forces the i-th head's output to zero. This has no effect on the returned `lse` value.

 Sources: [flash_mla/flash_mla_interface.py88](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L88-L88) [flash_mla/flash_mla_interface.py194-197](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L194-L197)

 
### Extra KV Cache Support

 Sparse decode supports attending to an additional set of tokens via `extra_k_cache` and `extra_indices_in_kvcache`:

 
```

```

 This enables hybrid attention patterns where different cache structures are used for different token subsets.

 Sources: [flash_mla/flash_mla_interface.py66-69](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L66-L69) [flash_mla/flash_mla_interface.py89-90](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/flash_mla/flash_mla_interface.py#L89-L90)

 
## Performance Characteristics

 Sparse attention achieves significant performance on compute-bound workloads:

 
| Configuration | Architecture | Performance |
|---|---|---|
| Sparse Decode (topk=2048) | H800 SXM5 | 410 TFlops |
| Sparse Decode (topk=32768) | H800 SXM5 | 460 TFlops |
| Sparse Prefill | H800 SXM5 | 640 TFlops |
| Sparse Prefill | B200 | 1450 TFlops |

 The sparse decode kernel achieves efficiency through the crossover technique (see [Kernel Performance Optimizations](https://deepwiki.com/deepseek-ai/FlashMLA/4.4-kernel-performance-optimizations)), which uses Distributed Shared Memory to eliminate the dequantization bottleneck.

 For context lengths > 3000, the sparse decode kernel with topk=2048 achieves comparable or better performance than dense decode with full context, demonstrating the effectiveness of DeepSeek Sparse Attention.

 Sources: [README.md35-36](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L35-L36) [README.md50-51](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/README.md?plain=1#L50-L51) [docs/20250929-hopper-fp8-sparse-deep-dive.md47-52](https://github.com/deepseek-ai/FlashMLA/blob/15f13e50/docs/20250929-hopper-fp8-sparse-deep-dive.md?plain=1#L47-L52)

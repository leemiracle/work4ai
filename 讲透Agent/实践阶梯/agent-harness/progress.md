# progress.md — 开发进度（每次 WRAP UP 更新）

> 规则：只记录有证据的进展；数字必须实测；失败/未验证项必须写明。

## 2026-08-24（第二次 WRAP UP）

- [x] L1 三版对照（l1.log）：few-shot 治塌缩 9→0、cat 1→6；重试收益有限；pass 全 0 暴露金标准粒度坑 → judge 改多重集匹配
- [x] L2 四文件建立（本目录）；冷启动接续验收动作留待下次会话实测
- [x] L3 四版跑完（eval30.log，34 条）：A pass6 / B pass8 / C pass7(重试27次净负) / D pass10(拆句重抽+2)
- [x] L4a 记忆层（04a_memory.py）：去重四动作+检索 5/5（2 条靠类别兜底）；词面去重抓不住同义重复（诚实边界）
- [x] L4b 意图识别（intent.log + intent_v1_naive.log）：naive scoring 27% 被"闲聊"先验压制 → PMI 校准 41% → 但 generation 65% 完胜 → 00 章"永远用 scoring"断言被证伪，修正为"受限 generation 为主+弃权闸"
- [~] E 版（词袋接地判据）容器跑批中：修"few-shot 示例自己通不过子串校验"的矛盾（03 章 §3.3）
- [~] L5 模型插槽 / 07 批判收尾：todo

## 2026-08-24（第一次）

- [x] 第零号摸底（smoke.log）：JSON 语法 5/5，语义 0/5（cat 越界 4/5 + fact 塌缩）；7.0 tok/s @1核，多线程无收益 → **B 版 few-shot 设计动机 + L4b scoring-only 架构依据**
- [x] 00 章设计决策落盘（矫正分层：语法/语义/行为三层）
- [~] L1 三版对照实验容器运行中（A 裸指令 / B few-shot / C few-shot+校验+重试）
      未验证：A/B/C 的 pass 具体数字，待 l1.log 回收
- [~] L2 四文件建立（本目录）
      未验证：「会话接续收益」需下次冷启动会话实测（新会话只读四文件能否 5 分钟内接上进度）

## 当前堵点

（无）


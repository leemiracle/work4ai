# 02 · DeepWiki 全量归档与刷新对照（2026-08-20 快照）

> 快照：https://deepwiki.com/deepseek-ai/deepseek-harness ，2026-08-20 索引于 commit `141eb6fe`（dsh 0.1.0-rc.8）——与本案例笔记钉版 HEAD、本地克隆完全一致，全部引文可本地复核。另附上游 Cordis 框架 wiki 全量归档（21 页）。
> 前篇：[01-DeepWiki对照与增补.md](01-DeepWiki对照与增补.md) 基于 2026-08-13 索引的 `47f94385`（rc.5）快照。rc.5→rc.8 间 wiki 随 647 commit 重新生成，本篇给出：① 全量归档（39/39 子页面，见 [wiki/](wiki/)）；② 新旧快照差异；③ 01 篇 §4 所记 4 项错误/缺口的逐条复核结论。
> 方法：curl 拉 SSR HTML → 解析 Next.js Flight 载荷中 `id:T<hexlen>` 行内嵌的 markdown 源串。每页 HTML 均内嵌全部 39 页，交叉提取去重：0 内容冲突、0 边界警告、39/39 页以 `Sources:` 自然收尾（无截断）。

## 1. 归档清单与校验

- **dsh wiki：39/39 页**（10 章 + 29 子页），共 501,588 字节，逐页 sha1 与二级标题数登记在 [wiki/README.md](wiki/README.md) 完整性清单。
- **上游 Cordis wiki：21/21 页**（另 188,706 字节），归档在 [wiki-cordis-upstream/](wiki-cordis-upstream/README.md)（2026-08-16 索引于 upstream master `8cc9e33f`）。**版本警示**：dsh vendor 钉的是 `56b3d4f`（4.0.0-rc.7）+ 18 条本地修改，upstream wiki 较新——以 `vendor/README.md` 修改日志为差异基准读。
- 每页保留：`<details>Relevant source files</details>` 依据文件清单 + 正文 `[path:line-line]()` 引文 + 结尾 `Sources:` 汇总。
- 抽样本地验证（dsh 快照=本地 HEAD，路径应 100% 存在）：`docs/architecture.md`、`docs/event-producer-consumer.md`、`packages/core/agent-loop/src/agent.ts`、`vendor/README.md`、`packages/host/apiproxy/README.md`、`packages/llm/llm-pi-ai/src/adapter.ts`、`packages/schedule/` 等 12+ 路径全部命中；行级抽查 `docs/event-producer-consumer.md:12`（`agent/created` 行）、`docs/architecture.md:45`（`core/session` 行）内容吻合。

## 2. 新旧快照差异（rc.5 wiki → rc.8 wiki）

| 维度 | 旧快照（01 篇所读） | 新快照（本次归档） | 本地证据 |
|---|---|---|---|
| 模型接入面 | 仅 DeepSeek 官方 | **pi-ai 目录化 provider 全面进入**：3.5 章 20 处、Overview 11 处提及，sources 大量引用 `packages/llm/llm-pi-ai/*` | `packages/llm/llm-pi-ai/` 存在（catalog/config/adapter） |
| 能力接缝 | 无 directory-picker | 2.3 章引 `2026-07-28-directory-picker-capability-seam` Agent Note，`ctx.directoryPicker` 入接缝表 | `packages/host/directory-picker/` 存在 |
| schedule | 未覆盖 | 7.3 章引 `2026-08-05-durable-web-schedule` Note + `apps/web/tests/schedule-after.e2e.ts` | `packages/schedule/` 存在 |
| Web 设置面 | 未覆盖 | 6.4 章引 `ui-agent-preset`、`ui-settings-*` 系列（settings mirror/scope） | `packages/client/ui-agent-preset/` 存在 |
| CI 覆盖 | release.yml 未展开 | 8.3 章 sources 含 `.github/workflows/release.yml`、`scripts/run-gates.ts` | 两文件存在 |
| Glossary | 无 SESSION_FORMAT_VERSION | **新增 §SESSION_FORMAT_VERSION（Glossary.md:218）** | 与本地 `types.ts` 实测一致 |

## 3. 01 篇 §4 错误/缺口逐条复核

1. **`SESSION_FORMAT_VERSION=0` 缺失 → 已修复**。新 Glossary.md:218 起专节收录，且正确表述"预发布期钉 0、无兼容承诺"。
2. **事件名单复数不一致 → 仍存在**。新 2.3 章正文仍写 `tool/pre-execute`（单数，归档 `wiki/2.3-event-bus-and-capability-seams.md` 正文第 3.1 节第 3 步），而 2/3.1/3.2 章 Glossary 均写 `tools/pre-execute`。源码真值：`docs/event-producer-consumer.md:58` 与 `packages/core/tools/src/index.ts` 为复数。**读 2.3 章时按复数纠正。**
3. **校验库混淆 → 仍存在**。新 2.2 章仍称配置块由"Zod schemas generated from the TypeScript interfaces"校验（`wiki/2.2-plugin-composition-profiles-bundles-and-configuration.md` Type Safety 节），与 2.1 章 vendored schemastery 并存打架。实情不变：**loader/插件 Config 走 schemastery（Standard Schema），wire/API 载荷走 Zod**。
4. **覆盖缺口 → 部分修复**。release.yml、run-gates 已入 8.3 章引文与正文；per-lane vitest 配置（e2e/snapshot/web/web-stress 独立文件）仍只提 `vitest.config.ts` 双工程（thread-safe/process-bound），web/perf 工程未展开——本案例 README 命令表仍更全。

## 4. 与本地一手资料的分工（不变，重申）

DeepWiki 仍是"第二入口"：章节全、mermaid 图多、源文件链接丰富，适合**横穿式浏览**（尤其 6.x Web UI 与 8.x 测试基建，本地笔记当时未深读、wiki 提供了首证细节）；但引用行号与个别术语（§3 第 2/3 条）需回源码复核。**本案例笔记以本地实测为准；wiki 全量原文现已在 [wiki/](wiki/) 就地可查，不必再访问网站。**

## 5. 复验命令

```bash
$ cd 讲透Agent/Agent框架案例/deepseek-harness插件化框架/notes/06-deepwiki/wiki
$ ls *.md | wc -l                 # 40 = 39 页 + README
$ ls ../wiki-cordis-upstream/*.md | wc -l   # 22 = 21 页 + README
$ grep -n 'tool/pre-execute' 2.3-event-bus-and-capability-seams.md   # 复核错误②
$ cd ~/ai/agent/awesome-agents/repos/deepseek-harness && git log -1 --format=%h  # 141eb6fef8，与快照一致
```

→ 增补事实正文见 [01-DeepWiki对照与增补.md](01-DeepWiki对照与增补.md)；生态分析续见 [07-ecosystem](../07-ecosystem/01-dsh-plugin生态分析.md)

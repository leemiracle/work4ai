# quattro 文档映射 —— DeepWiki 39 页 → omarchy-quattro docs/ 覆盖对账（2026-08-25）

> **任务**：以 https://deepwiki.com/basecamp/omarchy 全部 39 子页为知识底座，补齐 quattro（`/data/usershare/linux-src/omarchy-quattro`，v4.0.0.alpha）`docs/` 的主题空缺。原则：**wiki 只做组织参考，每条写进文档前对 quattro 代码逐条核实**——wiki 部分页基于 v3 残留（见"失效页"），直接照抄会写错。
> **产出**：新增 10 页 docs/；既有 9 页与上游 fa955b 对齐或更新（theming/notifications 领先 wiki），不动。

## 覆盖矩阵

| DeepWiki 页 | quattro docs 落点 | 状态 |
|---|---|---|
| 1 Overview | `docs/file-layout.md` + `docs/cli-router.md`（既有） | ✅ 既有覆盖 |
| 2 Installation and Setup / 2.1 Installation Process | `agents/skills/install-scripts.md`（既有 skill） | ✅ 既有覆盖（过程性知识归 skill） |
| 2.2 Boot Management and Snapshots | **新增 `docs/boot-snapshots.md`** | 🆕 全核实（snapshot 127 语义/保留策略/initramfs NVIDIA+vconsole/refresh-limine） |
| 2.3 Session Services and Autostart | **新增 `docs/session-services.md`** | 🆕 全核实（autostart.lua 逐行/9 个用户单元/first-run enable） |
| 3 User Interface / 3.1 Omarchy Menu System | `docs/menu.md` + `docs/omarchy-shell.md`（既有） | ✅ 既有覆盖 |
| 3.2 Waybar Status Bar | — | ⛔ v3 残留：quattro 无 waybar，栏=Quickshell `shell/plugins/bar/`，见 omarchy-shell.md |
| 3.3 Walker Application Launcher | — | ⛔ v3 残留：quattro 无 walker，启动=Quickshell 菜单（menu.md） |
| 4 Desktop Environment / 4.1 Hyprland Configuration | **新增 `docs/hyprland.md`** | 🆕 全核实（三层 require/o API/规则三阶段/launch-or-focus/toggles） |
| 4.2 Window Management Rules | 并入 `docs/hyprland.md` §Window rules | 🆕 |
| 4.3 Keybindings Reference | 并入 `docs/hyprland.md`（架构层；用户层按键在 `manual/07-hotkeys.md` 既有） | 🆕 |
| 4.4 Screensaver System | **新增 `docs/screensaver.md`** | 🆕 全核实（ttfx 循环/终端白名单/screensaver-off） |
| 5 / 5.1 / 5.2 Screen Capture and Recording | **新增 `docs/capture.md`** | 🆕 全核实（capture-* 8 命令参数表/grim+gpu-screen-recorder/测试锚点） |
| 6 Package Management / 6.1 Package Repositories | **新增 `docs/package-sources.md`** | 🆕 全核实（三源/四通道/channel-current 判定/refresh-pacman+hook/guard 豁免） |
| 6.2 Base Package Manifest | 并入 `docs/package-sources.md` §Manifests and preinstalls | 🆕（waybar/mako 行弃用，quattro 移除清单实测） |
| 6.3 Interactive Package Tools | 并入 `docs/package-sources.md` §pkg family | 🆕（脚本级 vs fzf TUI 两层） |
| 6.4 Update System | `docs/update-process.md`（既有） | ✅ 既有覆盖 |
| 7 / 7.1 / 7.2 Customization and Theming | `docs/theming.md`（既有，**领先 wiki**：installed-theme 安全过滤节 wiki 无） | ✅ |
| 8 / 8.1 / 8.2 / 8.3 Shell Environment | **新增 `docs/shell-environment.md`** | 🆕 全核实（rc 链/别名实测含 quattro 新增 a/h/ic/mup/fns 8 文件/tmux） |
| 9.1 Development Tools Installation | 并入 `docs/shell-environment.md`（mise 层）+ `docs/ai-agents.md`（agent 安装） | 🆕（dev-env 18 语言实测） |
| 9.2 Authentication and Security | 并入 `docs/hardware.md` §Login and fingerprint auth | 🆕（pam_fprintd/apply-lock/sddm keyring 实测） |
| 9.3 Hardware-Specific Configurations | **新增 `docs/hardware.md`** | 🆕 全核实（26 个 hw-* 谓词/T2/混合GPU/亮度三级/指纹 sysfs） |
| 10.1 Configuration Refresh System | `docs/file-layout.md`（既有 seed/resync/refresh） | ✅ 既有覆盖 |
| 10.2 Migration System | `docs/update-process.md`（既有） | ✅ |
| 10.3 AI Agent Integration | **新增 `docs/ai-agents.md`** | 🆕 全核实（10 agent 名录/quattro 差异 agy·ori/无值守旗标表/usage 管线） |
| 10.4 Quickshell Plugin Architecture | `docs/omarchy-shell.md`（既有） | ✅ 既有覆盖 |
| 11 Glossary | **新增 `docs/glossary.md`** | 🆕（术语全部换算到 quattro 实体并交叉链接） |

## 既有 9 页处置

`audio-tuning / cli-router / file-layout / menu / omarchy-shell / testing / update-process` 与上游 fa955b **逐字节一致**（不动）；`theming / notifications` 为 quattro 领先版本（installed-theme 安全节、D-Bus 直调通知），**保持不动**。

## wiki 对 quattro 的失实处（写文档时已规避）

1. 3.2/3.3/6.2 的 Waybar/Walker/mako 断言（v3 残留，quattro 全无）。
2. 10.3 的 agent 名录（wiki: gemini 等；quattro: `agy`=Antigravity、新增 `ori`，无 gemini CLI 名）。
3. 10.3 的 omarchy-agent 旗标（quattro 多 `--pick`；prompt 走独立 `omarchy-agent-prompt` 防 shadow）。
4. 6.1 的 refresh-pacman（quattro 额外备份 mirrorlist、经 `OMARCHY_UPDATE_PACMAN=1` 调 pacman）。
5. 6.2 preinstalls 机制（wiki: manifest 子集；quattro: webapp/tui remove-all + mise stub 清理 + 显式 13 包清单）。

> 来源: [https://deepwiki.com/ShangtongZhang/DeepRL/2.3-policy-gradient-methods](https://deepwiki.com/ShangtongZhang/DeepRL/2.3-policy-gradient-methods)
> DeepWiki ShangtongZhang/DeepRL | Last indexed: 23 April 2025 (c0968b

# Policy Gradient Methods

  Relevant source files 
 - [deep_rl/agent/A2C_agent.py](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py)
 - [deep_rl/agent/NStepDQN_agent.py](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/NStepDQN_agent.py)
 - [deep_rl/agent/OptionCritic_agent.py](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/OptionCritic_agent.py)
 
  This page documents the implementation of policy gradient methods in the DeepRL framework. The focus is on algorithms that directly optimize the policy through gradient ascent on the expected return, including Advantage Actor-Critic (A2C) and Proximal Policy Optimization (PPO). For information about actor-critic methods with deterministic policies like DDPG and TD3, see [Actor-Critic Methods](https://deepwiki.com/ShangtongZhang/DeepRL/2.4-actor-critic-methods).

 
## Policy Gradient Fundamentals

 Policy gradient methods learn a parameterized policy that directly maps states to action probabilities. Unlike value-based methods that learn a value function and derive a policy from it, policy gradient methods optimize the policy parameters directly to maximize expected returns.

 
```

```

 Sources: [deep_rl/agent/A2C_agent.py](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py)

 
### Core Concepts

 
 - **Direct Policy Parameterization**: Using neural networks to directly output action probabilities
 - **Policy Gradient Theorem**: Provides the mathematical foundation for updating policy parameters
 - **Variance Reduction**: Methods like using advantage functions and baselines to reduce variance
 - **Entropy Regularization**: Encouraging exploration by adding an entropy bonus
 
 
## Advantage Actor-Critic (A2C)

 A2C is a synchronous variant of the Actor-Critic architecture that uses multiple parallel workers to collect experiences. It maintains both a policy network (actor) and a value network (critic), often sharing parameters.

 
```

```

 Sources: [deep_rl/agent/A2C_agent.py12-65](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py#L12-L65)

 
### A2C Implementation

 The A2C agent maintains the following components:

 
 - **Network**: A neural network that outputs both policy and value predictions
 - **Optimizer**: Updates network parameters based on computed gradients
 - **Storage**: Temporarily stores experiences during rollouts
 
 The main training loop follows this process:

 
 - Collect experiences for a fixed number of steps (rollout length)
 - Compute advantages and returns for each timestep
 - Calculate policy, value, and entropy losses
 - Update network parameters through backpropagation
 
 The implementation supports both standard returns and Generalized Advantage Estimation (GAE).

 
```

```

 Sources: [deep_rl/agent/A2C_agent.py22-64](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py#L22-L64)

 
### Key Components in A2C

 
 - **Storage Mechanism**: The `Storage` component handles experience collection and advantage computation:

 
```

```
 - **Advantage Calculation**: A2C supports both standard returns and GAE:

 
```

```
 - **Loss Computation**: Policy, value, and entropy losses are combined:

 
```

```
 
 Sources: [deep_rl/agent/A2C_agent.py43-62](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py#L43-L62)

 
### Configuration Options

 A2C provides several configuration parameters:

 
| Parameter | Description |
|---|---|
| rollout_length | Number of steps to collect before updating |
| num_workers | Number of parallel workers |
| discount | Discount factor for future rewards |
| use_gae | Whether to use Generalized Advantage Estimation |
| gae_tau | GAE parameter for advantage calculation |
| entropy_weight | Weight for entropy bonus to encourage exploration |
| value_loss_weight | Weight for value function loss |
| gradient_clip | Maximum norm of gradients during updates |

 Sources: [deep_rl/agent/A2C_agent.py13-64](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py#L13-L64)

 
## Proximal Policy Optimization (PPO)

 PPO is an extension of policy gradient methods that uses a clipped surrogate objective to prevent too large policy updates. It offers improved sample efficiency and stability compared to standard policy gradient methods.

 
```

```

 
### PPO Key Components

 
 - **Clipped Surrogate Objective**: The core innovation of PPO is the clipped objective that prevents excessively large policy updates:

 
```

```
 - **Multiple Epochs**: Unlike A2C which performs a single update per rollout, PPO typically performs multiple optimization epochs on the same batch of data:

 
```

```
 - **Value Function Clipping**: Many PPO implementations also clip value function updates to stabilize learning.
 
 
## Shared Components and Utilities

 Policy gradient methods in the DeepRL framework share several components:

 
### Actor-Critic Network Architecture

 Most policy gradient implementations use a shared network architecture with separate heads for policy and value functions:

 
```

```

 Sources: [deep_rl/agent/A2C_agent.py27-28](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py#L27-L28)

 
### Experience Storage

 The `Storage` class handles experience collection and provides utilities for computing returns and advantages:

 
```

```

 
### Parallel Environments

 Policy gradient methods often use multiple parallel environments to collect diverse experiences:

 
```

```

 Sources: [deep_rl/agent/A2C_agent.py36](https://github.com/ShangtongZhang/DeepRL/blob/c0968b5c/deep_rl/agent/A2C_agent.py#L36-L36)

 
## Conclusion

 Policy gradient methods in the DeepRL framework follow a common architecture where a neural network outputs both policy and value predictions. The A2C implementation collects fixed-length rollouts and updates the network once per rollout, while PPO (though not explicitly shown in the provided code) would use clipped objectives and multiple optimization passes over the same data.

 These methods share core components like the storage mechanism for experience collection, advantage calculation, and similar loss functions combining policy, value, and entropy losses.

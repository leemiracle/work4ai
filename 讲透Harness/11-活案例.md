# 11 · ★ 活案例：五仓家族 + 本仓库自身 + 2026-08-26 部署实录

> 本章不引外部论文——全部是本仓库的**现场证据**（文件路径级）。

## 案例一：五仓家族——"换领域 = 换插件"的插件化验证

| 仓 | 领域 | 验证范式（V 组件特化） |
|---|---|---|
| deepseek-kernel-harness | Linux kernel | 编译期（checkpatch/sparse/build/boot 金字塔 L1-L4） |
| deepseek-rust-harness | Rust | 编译期 + graph 三查（goodhart_guards/全局冲突/patch_queue） |
| deepseek-rl-harness | RL 训练 | 学习方向（训练冒烟 + 同 seed 复现） |
| deepseek-llm-harness | LLM 生成 | 生成质量（三级降级冒烟，实测 Qwen2.5-0.5B NLL=4.96） |
| deepseek-agent-harness | Agent 系统 | 行为审计（轨迹 schema + authorize 自举红线） |
| **deepseek-universal-harness**（2026-08-26） | **通用研究（math 一等）** | **profile 路由：math 三层降级（Lean/sympy/固定seed数值）+ win-trap 库 + 引理检索**；首跑 e2e 抓真 bug（authorize 静态白名单拦 profile 工具→schema 动态注册修复）；下游对接 C1 攻坚 |

**命题验证**：换领域时宿主与引擎方言层（8 引擎）零改动——`Agent = Model + Harness` 里 Harness 又分层：`Harness = 宿主 + 领域插件`。

## 案例二：2026-08-26 部署实录（zhipu glm-5.3 coding plan 通道）

全程 40 分钟从冷启动到 e2e，踩坑三连全记录（[QUICKSTART](../../deepseek-agent-harness/QUICKSTART.md)）：

1. **self-test 零依赖全绿**（14+ 项：工具注册/authorize 拦 pip install/bash -n/L3 冒烟/账本只追加/方言注册表）
2. **坑 1**：`KH_ENGINE=zhipu` 只切方言不切 base_url → zhipu key 打 api.deepseek.com → 401。**解：KH_BASE_URL 必须同设**
3. **坑 2（通道判定，三环证据链）**：key 来源（auth.json zhipuai-coding-plan 尾 3KXf）+ 端点路径段（/api/**coding**/paas/v4）+ 差分实测（coding 端点 × glm-5.3 × thinking:disabled ✓ 0.9s 回显 glm-5.3；普通 paas 端点拒 thinking:disabled 400）
4. **意外发现（推翻 08-17 记忆档案）**：paas 端点现在也通 glm-5.3（默认 thinking）——两通道真实差异 = coding 接受 thinking:disabled。**带 thinking:disabled 的工具循环只能走 coding 通道**
5. **真实任务 e2e**：修 calc.py 的 `a - b → a + b` bug——agent 6 步（read→write→agent_test→run_verify→agent_test→done），**人工复核测试真过**（完成声明为真；Ch03 验证即证据的现场演示）

## 案例三：本仓库自身——讲透系列就是 harness 的活标本

| harness 组件 | 本仓库实现 |
|---|---|
| Instructions | AGENTS.md（写入规范/红线/脱敏规范） |
| State | `.agent/MEMORY.md` + `RESUME-*.md`（每单元断点续传——E3 的账本模式） |
| Verification | 三层宪法"bash 跑通"+ papers.md 引用核实 + gongwen 虚标教训（数字必须实测） |
| Scope | 项目红线（用例库分册不可重排/.workbuddy-ai 只读） |
| Lifecycle | 会话启动读 MEMORY/USER.md → 长任务查 .research/ → RESUME 收档 |
| Observability | `.agent/journal/` + 记忆块 updated 字段 |

**本单元自己的 E3 meta 教训（解析器 bug 高估幻觉）也是现场证据：实验代码 = harness，度量工具要过验证。**

## 案例四：论述命名的现实着落（2026-08-26 用户论述整合）

"harness 成为第一等公民"的论述谱系在已核实语料里的着落：隐式胶水→显式层（Guo Phase 2→3）→可优化对象（AHE + Harness-Bench）→ Meta/Self/Evo（Trellis spec 晋升 / AHE 观测性支柱 / AHE 进化外环）——完整映射表见 [README](../README.md)，**论述性命名不虚构引用**是本单元的宪法级纪律。

→ 下一章：[12 · 不足与展望](./12-不足与展望.md)

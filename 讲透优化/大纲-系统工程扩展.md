# 系统工程扩展 · 大纲部（45/46/47）

## 45 计算存储通信三角 🟡
①比算力更常见的两堵墙——存储墙与通信墙。
②三角权衡的正式化（任何系统都在这三资源间换位）；**异构计算**（CPU+GPU+NPU+FPGA）要求优化算法感知硬件拓扑（NUMA 亲和/PCIe 层级/PIM 的新变量——讲透高性能计算 02/05 章的系统版）；**时空转换作为统一方言**：缓存=空间换时间、压缩=时间换空间、预取=提前的时间换并行空间——四两拨千斤的工程版权衡论。
③→26（桥章）/ 讲透高性能计算 03/05/06（三墙的物理全解）/ 60-65 应用层（同一三角的业务版）。
④补全位：NUMA 绑核 vs 跨核访存的延迟实测。

## 46 编译与自动调优 🟡
①编译器本身是优化器——AI 编译栈把搜索装进构建。
②TVM/MLIR 的分层 IR 哲学；AutoTVM/Ansor 的调度搜索（搜索空间构造+剪枝+实测回填）；学习式 cost model（Tiramisu 的跨硬件泛化——MLSys 2021 正典）；**auto-scheduling 与人工调优的 Bolt gap 永恒问题**（讲透高性能计算 C07 簇的清算视角）。
③→22（0 阶搜索的编译版）/ 27（原篇目并入此处：学习式优化谱系）/ 48（学习型系统的编译特例）。
④补全位：tvm te 算子的 schedule 空间手工探索记录。

## 47 绿色与近似计算 🟡
①性能的第二本账——每瓦特性能。
②能耗指标体系（FLOP/J、token/J——MLSys Sustainable AI 的宪法化）；**DVFS 动态调频**（电压-频率-性能三角，实时调度的能耗版）；**近似计算**（牺牲可控精度换数量级能耗——近存/低位宽/跳步计算的谱系，与 07 章混合精度数值的分界：近似=可错、量化=精确到格点）；绿色调度（碳强度感知的任务时间迁移）。
③→讲透高性能计算 10 章（能耗批判的扩展数学面）/ 07 混合精度（讲透高性能计算：位宽三重红利）/ 13 能耗簇。
④补全位：DVFS 三档的能效-延迟实测（如 CPU 频率调节实验）。
